---
baseline_commit: dfa9787
---

# Story 3.1: Taak Aanmaken (Kerngegevens) met Doelmoment-berekening

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As Evelien,
I want een nieuwe taak aanmaken met de verplichte kerngegevens,
so that Flowz er meteen een realistisch doelmoment vóór de deadline voor berekent.

## Acceptance Criteria

1. **Given** Evelien tikt op de "+"-knop vanaf een willekeurige pagina, **when** 2.1-taak-formulier opent, **then** toont het de verplichte velden `taak-subject-select` (vak, combo-select vrij aanmaakbaar), `taak-title-input`, `taak-type-select`, `taak-deadline-input`, `taak-difficulty-select` (default Gemiddeld), `taak-priority-select` (default Gemiddeld), `taak-session-duration-input`, **and** worden alle velden zowel on-blur als on-submit gevalideerd (UX-DR13): titel niet-leeg max 100 tekens, deadline niet in het verleden, sessieduur ≥ 5 min, vak/type verplicht.
2. **Given** Evelien vult de kerngegevens geldig in en klikt "Opslaan", **when** de server de taak opslaat (`POST /api/tasks`), **then** wordt een `Task`-rij aangemaakt (AD-3) en berekent `server/domain/scheduling/` een doelmoment: laatste geplande sessie vóór de deadline, met een buffer (percentage van de totale benodigde tijd) die groter is naarmate de taak moeilijker/groter is en kleiner naarmate de prioriteit hoger is (FR24), **and** wordt bij succes een flash-bevestiging getoond, de dagplanning direct bijgewerkt, en Evelien teruggestuurd naar de pagina van herkomst (FR10), **and** blijft de scheduling-berekening volledig server-side (AD-1) — de client vraagt en toont alleen, **and** roept de scheduling-service, indien Evelien een huiswerk-kleur heeft ingesteld (Epic 2, Story 2.3), voor elke nieuw geplande sessie de Calendar-sync-service aan om een bijbehorend event aan te maken.
3. **[TOEGEVOEGD 2026-09-05, Correct Course, sprint-change-proposal-2026-09-05.md]** **En plant in dezelfde berekening direct alle sessies vanaf nu tot en met het doelmoment** (aantal = totale benodigde tijd ÷ standaard sessieduur, naar boven afgerond), elk binnen een echt beschikbare-tijd-blok (AD-10) — niet alleen de eerstvolgende sessie. Dit is nodig zodat een tekort (benodigde tijd > beschikbare tijd tot de deadline) al bij het aanmaken zichtbaar wordt (FR15/UJ-6), niet pas nadat alle sessies één voor één zijn afgerond.

## Tasks / Subtasks

- [x] Task 1: Schema — `tasks` en `sessions` (AC: #2)
  - [x] Nieuwe tabel `tasks`: `id` (uuid pk), `userId` (FK → `users.id`), `subject` (text, vrij), `title` (text, max 100 gehandhaafd in de route), `type` (text: `'proefwerk' | 'so' | 'opdracht' | 'po'`), `deadline` (text, ISO-datum `YYYY-MM-DD` — zelfde vorm als `availableTimeExceptions.date`, geen tijdcomponent), `difficulty` (text: `'laag' | 'gemiddeld' | 'hoog'`, default `'gemiddeld'`), `priority` (text: zelfde enum, default `'gemiddeld'`), `defaultSessionDuration` (integer, minuten), `totalMinutes` (integer, **niet nullable** — zie Dev Notes voor waarom dit veld al in Story 3.1 bestaat), `description` (text, nullable, max 500 gehandhaafd in de route), `createdAt`/`updatedAt`.
  - [x] Nieuwe tabel `sessions`: `id` (uuid pk), `taskId` (FK → `tasks.id`), `startsAt` (text, ISO 8601 UTC datetime — **niet** alleen een datum, zie Dev Notes "Sessie-tijdstip"), `plannedMinutes` (integer), `createdAt`/`updatedAt`. Geen `actualMinutes`/status-kolom nu — Epic 4's sessie-runner voegt die later toe via een eigen migratie (schrijfpaden voor gepland vs. werkelijk blijven zo vanaf het begin gescheiden, Consistency Conventions), niet vooruitbouwen op een nog niet geanalyseerde behoefte.
  - [x] Migratie genereren (`drizzle-kit generate`, niet `push` — bekende table-recreation-bug tegen libSQL) en live toepassen tegen de Turso-database, geverifieerd (zelfde aanpak als elke eerdere schema-story: `PRAGMA table_info` via een tijdelijk, na gebruik verwijderd script). — migratie `0005_long_romulus.sql`, beide tabellen live bevestigd met alle verwachte kolommen.
- [x] Task 2: `server/domain/scheduling/` — doelmoment- en sessie-tijdstip-berekening (AC: #2, FR24)
  - [x] **Eerste echte inhoud van deze map** — Structural Seed reserveerde 'm al (`server/domain/scheduling/.gitkeep`). — geïmplementeerd als `doelmoment.ts` (bufferformule + doelmoment + dag-plaatsing) + `session-time.ts` (Amsterdam-anker + DST-conversie + stapeling), plus een nieuwe `getExceptionForDate`/`sumPlannedMinutesForUserOnDate`/`getDistinctSubjectsForUser` in de data-laag.
  - [x] Bufferpercentage-formule (FR24: "groter naarmate moeilijker/groter, kleiner naarmate prioriteit hoger" — geen exacte cijfers in PRD/architectuur, hieronder een beargumenteerd voorstel, zie Dev Notes "Bufferformule" voor de volledige redenering en het bijbehorende Open Question):
    ```
    BASE = 0.20
    DIFFICULTY_ADJUSTMENT = { laag: -0.05, gemiddeld: 0, hoog: +0.10 }
    PRIORITY_ADJUSTMENT   = { laag: 0, gemiddeld: -0.05, hoog: -0.10 }
    bufferPercentage = clamp(BASE + DIFFICULTY_ADJUSTMENT[difficulty] + PRIORITY_ADJUSTMENT[priority], 0.05, 0.40)
    bufferMinutes = totalMinutes * bufferPercentage
    ```
  - [x] Doelmoment-datum: haal het weekpatroon op (hergebruik `getOrCreateWeekPattern` uit `server/data/availability.ts`, Story 2.1), bereken `averageDailyAvailableMinutes` (gemiddelde van de 7 dagwaarden; bij een gemiddelde van 0 — nieuwe gebruiker heeft nog geen beschikbare tijd ingesteld — val terug op `bufferDays = 1` om delen door nul te voorkomen). `bufferDays = max(1, ceil(bufferMinutes / averageDailyAvailableMinutes))`. `doelmoment = deadline - bufferDays` (kalenderdagen, geen weekend-uitzondering). Is `doelmoment` vóór vandaag (deadline te dichtbij voor de berekende buffer), clamp naar vandaag.
  - [x] Dag-plaatsing: zoek vanaf `doelmoment` **terugwaarts** (richting vandaag, nooit voorbij vandaag) naar de eerste dag met effectief beschikbare tijd ≥ `defaultSessionDuration` — hergebruik het weekpatroon plus een **nieuwe** `getExceptionForDate(userId, date): Promise<number | null>` in `server/data/availability.ts` (bestaat nog niet; `getExceptionsForMonth` is er wel maar is maand-breed, hier is een gerichte per-datum-lookup nodig). Terugwaarts, niet voorwaarts: een dag met te weinig ruimte overslaan naar een latere dag zou de buffer juist verkleinen (risicovoller), overslaan naar een eerdere dag vergroot 'm (veiliger) en voldoet nog steeds aan "vóór de deadline". Wordt na terugzoeken tot vandaag niets gevonden: plaats de sessie toch op de oorspronkelijk berekende `doelmoment`-datum (best-effort) — **een écht tekort oplossen is expliciet Epic 6's taak, niet deze story's**, dus geen escalatielogica hier bouwen.
  - [x] Sessie-tijdstip (nodig voor de Calendar-sync-aanroep in AC #2, zie Dev Notes "Sessie-tijdstip" voor de volledige redenering): vast lokaal ankertijdstip **16:00 Europe/Amsterdam**. Voor de gekozen dag: zoek alle bestaande `sessions` van déze user op die datum (join via `tasks.userId`), tel hun `plannedMinutes` op, en start de nieuwe sessie na afloop daarvan (`anchor + som-bestaande-minuten`) — zo stapelen meerdere sessies op dezelfde dag na elkaar, geen overlap. Reken het lokale ankertijdstip om naar een correct UTC-instant (DST-bewust — Europe/Amsterdam wisselt tussen CET/CEST) via Node's ingebouwde `Intl.DateTimeFormat`-API (`timeZone: 'Europe/Amsterdam'`), **geen nieuwe dependency**. Dit is de eerste keer dat deze codebase server-side een IANA-tijdzone-conversie nodig heeft (Lambda draait zelf in UTC) — expliciet los live verifiëren rond een DST-omschakeldatum (bv. eind maart/eind oktober), niet aannemen dat het klopt.
- [x] Task 3: `server/domain/tasks/` — `createTask` (AC: #2)
  - [x] **Eerste echte inhoud van deze map** — Structural Seed reserveerde 'm al (`server/domain/tasks/.gitkeep`). — geïmplementeerd als `create-task.ts`.
  - [x] Orkestreert: Task-rij aanmaken (`totalMinutes` = `defaultSessionDuration` bij het aanmaken — zie Dev Notes, Story 3.2 herberekent dit veld later via deeltaken), Task 2's doelmoment-/plaatsings-/tijdstip-berekening aanroepen, één `Session`-rij aanmaken op de berekende datum/tijd.
  - [x] **Calendar-sync-aanroep (AC #2, laatste bullet):** ná het aanmaken van de Session, roep `createHomeworkEvent` uit `server/domain/calendar-sync/homework-events.ts` (Story 2.3, ongewijzigd herbruikt) aan met `{ sessionId: <de nieuwe Session-id>, subject, title, startsAt: <Session.startsAt>, endsAt: <startsAt + plannedMinutes> }`. Die functie is al zelf-bewakend op kleur + write-scope (Story 2.3-code-review) — hier dus geen eigen if-check nodig, gewoon aanroepen. **`googleEventId`-opslag is nog steeds expliciet buiten scope** (Story 2.3's eigen grens: geen mapping-tabel vooruitlopend op een datamodel-behoefte die pas ontstaat zodra een latere story een event moet kunnen bijwerken/verwijderen — voor déze story wordt een gecreëerd event dus nooit meer teruggevonden; dat is geen regressie, `Session`/`Task` bestonden nog niet toen Story 2.3 die grens trok).
  - [x] Synchroon binnen het request-pad (AD-1/AD-7) — geen achtergrondtaak; een falende Calendar-call mag de hele `POST /api/tasks`-aanroep laten falen (consistent met hoe Story 2.3's service zelf faalt: een gegooide `Error`, geen stille no-op tenzij kleur/scope ontbreken).
- [x] Task 4: `server/api/tasks` — routes (AC: #1, #2)
  - [x] `POST /api/tasks`: valideert alle 7 verplichte velden (zie Dev Notes "Validatieregels" voor de volledige tabel), 400 met de bestaande error-envelope (`server/domain/errors.ts`, `ErrorCodes.ValidationError`) bij een ongeldig veld — **niet** de UX-spec's per-veld `ERR_*`-codes als server-`code` gebruiken, zie Dev Notes "Error-codes: reconciliatie". Schrijfpad via `server/domain/` (mutatie-ownership-regel). Response: de aangemaakte `Task` (voldoende voor de front-end om de flash-bevestiging te tonen — geen aparte `GET` nodig). Inclusief server-side enum-validatie voor `difficulty`/`priority` (vooraf toegepaste code-review-les, zie Dev Notes).
  - [x] `GET /api/tasks/subjects`: retourneert de unieke `subject`-waarden uit deze user's bestaande `tasks`-rijen (`SELECT DISTINCT subject FROM tasks WHERE user_id = ?`), voor `taak-subject-select`'s suggestielijst (UX-spec's `subjects_list`-databron — geen aparte "Vak"-tabel, zie Dev Notes). Lege array bij een nieuwe gebruiker zonder taken — het veld blijft dan gewoon vrij typbaar.
- [x] Task 5: Front-end — `/taak/nieuw` (AC: #1, #2)
  - [x] Nieuwe pagina `app/pages/taak/nieuw.vue`, route exact zoals de UX-spec (`/taak/nieuw`), paginatitel "Nieuwe taak".
  - [x] Secties: `taak-header-section` (titel + sluiten-knop `taak-header-close`), `taak-core-section` (alle 7 kerngegevens-velden, exacte Object IDs uit de UX-spec), `taak-action-section` (`taak-cancel-link`, `taak-save-button`). **Ook** een minimale `taak-extra-section` met uitsluitend `taak-description-textarea` (Omschrijving) — zie Dev Notes "Omschrijving: waarom nu al" voor de redenering; Story 3.3 voegt `taak-needs-input` later toe aan dezelfde sectie. **Nog niet bouwen:** `taak-scope-section` (Deeltaken/Totale-tijd, Story 3.2) — sectiegrenzen dus bewust op 3 secties gehouden i.p.v. de volledige 2.1-mockup, geen lege placeholder-secties voor nog niet bestaande stories. — met een grep op de open/sluit-tags bevestigd dat geen enkele sectie in een andere sectie genest zit (de eigenlijke Story 2.2-les). **Correctie (code review 2026-08-01):** de eerdere formulering "alle vier secties zijn zusjes" was onnauwkeurig — `taak-header-section` is een zusje van het `<form>`-element, en pas daarbinnen zijn `taak-core-section`/`taak-extra-section`/`taak-action-section` onderling zusjes. Functioneel geen probleem (geen sectie-in-sectie-nesting, wat de eigenlijke eis is), maar de claim zelf klopte niet letterlijk.
  - [x] Validatie on-blur (titel, deadline, sessieduur, omschrijving) én on-submit (alle 7 verplichte velden + omschrijving), exacte regels uit de UX-spec (zie Dev Notes-tabel). Bij een submit-fout: spring naar het eerste veld met een fout (UX-spec, `aria-live="assertive"`).
  - [x] `taak-subject-select`: combo-select, haalt `GET /api/tasks/subjects` op bij het laden, vrij aanmaakbaar (geen match in de lijst → gewoon de getypte waarde gebruiken, geen aparte "aanmaken"-bevestiging nodig, dat is UI-suikergoed dat deze story niet hoeft te bouwen — een simpele combobox/datalist-achtige component volstaat). — geïmplementeerd als `<input list>` + `<datalist>`.
  - [x] `taak-header-close`/`taak-cancel-link`: ongewijzigd formulier → direct terug naar de pagina van herkomst; gewijzigd → bevestigingsdialoog ("Wil je stoppen? Je invoer gaat verloren"). "Pagina van herkomst" = `history.state.back`-patroon, exact hergebruikt van `beschikbare-tijd.vue`'s `terug()`-functie (Story 2.1), met fallback naar `/` — er bestaat nog geen navigatiemenu/"+"-knop ergens in de app (zie Dev Notes "Entry point: nog geen '+'-knop"), dus deze pagina is voorlopig alleen via een directe URL bereikbaar, exact zoals `beschikbare-tijd.vue` dat ook was vóórdat er een hamburgermenu kwam.
  - [x] `taak-save-button`: `Bezig`-state (spinner, disabled, overige velden niet bewerkbaar) tijdens de `POST`-call; bij succes flash-bevestiging + navigatie terug naar pagina van herkomst; bij serverfout inline foutmelding boven de actiebalk, knop weer actief, ingevulde data behouden (geen dataverlies bij een mislukte save). — flash-bevestiging als korte inline melding (`taak-save-confirmation`) vóór de navigatie: er bestaat nog geen cross-pagina-toastmechanisme elders in de app om op aan te sluiten.
  - [x] Gedeelde types (`CreateTaskInput`/`-Response`, `TaskType`/`Difficulty`/`Priority`-unions) naar `shared/types/tasks.d.ts` — niet lokaal in `app/` dupliceren (vaste gewoonte sinds Story 2.1).
- [x] Task 6: Verificatie
  - [x] `npm run typecheck` slaagt.
  - [x] `npx nuxt build` slaagt.
  - [x] Live geverifieerd tegen de dev-stage (sealed-cookie-techniek, zelfde als elke eerdere story): taak aanmaken met verschillende moeilijkheid/prioriteit-combinaties → bevestig dat de berekende `doelmoment`/sessie-datum plausibel verschuift (hogere moeilijkheid → grotere buffer → eerdere sessie-datum; hogere prioriteit → kleinere buffer → latere sessie-datum). — met `defaultSessionDuration: 25` was het verschil te klein (beide op de 1-dag-ondergrens); herhaald met `600` om het aantoonbaar te maken: hoog/laag → 3 dagen vóór de deadline, laag/hoog → 1 dag vóór de deadline (deadline 2026-12-10 → resp. 2026-12-07 en 2026-12-09). Testtaken/-sessies nadien verwijderd.
  - [x] Live geverifieerd: twee taken met sessies op dezelfde dag → tweede sessie start ná de eerste (stapeling), geen overlappende `startsAt`/`endsAt`. — `15:00:00Z` en `15:25:00Z` op dezelfde datum (25 min uit elkaar, exact de eerste sessie se duur).
  - [x] Live geverifieerd: taak aanmaken terwijl een huiswerk-kleur is ingesteld (Story 2.3) → een écht Calendar-event verschijnt (duidelijk testgemarkeerde titel, direct opgeruimd na verificatie — zelfde discipline als Story 2.3's Task 7); taak aanmaken zonder kleur → geen Calendar-call (hergebruikt Story 2.3's al bestaande zelf-bewaking, alleen bevestigen dat de aanroep zelf goed doorkomt). — 4 events rechtstreeks bij Google bevestigd (juiste titel-template "Huiswerk: {vak} — {titel}", juiste tijd) en verwijderd; kleur tijdelijk (buiten de app om, want wissen kan niet meer via de nu-verplichte PATCH-route) op `NULL` gezet voor het geen-kleur-scenario → taak werd aangemaakt, `GET`-zoekopdracht bij Google bevestigde geen nieuw event; kleur nadien hersteld naar "Druif" (Hillebrand's echte keuze).
  - [x] Live geverifieerd: DST-omschakeling — een sessiedatum kort vóór en kort ná een DST-omschakeldatum (bv. 2026-10-25) geeft in beide gevallen het correcte lokale 16:00-tijdstip terug in UTC (niet 15:00 of 17:00 door een DST-fout). — getest rond zowel de oktober- als de maart-omschakeling: 24/25/26 okt en 28/29/30 mrt 2026, alle zes conversies correct (CEST=+2, CET=+1, juiste kant van de omschakeling op de dag zelf omdat 16:00 lokaal ruim ná het omschakelmoment ligt).
  - [x] Alle validatieregels (7 verplichte velden + omschrijving) live getest, inclusief de invalid-deadline-in-het-verleden- en de te-korte-sessieduur-randgevallen. — deadline-in-verleden, sessieduur < 5, lege titel, ongeldig `type`, ongeldige `difficulty` stuk voor stuk bevestigd met de juiste 400-envelope.
  - [x] Geen secrets/placeholder-waarden in code/commits.
  - [x] **Niet live geverifieerd (eerlijk gemeld, niet verzwegen):** de daadwerkelijke pagina-rendering van `/taak/nieuw` in de browser — de Chrome-extensieverbinding was op het moment van verifiëren niet beschikbaar. Typecheck, build en de volledige API-laag (validatie, doelmoment-berekening, stapeling, Calendar-sync, DST) zijn wel end-to-end bevestigd; alleen de daadwerkelijke client-rendering (sectienesting is wel via grep bevestigd, zie Task 5) is nog niet met echte browserclicks getoetst.

- [x] Task 7: AD-10-rework — beschikbare tijd live uit de gekoppelde Google Calendar-agenda i.p.v. het oude weekpatroon (Correct Course 2026-09-02, implementation-note bij deze story in sprint-status.yaml; vervangt de Task 2-bron)
  - [x] **Aanleiding:** Story 2.1 (herzien) koppelt sindsdien `User.availabilityCalendarId` aan een door Evelien beheerde Google Calendar-agenda, maar liet `server/domain/scheduling/doelmoment.ts` bewust ongewijzigd op het oude `AvailableTimePattern`/`AvailableTimeException`-model draaien (zie 2.1's eigen Task 1.3 en Dev Notes "Scope-grens"). AD-10 (`ARCHITECTURE-SPINE.md`) is nu leidend: "er is geen aparte tabel meer" voor beschikbare tijd — de blokken in de gekoppelde agenda zíjn de beschikbare tijd, live en on-demand opgehaald (AD-4's pull-only-regel, nu ook voor de scheduling-engine zelf).
  - [x] **Blok-model, niet vrij/bezet-model:** volgens 4.1-beschikbare-tijd-instellen (herzien) beheert Evelien in de gekoppelde agenda zélf tijdblokken voor huiswerk — de events in die agenda ZIJN de beschikbare blokken (optellen), niet een vrij/bezet-rooster waar beschikbaarheid uit wordt afgeleid (dat laatste blijft `actual-availability.ts`'s eigen, aparte concern voor conflict-detectie tegen de overige agenda's, ongewijzigd door deze taak).
  - [x] Nieuw `server/domain/availability/calendar-blocks.ts`: `getAvailableMinutesForDate(userId: string, date: string): Promise<number>`.
    - Haalt `user.availabilityCalendarId` op (`getUserById`, al bestaand). **Geen agenda gekoppeld:** retourneer `0` (zelfde effectieve uitkomst als het oude model voor een nieuwe gebruiker zonder ingesteld weekpatroon — `calculateDoelmoment`/`findSessionDate` vallen daar al op terug, zie Open Questions hieronder voor de vraag of dit ook een Notification verdient).
    - **Wel gekoppeld:** haal de events uit uitsluitend die ene agenda op voor de opgegeven datum (Amsterdam-dag-venster `00:00`–`23:59`, zelfde `amsterdamLocalToUtcIso`-patroon als `getTodayEvents` in `day-events.ts`) — exporteer daarvoor `getEventsForCalendar` uit `server/domain/calendar-sync/day-events.ts` (nu module-privaat) i.p.v. een tweede events-ophaal-implementatie te schrijven.
    - Tel de duur van elke **timed** event op (hergebruik `isTimedEvent` uit `actual-availability.ts`, geëxporteerd) — eerst overlappende blokken mergen vóór het optellen (zelfde reden als `actual-availability.ts`'s `mergedOverlapMinutes`: een dubbel ingepland blok mag niet dubbel meetellen; een kleine, losse merge-helper hier, geen sessie-vergelijking nodig zoals bij die functie).
    - **Mislukte Calendar-call:** gooi een expliciete `Error` (zelfde "moet zichtbaar falen"-precedent als `calculateActualAvailableMinutes`) — AD-10 verbiedt expliciet een stille terugval op verouderde/aangenomen beschikbaarheid. **Geen aparte Notification-plumbing nodig hiervoor** (AD-6 bindt uitsluitend aan UJ-6/7/8's tekort-/escalatiemeldingen, niet aan UJ-2's taak-aanmaken-flow, zie Dev Notes "Foutafhandeling: bestaande 500-envelope, geen Notification" — gecorrigeerd t.o.v. de eerdere formulering hieronder): de `Error` propageert gewoon naar `server/api/tasks.post.ts`'s bestaande catch-all, exact zoals Task 3's Calendar-sync-fout dat al deed (500 `ErrorCodes.InternalError`, generieke boodschap). Geen routewijziging nodig.
  - [x] `doelmoment.ts` herbouwen op de nieuwe bron — **exacte functiehandtekeningen ongewijzigd laten** zodat alle bestaande consumenten (`actual-availability.ts`, `conflict-detection.ts`, `session-time-check.ts`, `shortfall.ts`, `week-overview.ts`, `ordering.ts`, `recalculate.ts` — Epic 3/6's gedeelde engine) vanzelf meeliften zonder eigen wijziging:
    - `availableMinutesForDate(userId, date)`: verwijder de `pattern`-parameter en de `getExceptionForDate`/`getOrCreateWeekPattern`-aanroepen; roep in plaats daarvan `getAvailableMinutesForDate` aan. Update de handvol aanroepers van de oude `pattern`-parameter-variant.
    - `findSessionDate`: verwijder de `getOrCreateWeekPattern`-aanroep vooraf (geen patroon meer om te cachen); elke iteratie van de terugwaartse zoeklus roept nu rechtstreeks `availableMinutesForDate` aan. **Let op:** dit maakt elke iteratie een live Calendar-call i.p.v. een in-memory patroon-lookup — bij een volle `MAX_SEARCH_DAYS = 90`-zoeklus is dat potentieel 90 sequentiële Calendar-aanroepen binnen één synchroon request-pad (AD-1/AD-7). Was al zo'n risico vóór deze rework (databasecalls i.p.v. Calendar-calls, zelfde volgorde-grootte), dus geen nieuwe regressie, maar wel duidelijker zichtbaar nu; geen actie hier verwacht, alleen niet opnieuw als verrassing tegenkomen bij een trage live-test.
    - `averageDailyAvailableMinutes(userId)`: kan niet langer op een statisch 7-daags patroon leunen (de agenda kent geen "vaste weekdag"-structuur). Bereken in plaats daarvan het gemiddelde over een vast venster van de eerstvolgende N kalenderdagen vanaf vandaag (voorstel: **N = 14**, twee weken — lang genoeg om een enkele weekend-uitschieter te dempen, kort genoeg om niet te veel Calendar-calls per `createTask`-aanroep te doen) door `getAvailableMinutesForDate` N keer aan te roepen en te middelen. **Dit getal (14) is een productbeslissing zonder vastgelegd PRD-cijfer, zelfde categorie als de bufferformule-constanten** — zie Open Questions.
  - [x] `server/data/availability.ts`'s `getExceptionForDate`/`getOrCreateWeekPattern` **niet verwijderen** in deze taak — `apply-recommendation.ts` en `server/api/day/shortfall.post.ts` (Story 6.1/6.2's "tijd verruimen"-pad) gebruiken het oude exceptie-model nog actief; die krijgen hun eigen AD-10-rework bij Story 6.1/6.2 (staat al als open implementation-note in `sprint-status.yaml`). Uitsluitend het lees-pad in `doelmoment.ts` wordt hier verlegd.
  - [x] Typecheck (`npm run typecheck`) en build (`npx nuxt build`) slagen.
  - [x] Live geverifieerd tegen de dev-stage (deploy `npx sst deploy --stage dev`, met expliciete toestemming van Hillebrand): een taak aanmaken **zonder** gekoppelde agenda (uitgangstoestand, `availabilityCalendarId: null`) → 0 beschikbare minuten, `bufferDays = 1`, doelmoment/sessie-datum valt correct terug op `deadline - 1 dag` (bevestigd met twee testtaken, deadlines 2026-12-15 en 2026-09-09 → sessies resp. op 2026-12-14 en 2026-09-08 via `/api/week`), geen crash. Vervolgens **wél** gekoppeld (tijdelijk, Hillebrand's eigen "Gezin"-agenda via `PATCH /api/settings/availability-calendar`) → een taak aanmaken haalt zonder fouten echte Google Calendar-events op via de nieuwe `getEventsForCalendar`/`getAvailableMinutesForDate`-aanroep (geen exception tegen echte API-response-vormen); zichtbaar effect op de plaatsing kon met déze specifieke agenda niet worden aangetoond (weinig/geen timed events in het geteste venster), de mechaniek zelf (live Calendar-call → som → doelmoment) is wel end-to-end bevestigd zonder crash. **Niet uitgevoerd (eerlijk gemeld, niet verzwegen):** de mislukte-Calendar-call-scenario met een kunstmatig gecorrumpeerd token — het ophalen van het echte, ontsleutelde access-token naar een lokaal script werd terecht door de eigen veiligheidsclassifier geblokkeerd (een live OAuth-token met Calendar-write-scope hoort niet naar schijf te gaan); dit pad leunt op exact dezelfde, al herhaaldelijk beproefde `getEventsForCalendar`/`calendarGetMetVerversing`-foutafhandeling die `getTodayEvents`/`actual-availability.ts` al gebruiken, dus laag risico, maar niet zelf live getoetst voor déze nieuwe aanroeper. Testdata opgeruimd (beide testtaken verwijderd, `availabilityCalendarId` teruggezet naar `null` via een directe DB-update na overleg met Hillebrand omdat er bewust geen ontkoppel-endpoint bestaat, zie Story 2.1's PATCH-route-commentaar), alle tijdelijke scripts verwijderd.

- [x] Task 8: Meerdere sessies vooruit plannen binnen echte beschikbare-tijd-blokken (Correct Course 2026-09-05, `sprint-change-proposal-2026-09-05.md`; AC: #3)
  - [x] **Aanleiding:** live-verificatie toonde dat een taak van 7 uur (25 min/sessie ⇒ ~17 sessies nodig) met een deadline over ~1 week maar 1 sessie gepland kreeg, en die sessie stond niet eens binnen een echt studietijd-blok (aggregaat-minuten i.p.v. blok-intervallen). AD-1/AD-3 vereisten dit 1:N-model al, dit is een correctie, geen nieuwe architectuurbeslissing.
  - [x] Nieuw in `server/domain/availability/calendar-blocks.ts`: `getAvailableBlocksForDate(userId, date): Promise<{ start, end }[]>` — naast (niet in plaats van, `getAvailableMinutesForDate` blijft in gebruik door o.a. `shortfall.ts`/`week-overview.ts`) de bestaande aggregaat-functie. Retourneert de gemergde, op het dagvenster geclampte tijdsintervallen. Geen agenda gekoppeld → lege array. De gedeelde events/window-ophaal-logica is geëxtraheerd naar een nieuwe, module-interne `getTimedBlocksAndWindow`, hergebruikt door beide functies.
  - [x] Nieuw in `server/domain/scheduling/doelmoment.ts`: `planSessionSlots(userId, today, doelmoment, totalMinutes, sessionMinutes, excludeTaskIds)` — plant **voorwaarts vanaf vandaag** (niet langer terugwaarts vanaf het doelmoment, zoals de oude enkele-sessie `findSessionDate`, die ongewijzigd blijft bestaan maar geen aanroepers meer heeft) alle benodigde sessies, via een nieuwe `packSlotsInBlocks`-helper die sessies binnen de daadwerkelijke blokgrenzen past (na het overslaan van al-bezette tijd op die dag). Best-effort verder dan het doelmoment (tot `MAX_PLAN_SEARCH_DAYS = 90`) als het nog niet past; een terugval-pad (gestapeld vanaf het vaste anker, blokgrenzen genegeerd) als er in het hele venster geen enkel blok is (geen/lege gekoppelde agenda) — zelfde precedent als het oude model voor dat scenario.
  - [x] `createTaskAndSession` (`server/data/tasks.ts`) is `createTaskAndSessions` geworden: neemt vooraf-berekende `sessionSlots` aan (geen anker-/stapelingsrekenwerk meer in de data-laag — dat vereist nu live Calendar-aanroepen over mogelijk meerdere dagen, hoort niet in een DB-transactie) en insert de `Task`-rij + **N** `Session`-rijen + Subtaken atomair.
  - [x] `server/domain/tasks/create-task.ts`: roept voor elke betrokken datum `syncHomeworkBlocksForDate` aan (Story 2.5's bestaande, per-datum herberekenende mechanisme, hergebruikt) i.p.v. voor slechts 1 sessie.
  - [x] Geen wijziging aan `getAvailableMinutesForDate`, `averageDailyAvailableMinutes`, de bufferformule, of het doelmoment zelf (dat blijft "laatste sessie vóór de deadline") — alleen wat daartussen gebeurt (1 sessie → N sessies) verandert.
  - [x] **Bredere blast radius ontdekt tijdens implementatie (na overleg met Hillebrand — "volledige rework nu, stap voor stap"):** de "precies 1 sessie per taak"-aanname bleek niet alleen in Story 3.5's `recalculateTaskPlanning` te zitten, maar rechtstreeks in **7 aanroepplekken over 6 bestanden**. Alle 7 zijn meegenomen, groter dan het oorspronkelijke voorstel inschatte:
    - `server/data/tasks.ts`: `getSessionForTask` gooit niet langer een `Error` bij >1 sessie — geeft voortaan de **eerstvolgende** sessie terug (kleinste `startsAt`), wat voor elke bestaande aanroeper semantisch al de bedoeling was ("de sessie die nu aan de beurt is"). Nieuw: `getSessionsForTask` (alle sessies, chronologisch), `deleteTaskAndSessions` (vervangt `deleteTaskAndSession` — verwijdert ALLE sessies van een taak, niet één specifieke), `insertSessionsForTask`, `deleteSessionsById`. `sumPlannedMinutesForUserOnDate`'s `excludeTaskId` accepteert nu ook een array.
    - `server/domain/scheduling/recalculate.ts` (Story 3.5): `recalculateTaskPlanning` regenereert voortaan de volledige sessiereeks (nieuwe `sessions`-array i.p.v. enkelvoud `session` in de return-waarde) — de eerstvolgende bestaande sessie wordt altijd in-place herpositioneerd (nooit verwijderd-en-opnieuw-aangemaakt, om een eventuele live-sessie/heartbeat-tracking op die rij nooit te laten verdwijnen), overige/extra sessies worden verwijderd/toegevoegd. Nieuwe optie `excludeSessionId` voor Story 4.7's aanroep ná sessie-afronding.
    - `server/domain/scheduling/replan.ts` (Story 4.7): geeft `excludeSessionId` door aan `recalculateTaskPlanning`; de "resterende tijd 0"-tak synct nu Calendar-blokken voor ALLE (nu overbodige) sessiedatums van de taak, niet slechts 1.
    - `server/domain/tasks/delete-task.ts` (Epic 5): gebruikt `getSessionsForTask`/`deleteTaskAndSessions`, synct alle betrokken datums.
    - `server/domain/scheduling/apply-recommendation.ts` (Epic 6): `ShortfallRecommendation.id` draagt sinds deze rework `tier:taskId:sessionId` (niet meer alleen `tier:taskId`) voor `herplannen`/`inkorten` — een taak kan nu een sessie op een héél andere dag hebben dan de tekortdag waarvoor de aanbeveling gegenereerd werd; `applyHerplannen`/`applyInkorten` gebruiken `getSessionById` met dat exacte id i.p.v. `getSessionForTask` (die de verkeerde sessie kon teruggeven). `applyVervallen` synct nu alle sessiedatums van de vervallen taak.
    - `server/domain/scheduling/shortfall.ts` (Epic 6): aanbeveling-id-formaat aangepast voor `herplannen`/`inkorten` (zie hierboven); `vervallen` ongewijzigd (taak-niveau, geen specifieke sessie nodig).
    - `server/domain/scheduling/energy.ts` (Epic 6): `EnergyProposalItem` kreeg een verplicht `sessionId`-veld — `applyEnergyProposal` gebruikt dat i.p.v. `getSessionForTask` te hergebruiken (zelfde correctheidsreden als apply-recommendation.ts); het "naar voren halen"-pad zoekt nu expliciet de eerste sessie van een taak ná de betrokken dag (`getSessionsForTask` + filter) i.p.v. de eerstvolgende sessie ooit.
    - `server/api/tasks/[id].get.ts`, `server/api/school-sessions.post.ts`: ongewijzigd — beide gebruiken `getSessionForTask` precies met de "eerstvolgende sessie"-semantiek die de functie nu expliciet biedt.
  - [x] Typecheck (`npm run typecheck`) en build (`npx nuxt build`) slagen.
  - [x] **Live geverifieerd tegen de dev-stage** (`npx sst deploy --stage dev`, expliciete toestemming Hillebrand): taak aangemaakt met Eveliens exacte scenario (420 min, 25 min/sessie, deadline over ~1 week) → **17 sessies** aangemaakt, verspreid over 5 dagen (2026-09-07 t/m 2026-09-11, vóór de deadline 2026-09-12), elk passend binnen een echt beschikbaar blok van de gekoppelde agenda (bevestigd via `/api/week`'s per-dag beschikbare/benodigde minuten). `startupCheck` gaf terecht `resolved: true` (voldoende tijd, geen tekort).
  - [x] **Tijdens live-verificatie ontdekt en gefixed: een écht resterend gat.** Een taak aangemaakt die haar deadline aantoonbaar niet kan halen (600 min, deadline morgen, nauwelijks beschikbare tijd) gaf `startupCheck: { resolved: true }` — **geen tekort gedetecteerd**, terwijl FR15/UJ-6 dit expliciet vereisen. Oorzaak: `detectAnyShortfall` (Story 6.1) toetst alleen dag-aggregaat (geplande vs. beschikbare minuten per dag) — met sessies netjes verspreid over veel dagen (Task 8's hele doel) overschrijdt geen enkele individuele dag zijn eigen capaciteit, dus het "de taak haalt haar deadline niet"-tekort werd nergens zichtbaar. **Gefixed in `shortfall.ts`:** nieuwe `detectDeadlineOverrun` (scant openstaande taken op sessies die ná hun eigen deadline vallen) wordt vóór de dag-aggregaatcheck aangeroepen in `detectAnyShortfall`; nieuw `ShortfallResult.overrunTaskId`-veld; `generateShortfallRecommendations` genereert voor dit geval direct "tijd verruimen" + "laten vervallen van déze taak" (niveau 1 "herplannen" slaat nergens op — het plannen heeft zelf al voorwaarts gezocht, ook voorbij het doelmoment). Nieuwe `detectShortfallForDateOrOverrun`, gebruikt door de accept/recheck/reject-routes (`server/api/day/shortfall/recommendations/[id]/*.post.ts`) zodat een overrun-aanbeveling ook daadwerkelijk geaccepteerd kan worden (anders zou "de sessie op déze datum" niet meer terugvindbaar zijn, want de `ShortfallResult.date` van een overrun is de taak se deadline, geen dag met een eigen aggregaattekort). **Opnieuw live geverifieerd na de fix:** dezelfde 600-min/morgen-deadline-taak → `resolved: false`, `/api/day/shortfall` toont beide aanbevelingen met een duidelijke, taak-specifieke omschrijving; "laten vervallen" geaccepteerd → taak vervalt, `resolved` wordt weer `true`. Alle testdata (2 taken, in totaal 18 sessies) via `DELETE /api/tasks/{id}` opgeruimd, bevestigd leeg.
  - [x] **Story 4.7's sessie-afronden-flow apart live getest** (`POST /api/sessions/{id}/replan`, na de fix hierboven): een taak van 100 min (4 sessies) → sessie afgerond met 20 min resterend → correct teruggebracht naar 1 sessie; die sessie afgerond met 120 min resterend → correct opgehoogd naar 5 sessies; laatste sessie afgerond met 0 min resterend → alle sessies correct verdwenen, taak op `completedAt`. Bevestigt dat `recalculateTaskPlanning`'s `excludeSessionId`-pad zowel kan krimpen als groeien. Testdata opgeruimd.
  - [x] **Kleine, transiënte Calendar-API-fout tegengekomen tijdens verificatie** (`/api/week` gaf één keer een 500 op "Kon beschikbare-tijd-agenda niet ophalen" voor één specifieke datum) — een herhaalde aanroep na enkele seconden slaagde meteen. Dit is hetzelfde, al in Story 3.1 Task 7's Dev Notes erkende en geaccepteerde risico ("potentieel veel sequentiële Calendar-aanroepen binnen één synchroon request-pad"), nu iets vaker zichtbaar omdat `planSessionSlots` per gescande dag een blok-aanroep doet i.p.v. `findSessionDate`'s eerdere aggregaat-aanroep. Geen structurele fix in déze taak (zou een bredere Calendar-call-batchings-/retry-strategie vergen, buiten scope) — wel genoteerd als Open Question voor een toekomstige story als dit in de praktijk hinderlijk blijkt.

### Review Findings

- [x] [Review][Patch] Geen transactie/opruiming rond Task+Session-insert + de Calendar-sync-aanroep — een falende `createHomeworkEvent` laat al-gecommitte Task/Session-rijen als weeskind achter, geen herstelpad [server/domain/tasks/create-task.ts:38-71] — Task+Session-insert nu atomair in `server/data/tasks.ts`'s nieuwe `createTaskAndSession` (transactie); een falende Calendar-call triggert een compenserende `deleteTaskAndSession`. Live geverifieerd: beide tokens gecorrumpeerd (na backup), taak aanmaken → 500, database-check bevestigde geen weeskind-Task/Session; tokens hersteld en opnieuw functioneel bevestigd.
- [x] [Review][Patch] Sessie-stapeling heeft een TOCTOU-race — twee gelijktijdige `createTask`-aanroepen voor dezelfde user/dag lezen dezelfde `existingMinutes` en kunnen overlappende sessies invoegen (zelfde racecategorie die `updateExceptionForDate` al met een transactie oplost, hier niet toegepast) [server/domain/scheduling/session-time.ts:53-59] — de stapelings-som-lezing zit nu in dezelfde transactie als de Task/Session-insert (zie hierboven), sluit de race.
- [x] [Review][Patch] `findSessionDate` controleert de totale dagcapaciteit, niet de resterende capaciteit ná al gestapelde sessies — een dag kan zo boven zijn eigen beschikbare tijd uit gestapeld worden, en bij genoeg stapeling (≥480 min) rolt het sessie-tijdstip zelfs over naar de volgende kalenderdag [server/domain/scheduling/doelmoment.ts:74-96, server/domain/scheduling/session-time.ts:53-59] — `findSessionDate` trekt nu `sumPlannedMinutesForUserOnDate` af van de beschikbare tijd vóór de capaciteitscheck. Live geverifieerd: 4 taken van 40 min met dezelfde deadline/moeilijkheid/prioriteit — de eerste 3 stapelden correct op dezelfde dag (150-min-zaterdag had nog ruimte), de 4e (120+40=160>150) werd correct naar een andere dag verschoven.
- [x] [Review][Patch] Client en server hanteren twee verschillende definities van "vandaag" voor de deadline-validatie — de client gebruikt de browser se lokale tijdzone, de server `Europe/Amsterdam` — terwijl de rest van deze story juist zorgvuldig Amsterdam-tijdzone-correct is [app/pages/taak/nieuw.vue:58-64, server/api/tasks.post.ts] — beide gebruiken nu dezelfde `todayInAmsterdam()` uit een nieuw `shared/utils/scheduling.ts`.
- [x] [Review][Patch] Server trimt `subject`/`title`/`description` niet vóór opslag — alleen de client trimt, dus een rechtstreekse API-aanroep kan gepadde waarden persisteren, incl. in de Calendar-eventtitel en de vak-suggestielijst [server/api/tasks.post.ts:49-53] — route trimt nu zelf. Live geverifieerd: `"  Wiskunde  "`/`"  TEST trim taak  "` ingestuurd, respons bevatte de getrimde waarden.
- [x] [Review][Patch] `amsterdamOffsetMinutes` valt bij een onherkend `Intl`-formaat stil terug op offset 0 — dit is de eerste server-side IANA-tijdzone-conversie in deze codebase, een verkeerd sessie-tijdstip zonder enig diagnostisch spoor is dan mogelijk [server/domain/scheduling/session-time.ts:20-31] — gooit nu een expliciete `Error` bij een onherkend formaat i.p.v. stil op 0 terug te vallen (verplaatst naar `shared/utils/scheduling.ts`).
- [x] [Review][Patch] Geen bovengrens op `defaultSessionDuration`, en `findSessionDate`'s terugwaartse zoeklus is onbegrensd — bij een grote sessieduur (die geen enkele dag kan faciliteren) loopt de zoeklus dag-voor-dag helemaal terug tot vandaag, mogelijk honderden sequentiële databasecalls binnen één synchrone `POST /api/tasks`-aanroep (AD-1/AD-7) [server/api/tasks.post.ts, server/domain/scheduling/doelmoment.ts:74-96] — `defaultSessionDuration` nu gecapt op 480 min (server-side), `findSessionDate`'s lus begrensd op `MAX_SEARCH_DAYS = 90`. Live geverifieerd: 500 min → 400-envelope.
- [x] [Review][Patch] Sluiten (✕) en Annuleren blijven klikbaar tijdens een lopende `POST`-call — wegnavigeren tijdens het opslaan laat de respons op een verlaten pagina landen [app/pages/taak/nieuw.vue:209-216, 366-371] — `requestLeave()` negeert de klik nu zolang `saving` waar is; sluiten-knop krijgt ook `:disabled`.
- [x] [Review][Patch] `taak-save-button` blijft ~800ms actief ná een geslaagde save (vóór de navigatie) — een tweede klik in dat venster maakt een dubbele taak/sessie/Calendar-event aan [app/pages/taak/nieuw.vue:190-199] — `saving` wordt nu alleen in het foutpad teruggezet, niet meer onvoorwaardelijk in een `finally`; blijft `true` tot de navigatie na een geslaagde save.
- [x] [Review][Patch] `taak-type-select`'s foutmelding wordt niet opnieuw gevalideerd bij wijziging (geen `@change`-handler) — blijft na een submit-fout zichtbaar staan ook nadat een geldig type is gekozen, tot de volgende submit-poging [app/pages/taak/nieuw.vue:259-269] — `@change`-handler toegevoegd die `errors.type` herberekent.
- [x] [Review][Patch] "Bezig"-state heeft geen spinner, alleen tekst — wijkt af van zowel de eigen Task 5-tekst ("spinner, disabled") als de UX-spec; `inloggen.vue` heeft al een werkend spinner-patroon (`login-google-button--busy`) om te hergebruiken [app/pages/taak/nieuw.vue:378] — een vergelijkbaar CSS-spinner-patroon toegevoegd (`taak-save-spinner`, respecteert `prefers-reduced-motion`).
- [x] [Review][Patch] `taak-save-confirmation` mist een `id`-attribuut, in tegenstelling tot elk ander Object-ID-element op de pagina [app/pages/taak/nieuw.vue:362] — `id="taak-save-confirmation"` toegevoegd.
- [x] [Review][Patch] Dev Agent Record se claim "alle vier secties zijn zusjes" klopt niet letterlijk — `taak-header-section` zit buiten de `<form>`, de andere drie zitten er één niveau in genest (wél onderling zusjes, geen sectie-in-sectie-nesting — de eigenlijke les uit Story 2.2 is niet geschonden, maar de claim zelf is onnauwkeurig geformuleerd) — Task 5's checkbox-notitie gecorrigeerd naar een nauwkeurige beschrijving.
- [x] [Review][Patch] UX-spec's "lichte laad-indicator" op `taak-subject-select` tijdens het ophalen van de vakkenlijst ontbreekt, en is niet als bewust overgeslagen genoteerd — de vak-suggesties-fetch is niet meer blokkerend (`useFetch` zonder `await`, voorheen liet een top-level `await` de hele pagina op deze niet-kritieke fetch wachten via Suspense); het formulier rendert nu meteen met een lichte "Vakken laden..."-indicator op het vak-veld zelf.
- [x] [Review][Defer] Moeilijkheid/Prioriteit-segmented-controls gebruiken `role="radiogroup"`/`role="radio"` zonder het bijbehorende toetsenbordmodel (pijltjestoetsen, één tabstop) — eerste component van dit type in de codebase, geen bestaand patroon om op aan te sluiten, verdient een eigen toegankelijkheidspas — deferred, geen bestaand roving-tabindex-precedent in dit project om op te leunen, groter dan een losse patch.
- [x] [Review][Defer] Mislukte `GET /api/tasks/subjects`-aanroep (anders dan 401) toont geen foutstatus — de select blijft gewoon leeg-typbaar, geen functionele breuk — deferred, cosmetische degradatie, lage impact (vak blijft vrij typbaar).

#### Review-ronde 2 (2026-09-05, Task 8 — 3 parallelle agents op Opus 5: Blind Hunter, Edge Case Hunter, Acceptance Auditor)

**Decision-needed — opgelost met Hillebrand (2026-09-05):**

- [x] [Review][Decision] Sessies verplaatst via `session-placement.ts` (`applyHerplannen`/Epic 6.2, `applyEnergyProposal`/Epic 6.4) zijn niet blok-bewust. **Besluit: nu block-aware maken** — `session-placement.ts` moet dezelfde blok-plaatsingslogica gebruiken als `planSessionSlots`/`packSlotsInBlocks` (server/domain/scheduling/doelmoment.ts:200-232), i.p.v. het vaste 16:00-anker. Zie nieuw Patch-item hieronder.
- [x] [Review][Decision] `planSessionSlots` plant alle N sessies "front-loaded" vanaf vandaag, zonder spreiding richting het doelmoment. **Besluit: spreiden richting doelmoment** — de bufferformule (FR24) moet weer daadwerkelijk invloed hebben op de plaatsing, niet alleen op het terugvalpad. Zie nieuw Patch-item hieronder.
- [x] [Review][Decision] `sessionPlacementLocks` is uit de nieuwe N-sessie-paden verdwenen zonder vervanging. **Besluit: hergebruik de bestaande lock-infrastructuur** — `acquireSessionPlacementLock`/`releaseSessionPlacementLock` (nu dode code) op elke datum die een `createTaskAndSessions`/`recalculateTaskPlanning`-aanroep raakt, sequentieel verkregen/losgelaten. Zie nieuw Patch-item hieronder.

**Patch (alle 15 doorgevoerd, 2026-09-05):**

- [x] [Review][Patch] `planSessionSlots`'s terugvalpad riep `resolveAnchorHourMinute` niet aan. Gefixed: het terugvalpad initialiseert `cursorMinutes` nu via `resolveAnchorHourMinute(fallbackDate, SESSION_ANCHOR_HOUR)`. [server/domain/scheduling/doelmoment.ts]
- [x] [Review][Patch] Terugvalpad se `cursorMinutes` had geen 24-uurs-grens. Gefixed: een binnenste lus schuift `cursorDate` door en trekt `24*60` af zodra `cursorMinutes` die grens bereikt, zowel voor `startsAt` als (indien nodig) `endsAt`/`slot.date`. [server/domain/scheduling/doelmoment.ts]
- [x] [Review][Patch] `recalculateTaskPlanning` verwijderde `excludeSessionId` vóór de (potentieel falende) planning. Gefixed: `planSessionSlots` draait nu volledig vóór enige DB-mutatie; `excludeSessionId` wordt pas verwijderd als onderdeel van de atomaire schrijf-stap ná een geslaagde planning. [server/domain/scheduling/recalculate.ts]
- [x] [Review][Patch] Geen transactie rond de herberekenings-schrijfstappen. Gefixed: nieuwe `applyRecalculatedSessions` (`server/data/tasks.ts`) bundelt bijwerken/invoegen/verwijderen atomair in één `getDb().transaction(...)`. [server/domain/scheduling/recalculate.ts, server/data/tasks.ts]
- [x] [Review][Patch] `replanAfterSession`'s `null`-resterende-tijd-tak trok `actualMinutes` niet af. Gefixed: `effectiveRemainingTotalMinutes = remainingTotalMinutes ?? Math.max(0, task.totalMinutes - actualMinutes)`. [server/domain/scheduling/replan.ts]
- [x] [Review][Patch] `detectDeadlineOverrun` negeerde een al-verstreken deadline. Gefixed: `if (task.deadline < today) continue`. [server/domain/scheduling/shortfall.ts]
- [x] [Review][Patch] `server/api/day/shortfall.post.ts` gebruikte nog `detectShortfallForDate`. Gefixed: nu `detectShortfallForDateOrOverrun`, consistent met de drie sibling-routes.
- [x] [Review][Patch] `verruimen`-id droeg geen taak-id, her-detectie matchte alleen de eerste overrunnende taak per datum. Gefixed: nieuw id-formaat `verruimen:overrun:${taskId}:${date}`, nieuwe `extractOverrunTaskId`-parser, `detectShortfallForDateOrOverrun`/accept/recheck/reject geven nu `recommendationId` door zodat de her-detectie taak-specifiek is. **Live geverifieerd** (2026-09-05): twee gelijktijdig overrunnende taken aangemaakt, "laten vervallen" van de eerste geaccepteerd → tweede taak se eigen, aparte tekort verscheen daarna correct (niet stilzwijgend "opgelost"). [server/domain/scheduling/shortfall.ts, de 3 accept/recheck/reject-routes]
- [x] [Review][Patch] `generateDeadlineOverrunRecommendations` gaf `[]` bij een verdwenen taak. Gefixed: valt terug op `detectShortfallForDate` + een recursieve `generateShortfallRecommendations`-aanroep voor diezelfde datum. [server/domain/scheduling/shortfall.ts]
- [x] [Review][Patch] Geen guard tegen `sessionMinutes ≤ 0`. Gefixed: `planSessionSlots` gooit nu meteen een expliciete `Error`. [server/domain/scheduling/doelmoment.ts]
- [x] [Review][Patch] Onjuiste/verouderde commentaren gecorrigeerd (de "allevier dezelfde formule"-claim, stale `findSessionDate`/`createTaskAndSession`-verwijzingen). [server/data/tasks.ts]
- [x] [Review][Patch] Dode code `findSessionDate` verwijderd (incl. de nu ongebruikte `MAX_SEARCH_DAYS`-constante). [server/domain/scheduling/doelmoment.ts]

**Decisions doorgevoerd (2026-09-05):**

- [x] **D1 (block-aware herplannen/energie-pad):** `session-placement.ts`'s `placeSessionOnDate` gebruikt nu `findBlockAwareSlot` (nieuw, `doelmoment.ts`) i.p.v. het vaste anker — gooit een expliciete `Error` als er geen aaneengesloten ruimte binnen een blok is, i.p.v. stil buiten een blok te plaatsen. **Bekende restbeperking (genoteerd, niet in deze ronde opgelost):** de kándidaat-dag-zoeklus zelf (`shortfall.ts`'s `findAlternativeDate`, `energy.ts`'s `remainingCapacityOnDate`/`tryDisplaceOnDate`) blijft aggregaat-gebaseerd — die kan dus in zeldzame gevallen een dag voorstellen die bij de daadwerkelijke, blok-bewuste plaatsing alsnog niet past (dan gooit `placeSessionOnDate` een `Error` i.p.v. de sessie te verplaatsen). Zie nieuwe Open Question hieronder.
- [x] **D2 (spreiden richting doelmoment):** `planSessionSlots` berekent nu per gescande dag een dagtarget (resterend-aantal-sessies ÷ resterend-aantal-dagen-tot-doelmoment, naar boven afgerond) i.p.v. elke dag volledig te vullen — zelfcorrigerend, geen limiet meer zodra het doelmoment gepasseerd is (inhaalslag). **Live geverifieerd:** Eveliens 420-min-scenario ging van 1/7/2/2/5-sessies-per-dag (front-loaded) naar 1/4/2/2/8 (meer gespreid, met een natuurlijke piek vlak vóór het doelmoment).
- [x] **D3 (concurrency-lock hersteld):** nieuwe `withSessionPlacementLocks` (`server/data/tasks.ts`, hergebruikt de bestaande `acquireSessionPlacementLock`/`releaseSessionPlacementLock`-primitieven) — `createTaskAndSessions` en `recalculateTaskPlanning` verkrijgen nu de lock voor elke betrokken datum (gesorteerd, tegen deadlocks) vóór hun schrijf-transactie. `placeSessionWithStackingOffset` (de enige vorige aanroeper, nu overbodig ná D1/D2) is verwijderd.

**Defer:**

- [x] [Review][Defer] Tot ~90 sequentiële live Google Calendar-aanroepen per taak-planning (`planSessionSlots` roept `getAvailableBlocksForDate` per gescande dag aan) — al genoteerd als Open Question #7 in deze story, nu iets prominenter zichtbaar. Geen nieuwe onbekende risicocategorie, wel groter dan ingeschat — deferred, heroverwegen als dit in de praktijk hinderlijk blijkt.
- [x] [Review][Defer] `getSessionForTask` kan een verouderde, al-gepasseerde sessie teruggeven als een geplande dag ooit stilzwijgend wordt overgeslagen zonder dat iets een herberekening triggert — pre-existent gedrag van het "geen sessie-afgerond-vlag, alleen datums"-model, niet door deze diff geïntroduceerd — deferred, hoort bij een bredere sessie-levenscyclus-heroverweging.
- [x] [Review][Defer] `detectDeadlineOverrun` + `getOpenTasksWithProgress` worden meermaals per request opnieuw aangeroepen (N+1-patroon, geen hergebruik tussen `detectAnyShortfall`/`detectShortfallForDateOrOverrun`/`generateDeadlineOverrunRecommendations`) — performance, geen correctheidsprobleem, consistent met bestaande N+1-tolerantie elders in dit project (bv. `week-overview.ts`) — deferred.
- [x] [Review][Defer] `ShortfallResult.availableMinutes`/`plannedMinutes` zijn gefingeerd (`0`/`overrunMinutes`) voor het overrun-geval — een representatie-smell voor toekomstige onderhouders, maar geverifieerd dat dit vandaag geen enkel client-gericht veld of niet-gerelateerde consument (bv. `calculateStudiedrukScore`, die zijn eigen `detectShortfallForDate`-aanroep gebruikt, ongemoeid) daadwerkelijk raakt — deferred, overweeg een discriminated union bij een volgende aanraking van dit bestand.

## Dev Notes

### Bufferformule — voorgesteld, geen vaste PRD-cijfers (Open Question)

FR24/PRD zeggen alleen "percentage... groter naarmate moeilijker/groter, kleiner naarmate prioriteit hoger" — geen concrete getallen staan ergens vast. Task 2 hierboven stelt een formule voor (basis 20%, ±5-10% per moeilijkheid/prioriteit-stap, geclampt tussen 5-40%). Dit is een productbeslissing zonder objectief "juist" antwoord — geïmplementeerd als een redelijk, gedocumenteerd default, maar **zeg het vóór implementatie als je andere cijfers wilt** (zie Open Questions onderaan). De cijfers staan als losse constanten bovenaan `server/domain/scheduling/doelmoment.ts`, dus makkelijk aan te passen zonder de rest van de logica te raken.

### Sessie-tijdstip — waarom dit nu al nodig is

Overal elders in Flowz (Epic 2's "beschikbare tijd") is een dag alleen een *minutenbudget*, geen tijdstip-concept. Maar AC #2's laatste bullet eist dat déze story, zodra een huiswerk-kleur is ingesteld, voor de nieuw geplande sessie een écht Calendar-event aanmaakt (Story 2.3's `createHomeworkEvent`), en dat vereist een concreet `startsAt`/`endsAt`. Overlegd met Hillebrand (2026-08-01): een vast lokaal ankertijdstip (16:00 Europe/Amsterdam) met opeenvolgende stapeling bij meerdere sessies op dezelfde dag — geen nieuw UI-veld, geen aparte tijdstip-keuze voor Evelien. Dit tijdstip is een placeholder voor v1, niet een uitgewerkte "wanneer op de dag werkt Evelien"-modellering; latere verfijning (instelbaar ankertijdstip, dagdeel-voorkeur) is niet in deze story's scope en niet architecturaal geblokkeerd door deze keuze (het blijft een simpele constante).

### `totalMinutes`: waarom nu al op `Task`, niet pas bij Story 3.2

Story 3.2 bouwt de deeltaken-UI en de handmatige-override-UI voor "totale benodigde tijd" — maar de bufferformule (déze story, FR24) heeft nu al een `totalMinutes`-waarde nodig om mee te rekenen. Twee opties overwogen: (a) de kolom nu al toevoegen met een redelijk default (`= defaultSessionDuration`, één impliciete sessie, want er zijn nog geen deeltaken), Story 3.2 werkt 'm later gewoon bij; (b) de kolom pas in Story 3.2 toevoegen en déze story rekent voorlopig direct met `defaultSessionDuration` zonder aparte kolom. Gekozen voor (a) — voorkomt een tweede migratie op dezelfde tabel voor hetzelfde concept, en de kolom heeft vanaf dag één een zinnige waarde. Zeg het als je (b) prefereert.

### Omschrijving: waarom nu al

`taak-description-textarea` staat in de UX-spec (2.1) maar wordt door **geen enkele** Epic 3-story's AC-tekst expliciet geclaimd (3.1 = kerngegevens, 3.2 = deeltaken/totale tijd, 3.3 = benodigdheden) — een gat in `epics.md`'s eigen opsplitsing. Omdat het veld eenvoudig en zonder cross-field-gedrag is (in tegenstelling tot deeltaken/benodigdheden), hier meegenomen i.p.v. permanent onbebouwd te blijven. Zeg het als je 'm liever aan een andere story toewijst.

### `subject`: geen aparte tabel

De architectuur se datamodel-lijst (`User, Task, Session, Subtask, AvailableTimePattern, AvailableTimeException`) noemt geen "Vak"/Subject-entiteit. `subject` is dus een los tekstveld op `Task`; de combo-select's suggestielijst komt uit `SELECT DISTINCT subject` over deze user's eigen taken — geen nieuwe tabel, geen aparte Story-scope hiervoor nodig.

### Error-codes: reconciliatie met de UX-spec

De UX-spec somt per veld een eigen foutcode op (`ERR_TITLE_REQUIRED`, `ERR_DEADLINE_PAST`, ...). De architectuur se Consistency Conventions zijn hier strikter: "gedeelde error-code-vocabulaire in `server/domain/errors.ts`... geen endpoint verzint een eigen `code`" — en dat vocabulaire kent alleen `internal_error | validation_error | unauthorized`. Gelezen als: de UX-spec's `ERR_*`-codes zijn **client-side message-lookup-sleutels** voor de on-blur-validatietekst (vóórdat er ooit een server-aanroep is), niet server-`code`-waarden. De server retourneert bij een 400 gewoon `ErrorCodes.ValidationError` met een beschrijvende `message` — zelfde patroon als elke eerdere route in dit project.

### Validatieregels (samengevat uit de UX-spec, on-blur + on-submit tenzij anders genoemd)

| Veld | Regel |
|---|---|
| `taak-subject-select` | Verplicht, niet leeg. On submit. |
| `taak-title-input` | Verplicht, niet leeg, max 100 tekens. On blur + on submit. |
| `taak-type-select` | Verplicht, één van `proefwerk/so/opdracht/po`. On submit. |
| `taak-deadline-input` | Verplicht, geldige datum, niet in het verleden (vandaag mag). On blur + on submit. Hergebruik `isValidCalendarDate` uit `shared/utils/availability.ts` (Story 2.1/2.2-precedent) voor het formaat; "niet in het verleden" is een simpele ISO-datumstring-vergelijking tegen vandaag, geen nieuwe shared-util nodig daarvoor. |
| `taak-difficulty-select`/`taak-priority-select` | Client: altijd een waarde door de default, geen losse UI-validatie nodig. **Server: wél valideren tegen de echte enum (`laag/gemiddeld/hoog`)** — Task 2's bufferformule doet een directe object-lookup (`DIFFICULTY_ADJUSTMENT[difficulty]`); een onherkende waarde geeft `undefined` en propageert stil naar `NaN` door `bufferMinutes`/`bufferDays`/`doelmoment`. Zelfde les als Story 2.2's regex-only-validatie-bevinding, hier toegepast vóórdat het een reviewbevinding hoeft te worden. On submit, zelfde 400-envelope als de andere velden. |
| `taak-session-duration-input` | Verplicht, geheel getal ≥ 5. On blur + on submit. |
| `taak-description-textarea` | Optioneel, max 500 tekens indien ingevuld. On blur + on submit. |

### Entry point: nog geen "+"-knop

Er bestaat nog geen hamburgermenu of persistente navigatie ergens in de app (`index.vue` is nog steeds Story 1.2's sessie-gated placeholder, Epic 4 bouwt het echte hoofdscherm). AC #1's "vanaf een willekeurige pagina" is dus voorlopig alleen waar via directe URL-navigatie naar `/taak/nieuw` — exact dezelfde tijdelijke beperking als `beschikbare-tijd.vue` had vóór er een hamburgermenu kwam (nog steeds niet gebouwd, zie `deferred-work.md`). Geen actie hier nodig, alleen niet verrast zijn dat er nog geen "+"-knop ergens klikbaar is.

### Wat expliciet buiten scope valt

- **Deeltaken, handmatige totale-tijd-override, benodigdheden** — Story 3.2/3.3.
- **Volgorde-algoritme bij concurrentie om dezelfde dag** — Story 3.4. Déze story plaatst één sessie zonder te heroverwegen of andere taken die dag "belangrijker" zijn; stapeling (opeenvolgend na bestaande sessies) is het enige concurrentie-gedrag hier.
- **Tijdgebrek-detectie/escalatie** — Epic 6. Vindt de plaatsingslogica geen dag met genoeg ruimte, wordt de sessie toch best-effort geplaatst; geen waarschuwing, geen escalatieketen.
- **`googleEventId`-opslag/mapping** — nog steeds Story 2.3's eigen, nog geldende scope-grens.
- **Bewerken/verwijderen van een taak** — Epic 5.

### Foutafhandeling: bestaande 500-envelope, geen Notification (Task 7)

AD-6's `Notification`-shape bindt expliciet aan UJ-6/7/8 (tekort-/escalatiemeldingen, zie `server/domain/notification.ts`'s eigen top-commentaar en `shortfall.ts`'s `buildShortfallNotification`) — **niet** aan UJ-2 (taak aanmaken, déze story). Een mislukte Calendar-read tijdens `getAvailableMinutesForDate` hoort dus gewoon bij het bestaande "een gegooide `Error` binnen `createTask` propageert naar `server/api/tasks.post.ts`'s catch-all → 500 `ErrorCodes.InternalError`"-patroon dat Task 3 al vastlegde voor een falende Calendar-sync-aanroep — geen nieuwe Notification-plumbing in deze route. AD-10's "geen stille terugval"-eis is hiermee voldaan (de fout wordt zichtbaar, niet stil op 0 teruggevallen); AD-6 komt pas in beeld zodra Epic 6's eigen escalatiepad (Story 6.1/6.2, al met een eigen implementation-note) een tekort expliciet aan Evelien voorlegt.

### Task 8 — Meerdere sessies vooruit plannen (Correct Course 2026-09-05)

Zie `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-05.md` voor de volledige Issue Summary/Impact Analysis. Kernpunt: dit is een **codecorrectie**, geen architectuurwijziging — AD-1 ("herberekening idempotent, gaat uit van de actuele Task/Session-staat") en AD-3 ("sessies zijn kind-rijen van Task", meervoud) beschreven het 1:N-model al vóór deze story. De vorige implementatie (Task 1-7 hierboven) bouwde dit model onbedoeld in als "1 sessie per taak" doordat er nog geen scenario was dat een taak met meer dan 1 benodigde sessie ver vooruit plande.

Story 3.5 (Idempotente Herberekening) krijgt de bijbehorende regeneratie-kant (wat gebeurt er als de staat ná het aanmaken wijzigt) — déze story (3.1) bouwt alleen de **initiële** N-sessie-plaatsing bij `createTask`. Story 3.4 (Volgorde-algoritme) en 4.7 (Sessie-afronden) volgen na 3.1/3.5.

### Architectuurcompliance

- AD-1: scheduling-logica (doelmoment, plaatsing, tijdstip) uitsluitend in `server/domain/scheduling/`, nooit in `app/`.
- AD-3: `Task` bezit `Session` (kind-rij); de planning zelf blijft een berekende weergave — geen losse "planning"-tabel.
- AD-7: Calendar-write synchroon binnen hetzelfde request dat de sessie plant — geen achtergrondtaak.
- Mutatie-ownership: `server/api/tasks*` roept nooit rechtstreeks `server/data/` aan, altijd via `server/domain/tasks/`.
- Consistency Conventions: datums/tijden ISO 8601 UTC in de data-laag (`sessions.startsAt`), duur in minuten (integer).

### Project Structure Notes

- Nieuw: `server/domain/scheduling/doelmoment.ts` (of vergelijkbaar bestand, dev-agent-keuze binnen de map), `server/domain/tasks/create-task.ts`, `server/api/tasks.post.ts`, `server/api/tasks/subjects.get.ts`, `app/pages/taak/nieuw.vue`, `shared/types/tasks.d.ts`.
- Gewijzigd: `server/data/schema.ts` (nieuwe `tasks`/`sessions`-tabellen), `server/data/availability.ts` (nieuwe `getExceptionForDate`).
- Hergebruikt, ongewijzigd: `server/domain/calendar-sync/homework-events.ts` (Story 2.3), `server/data/availability.ts`'s `getOrCreateWeekPattern` (Story 2.1), `shared/utils/availability.ts`'s `isValidCalendarDate` (Story 2.1/2.2).
- Geen conflicten met bestaande structuur.

### Testen

Nog steeds geen testframework (herhaaldelijk genoteerd sinds Story 1.2, `deferred-work.md`). Live verificatie tegen de dev-stage blijft de enige realistische toets, met dezelfde sealed-cookie-techniek als elke eerdere story.

## Previous Story Intelligence (Epic 2, alle drie stories)

- **`shared/types/` (compile-time) en `shared/utils/` (runtime) blijven de vaste plek voor iets dat zowel server als client nodig hebben** — nu voor het eerst gebruikt voor een heel nieuw domein (`tasks`) i.p.v. `availability`/`settings`.
- **Sectienesting fout maken is makkelijk, controleer het expliciet** — geldt hier niet direct (dit is een nieuwe pagina, geen sectie-toevoeging aan een bestaande), maar wel relevant zodra Story 3.2/3.3 secties aan déze pagina toevoegen: zorg dat `taak-core-section`/`taak-extra-section`/`taak-action-section` zusjes zijn, geen geneste hiërarchie.
- **Live verificatie kan een gedeeltelijk gefixte aanname blootleggen** — neem niets aan over hoe Google's Calendar API of Node's `Intl`-tijdzone-API zich in de praktijk gedraagt zonder het live te bevestigen (Story 2.3's `SERVICE_DISABLED`-blocker en de foutieve 404-aanname zijn hier het directe precedent).
- **Regex-only-validatie is niet genoeg voor semantisch beperkte waarden** — geldt hier voor `deadline` (hergebruik `isValidCalendarDate`, niet een losse regex) en voor `type`/`difficulty`/`priority` (valideer tegen de echte enum-waarden, niet alleen "is het een string").
- **`server/domain/calendar-sync/homework-events.ts` is nu voor het eerst aangeroepen van buíten Story 2.3 zelf** — lees het bestand volledig (zie referentie hieronder) vóór gebruik; de functie is al zelf-bewakend, dus geen dubbele if-checks bouwen.
- **Debug-routes voor live-verificatie van server-only logica altijd tijdelijk, nooit gecommit** — nodig omdat `useRuntimeConfig()`/`Resource.*` niet werken in een los `sst shell -- node`-script; bouw zo nodig een tijdelijke `server/api/_debug/*`-route, verwijder 'm daarna en bevestig de verwijdering met een `404`-check via een geauthenticeerde request.

## Git Intelligence

Laatste relevante commits: `19dc0e1` (Story 2.3 aangemaakt), Story 2.3's implementatie-/reviewcommits (huiswerk-kleur + calendar-sync + token-refresh — het `server/domain/calendar-sync/`-patroon dat déze story rechtstreeks hergebruikt). Geen eerdere Epic 3-story om patronen uit over te nemen (dit is Story 3.1, de eerste van het epic).

## References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-3.1-Taak-Aanmaken-Kerngegevens-met-Doelmoment-berekening] — user story + acceptatiecriteria (brontekst)
- [Source: _bmad-output/planning-artifacts/epics.md#Epic-3-Taak-Aanmaken-met-Automatische-Tijdsverdeling] — epic-context, FR9/FR10/FR24/FR25/FR26, welke sub-scope bij welke story hoort
- [Source: _bmad-output/planning-artifacts/prds/prd-Flowz-2026-07-11/prd.md#Automatische-tijdsverdeling] — doelmoment/bufferprincipe, studiedruk-concept
- [Source: _bmad-output/planning-artifacts/architecture/architecture-Flowz-2026-07-14/ARCHITECTURE-SPINE.md#AD-1] — scheduling server-only
- [Source: _bmad-output/planning-artifacts/architecture/architecture-Flowz-2026-07-14/ARCHITECTURE-SPINE.md#AD-3] — Task bezit Session/Subtask, planning is berekende weergave
- [Source: _bmad-output/planning-artifacts/architecture/architecture-Flowz-2026-07-14/ARCHITECTURE-SPINE.md#AD-7] — Calendar write-sync synchroon binnen request-pad
- [Source: design-artifacts/C-UX-Scenarios/02-evelien-taak-aanmaken/2.1-taak-formulier/2.1-taak-formulier.md] — volledige veldspecificatie, validatieregels, Object IDs, states (let op: bevat ook Story 3.2/3.3's velden — déze story bouwt alleen Kerngegevens + Omschrijving)
- [Source: _bmad-output/implementation-artifacts/2-3-huiswerk-kleur-kiezen-calendar-write-sync-service.md] — `createHomeworkEvent`'s exacte contract (`HomeworkSession`-shape), token-refresh-precedent, zelf-bewakingslogica
- [Source: _bmad-output/implementation-artifacts/2-1-weekpatroon-instellen.md] — `getOrCreateWeekPattern`, `shared/utils/`-precedent, sealed-cookie-live-verificatietechniek
- [Source: _bmad-output/implementation-artifacts/2-2-dag-specifieke-afwijkingen-instellen.md] — `isValidCalendarDate`, per-datum exceptie-patroon (basis voor de nieuwe `getExceptionForDate`)
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — bekend, nog open: geen testframework, geen lint/import-boundary-handhaving, nog geen hamburgermenu

## Open Questions

1. **Bufferformule-cijfers** (basis 20%, ±5-10% per stap, 5-40% clamp) — beargumenteerd voorstel, geen PRD-vastgelegde waarde. Zeg het vóór implementatie als je andere getallen wilt.
2. **`totalMinutes` nu al op `Task`** (= `defaultSessionDuration` bij aanmaken, Story 3.2 herberekent later) — beargumenteerd, voorkomt een tweede migratie. Zeg het als je liever wacht tot Story 3.2.
3. **Omschrijving nu al meegenomen** (niet expliciet aan een Epic 3-story toegewezen in `epics.md`) — zeg het als je 'm liever aan een andere story toewijst of nu overslaat.

Sessie-tijdstip (vast 16:00 Europe/Amsterdam-anker, opeenvolgend stapelen) is al met Hillebrand afgestemd (2026-08-01) — geen open vraag meer, hierboven ter referentie in Dev Notes vastgelegd.

4. **(Task 7, AD-10-rework) Geen gekoppelde agenda → stil 0 minuten of een Notification?** Voorstel houdt de bestaande stille-terugval-op-0 aan (zoals het oude model ook deed voor een vers weekpatroon) omdat 4.1-beschikbare-tijd-instellen al een eigen `avail-no-calendar-notice` toont op de instellingenpagina zelf — een tweede melding vanuit de scheduling-engine zou dubbelop kunnen zijn. Zeg het als je liever ook hier een expliciete Notification wilt (AD-6) op het moment dat Evelien een taak probeert aan te maken zonder gekoppelde agenda.
5. **(Task 7) `averageDailyAvailableMinutes`-venster: 14 kalenderdagen vooruit** — vervangt het oude 7-daagse weekpatroon-gemiddelde, geen vastgelegd PRD-cijfer. Beargumenteerd voorstel (dempt een enkele weekend-uitschieter, beperkt het aantal Calendar-calls); zeg het als je een ander venster wilt.
6. **(Task 7) `findSessionDate`'s terugwaartse zoeklus wordt live Calendar-call-per-dag** i.p.v. een in-memory patroon-lookup — functioneel ongewijzigd gedrag, wel een ander performance-profiel bij een volle 90-dagen-zoeklus. Genoemd voor de zichtbaarheid, geen blokkerende vraag.
7. **(Task 8) `planSessionSlots` doet één live Calendar-aanroep per gescande dag, sequentieel** — bij een taak die veel sessies over veel dagen nodig heeft (zoals Eveliens 17-sessies-scenario) kan dit oplopen tot tientallen sequentiële Calendar-calls binnen één `POST /api/tasks`-aanroep. Tijdens live-verificatie kwam hierdoor één keer een transiënte Calendar-fout voor (een herhaalde aanroep enkele seconden later slaagde meteen) — zelfde risicocategorie als Task 7's al geaccepteerde `findSessionDate`-risico (Open Question #6), nu iets prominenter. Geen structurele fix hier (zou batching/parallellisatie van de Calendar-aanroepen of een retry-laag vergen, buiten scope van déze taak) — heroverwegen als dit in de praktijk vaker dan incidenteel misgaat.
8. **(Task 8) Deadline-overrun-escalatie mist een "inkorten"-niveau** — `generateDeadlineOverrunRecommendations` genereert alleen "tijd verruimen" en "laten vervallen" (niveau 2 en 4), geen "inkorten" (niveau 3): de bestaande `applyInkorten`-mutatie verkort één specifieke sessie met een vast aantal minuten, wat niet het onderliggende probleem oplost (de taak heeft in totaal te veel tijd nodig, niet één te lange sessie). Een taak-brede "verkort alle resterende sessies met X%"-mutatie zou dit kunnen dekken, maar is een nieuwe mutatievorm — bewust buiten scope gehouden, zeg het als je dit alsnog wilt.
9. **(Review-ronde 2) Herplannen/energie-pad se kandidaat-dag-zoeklus blijft aggregaat-gebaseerd** — `findAlternativeDate` (shortfall.ts)/`remainingCapacityOnDate` (energy.ts) bepalen nog via `availableMinutesForDate`/`sumPlannedMinutesForUserOnDate` of een dag "genoeg ruimte" heeft, niet via de daadwerkelijke blokgrenzen. De uiteindelijke plaatsing (`placeSessionOnDate`) ís sinds deze review-ronde wel blok-bewust en gooit een `Error` als een voorgestelde dag toch niet past — dus nooit een stille blok-schending, maar in een zeldzaam geval kan een accept/energie-voorstel daardoor onverwacht falen i.p.v. slagen. Volledig oplossen vergt het blok-bewust maken van de zoeklussen zelf — buiten scope van déze review-ronde, zeg het als je dit alsnog wilt.
10. **(Review-ronde 3) `packSlotsInBlocks` modelleert bezetting als een scalair minutenaantal, niet als echte tijdsintervallen** — een bestaande sessie die via `applyInkorten`/het energie-pad in lengte is verkort (maar niet verplaatst) laat een "gat" achter dat de volgende plaatsing niet ziet: de skip-teller telt te weinig minuten, waardoor een nieuwe/verplaatste sessie alsnog op de resterende, nog bestaande sessie kan landen. Oplossen vergt interval-subtractie i.p.v. minuten-aftrek in `packSlotsInBlocks` — een grotere herziening van de kernplaatsingslogica, niet een losse patch. Bevestigd door alle 3 agents in twee opeenvolgende rondes; bewust niet aangepakt gezien de omvang.
11. **(Review-ronde 3) De TOCTOU-concurrency-bescherming is niet volledig gesloten** — `withSessionPlacementLocks` beschermt sinds ronde 2 de schrijffase van `createTaskAndSessions`/`recalculateTaskPlanning`, maar de planningsfase (`planSessionSlots`, met live Calendar-aanroepen) draait daarvóór, dus buiten de lock. Twee gelijktijdige aanroepen kunnen nog steeds allebei "er is nog ruimte" concluderen vóórdat een van beide schrijft. Volledig sluiten zou vereisen dat de lock al vóór het plannen wordt verkregen — maar dan moet je de betrokken datums al kennen vóórdat je ze hebt uitgerekend, een circulair probleem dat een andere aanpak vergt (bv. optimistische concurrency met een retry-lus na een botsing). Bewust niet in deze ronde opgelost.
12. **(Review-ronde 3) AD-6's `Notification`-shape heeft nog steeds nul aanroepers** — `buildShortfallNotification` (shortfall.ts) bestaat, wordt nergens aangeroepen; elke UJ-6/7/8-route retourneert in plaats daarvan een eigen DTO, en een mislukte Calendar-read in die routes valt terug op de technische error-envelope i.p.v. AD-6's gebruikersgerichte Notification-vorm. Dit is een gat dat al vóór Story 3.1 Task 8 bestond (niet door deze rework geïntroduceerd) — het daadwerkelijk overal verankeren van de Notification-shape is een aparte, epic-6-brede opruimactie.
13. **(Review-ronde 3) De UTC/Amsterdam-daggrens-spanning is verkleind, niet weggenomen** — `PlannedSessionSlot.date` komt nu consistent uit `startsAt` (UTC), maar `planSessionSlots`'s dag-voor-dag-zoeklus gebruikt zelf nog een Amsterdam-lokale kalenderdag (`candidate`) om zowel de blokken (`getAvailableBlocksForDate`, Amsterdam-lokaal venster) als de al-geplande minuten (`sumPlannedMinutesForUserOnDate`, UTC-bucket) op te vragen. Voor een beschikbaar-tijd-blok vroeg in de ochtend (bv. 00:00-02:00 lokaal in de zomer) vallen deze twee nog uiteen. Volledig oplossen vergt een keuze: overal consistent Amsterdam-lokale dagen gebruiken i.p.v. de UTC-substring-conventie die dit hele project sinds Story 3.1 hanteert — een bredere, project-brede beslissing, niet iets voor een losse patch. In de praktijk raakt dit alleen taken met een studietijd-blok vlak ná middernacht, wat bij Evelien vooralsnog niet voorkomt.
14. **(Review-ronde 3) Live-Calendar-aanroepen zijn nog steeds niet gecachet/gebundeld binnen één request** — `averageDailyAvailableMinutes` (14 aanroepen), `planSessionSlots`/`findAlternativeDate` (tot 90 elk) en hun gezamenlijke gebruik in `week.get.ts` (7× per dag) kunnen samen tot duizenden sequentiële Calendar-aanroepen binnen één verzoek optellen. Al genoteerd in eerdere rondes (Open Question #7), hier bevestigd als nog steeds actueel na twee hertoetsingen — heroverwegen (caching/batching binnen één request) als dit in de praktijk vaker dan incidenteel tot een trage of mislukte aanvraag leidt.

## Change Log

| Datum | Wijziging |
| --- | --- |
| 2026-08-01 | Story aangemaakt via create-story. Eén fundamentele modelvraag (hoe krijgt een sessie een tijdstip voor de Calendar-sync-aanroep, terwijl de rest van de app alleen dagelijkse minutenbudgetten kent) vooraf met Hillebrand afgestemd: vast 16:00 Europe/Amsterdam-anker, opeenvolgend stapelen. Onafhankelijke validatiepas vond en fixte één inconsistentie (ontbrekende server-side enum-validatie voor difficulty/priority) vóór dev-story begon. |
| 2026-08-01 | Task 1 (schema) afgerond: nieuwe `tasks`/`sessions`-tabellen, migratie `0005_long_romulus.sql` gegenereerd en live toegepast, geverifieerd via `PRAGMA table_info`. |
| 2026-08-01 | Task 2 (scheduling-engine) afgerond: bufferformule, doelmoment-berekening, terugwaartse dag-plaatsing, en het Amsterdam-anker/DST-conversie/stapelings-mechanisme voor het sessie-tijdstip. |
| 2026-08-01 | Task 3 (`createTask`-orkestratie) en Task 4 (`POST /api/tasks`, `GET /api/tasks/subjects`) geïmplementeerd, inclusief de Calendar-sync-aanroep (Story 2.3's `createHomeworkEvent` ongewijzigd hergebruikt) en server-side enum-validatie. |
| 2026-08-01 | Task 5 (front-end `/taak/nieuw`) toegevoegd: Kerngegevens- en Omschrijving-secties, on-blur/on-submit-validatie, combo-select via `<datalist>`, sluiten/annuleren-bevestigingsdialoog, flash-bevestiging. Typecheck en build slagen. |
| 2026-08-01 | Task 6 (verificatie) afgerond: live end-to-end getest tegen de dev-stage — doelmoment-differentiatie op moeilijkheid/prioriteit, sessie-stapeling, Calendar-sync met en zonder kleur (4 echte events aangemaakt/gecontroleerd/verwijderd bij Google), DST-conversie rond zowel de maart- als oktober-omschakeling, alle validatieregels. Browser-rendering niet getoetst (Chrome-extensie niet verbonden op het moment van testen) — expliciet gemeld, niet verzwegen. Alle testdata opgeruimd, kleur hersteld naar Hillebrand's echte keuze. Status → review. |
| 2026-08-01 | Formele code review (Blind Hunter + Edge Case Hunter + Acceptance Auditor, zwaar overlappend): 0 decision-needed, 13 patch, 2 defer. Kern: geen transactie rond Task+Session-insert + Calendar-sync (weeskind-rijen bij een falende Calendar-call), een TOCTOU-race in de sessie-stapeling, en `findSessionDate` die de totale i.p.v. de resterende dagcapaciteit toetste — alle drie in één keer opgelost door de stapelings-som-lezing, Task-insert en Session-insert atomair in één transactie te zetten (`server/data/tasks.ts`'s nieuwe `createTaskAndSession`) plus een compenserende `deleteTaskAndSession` bij een falende Calendar-call. Client/server-tijdzone-mismatch voor "vandaag" opgelost door een gedeelde `shared/utils/scheduling.ts` (`todayInAmsterdam`/`amsterdamLocalToUtcIso`, `server/domain/scheduling/session-time.ts` opgegaan in dit nieuwe bestand). Overige 8 patches: server-side trimmen, stille offset-0-terugval vervangen door een expliciete `Error`, bovengrens op sessieduur (480 min) + begrensde dag-zoeklus (`MAX_SEARCH_DAYS`), sluiten/annuleren geblokkeerd tijdens het opslaan, dubbele-klik-venster na een geslaagde save gedicht, live-hervalidatie op het soort-taak-veld, spinner op de "Bezig"-state, ontbrekend `id`-attribuut, een documentatie-correctie in de eigen Dev Agent Record, en een niet-blokkerende vak-suggesties-fetch met laadindicator (loste ook een niet eerder opgemerkt whole-page-Suspense-blokkade op). Typecheck, build en gerichte live-verificatie (weeskind-rij-test met tijdelijk gecorrumpeerde tokens, capaciteits-overloop-test met 4 taken) bevestigen de fixes. Alle testdata en tijdelijke scripts opgeruimd. Status → done. |
| 2026-08-18 | **De destijds als opgelost beschouwde TOCTOU-race in `createTaskAndSession` bleek dat niet te zijn** — het projectbrede `getDb().transaction()`-audit (aanleiding: Story 3.5's empirisch weerlegde transactie-aanname) vond deze functie als één van twee resterende kwetsbare plekken naast de al opgeloste Story 3.5/2.2-gevallen. `createTaskAndSession` leest de bestaande dag-som en plaatst de nieuwe sessie binnen dezelfde `tx`, exact het patroon dat bij Story 3.5 al bewezen niet serialiseert tegen deze Turso-verbinding. Opgepakt op verzoek van Hillebrand: gewrapt met dezelfde `sessionPlacementLocks`-tabel als `placeSessionWithStackingOffset` (bewust hergebruikt, niet een nieuwe tabel — beide races gaan over exact dezelfde resource: "hoeveel van vandaag is al bezet voor deze gebruiker"). Live geverifieerd met gelijktijdige aanmaak-verzoeken; geen overlap meer. Zie `deferred-work.md` en Story 3.5's Dev Notes voor het volledige verhaal achter dit patroon. |
| 2026-09-03 | **Heropend (Task 7 toegevoegd) voor de AD-10-rework** (Correct Course 2026-09-02): Story 2.1 (herzien) liet `server/domain/scheduling/doelmoment.ts` bewust nog op het oude `AvailableTimePattern`/`AvailableTimeException`-model draaien — déze story bouwde dat lees-pad oorspronkelijk, dus hoort de rework hier thuis, niet als losse nieuwe story. Task 7 verlegt `availableMinutesForDate`/`findSessionDate`/`averageDailyAvailableMinutes` naar een live, on-demand berekening uit `User.availabilityCalendarId` (nieuw `server/domain/availability/calendar-blocks.ts`), functiehandtekeningen ongewijzigd zodat alle downstream-consumenten (Epic 3/6) automatisch meeliften. Het schrijf-pad (`updateExceptionForDate`/`setExceptionForDate`, Story 6.1/6.2's "tijd verruimen") blijft bewust buiten deze taak — dat krijgt zijn eigen rework bij die stories. Status teruggezet naar `ready-for-dev`; bestaande Dev Agent Record/Review Findings/File List van de oorspronkelijke implementatie ongewijzigd gelaten als historisch record. |
| 2026-09-03 (na de Task 7-implementatie hieronder) | **Code review (Opus 5, /code-review) op Task 7 — 4 bevindingen, alle 4 gefixed, plus 2 zelf-ontdekte gerelateerde gaten meteen meegenomen (met akkoord Hillebrand):** (1) [Hoog] `getExceptionForDate` had na Task 7 geen enkele lezer meer, waardoor Epic 6's al-live "tijd verruimen"-aanbeveling (`apply-recommendation.ts`) en de handmatige tijd-override (`shortfall.post.ts`, 3.1-reden-kiezen) stilzwijgend niets meer deden — een geaccepteerde aanbeveling of ingetypte correctie had géén effect. Gefixed: `detectShortfallForDate` kreeg een nieuwe optionele `availableMinutesOverride`-parameter (alleen voor de aanroepende request, geen persistente exceptie meer — dat zou AD-10 opnieuw schenden); `applyVerruimen` en de nu-dode `updateExceptionForDate`/`setExceptionForDate` (`server/data/availability.ts`) gooien voortaan een expliciete fout i.p.v. een leeg succes; de "tijd verruimen"-aanbevelingstier wordt niet meer gegenereerd. **Tijdens het fixen zelf ontdekt (niet door de review gevonden):** twee andere live routes (`PATCH /api/availability/exceptions/[date]`, `POST /api/availability/day/[date]/prefill-conflict`, gebruikt door `/agendaconflict/aanpassen`) hadden precies hetzelfde stille-no-op-probleem — na overleg met Hillebrand met dezelfde expliciete-fout-aanpak gefixed (beide routes geven nu een nette 500 i.p.v. een valse succesrespons); de onderliggende `availabilityWriteLocks`-lock-functies en dode constanten zijn opgeruimd (implementatie blijft terugvindbaar in de git-geschiedenis van vóór 2026-09-03). Het echte "wat betekent tijd verruimen/aanpassen onder het Calendar-model"-productvraagstuk blijft expliciet Story 6.1/6.2/6.7's eigen AD-10-rework. (2) [Medium] `buildWeekDay` (`week-overview.ts`) deed twee live Calendar-calls voor exact dezelfde user/datum (zelf + via `detectShortfallForDate`) — gefixed door de al-opgehaalde waarde als `availableMinutesOverride` door te geven, één call i.p.v. twee per dag. (3) [Laag, bewust] `create-task.ts`'s scheduling-berekening had voor het eerst een onbehandelde live Calendar-afhankelijkheid — bewust géén Notification toegevoegd (buiten AD-6's bindingsscope, UJ-2 zit niet in UJ-6/7/8), wel een duidelijk gelabelde log-regel vóór de bestaande generieke 500. (4) [Medium] `calendar-blocks.ts`'s `mergedBlockMinutes` clampte events niet op het dagvenster, waardoor een middernacht-overschrijdend blok op beide dagen volledig (i.p.v. gesplitst) meetelde — gefixed door elk event te clampen op `[timeMin, timeMax]` vóór het mergen. Typecheck, build en een live smoke-test tegen de dev-stage (opnieuw gedeployed) bevestigen: `/api/week` en `/api/day/shortfall` werken nog normaal (200), en de twee nu-buiten-werking-gestelde routes geven een nette 500-envelope i.p.v. een stille valse-succesrespons. Geen aparte diepere live-test van de override-waarde zelf (geen taken aanwezig om een echt tekort mee te reproduceren) — laag risico, de wiring is triviaal en via code gelezen bevestigd. |
| 2026-09-03 | **Task 7 geïmplementeerd en live geverifieerd** — nieuw `server/domain/availability/calendar-blocks.ts` (`getAvailableMinutesForDate`), `getEventsForCalendar` geëxporteerd uit `day-events.ts`, `doelmoment.ts`'s drie functies herbouwd op de nieuwe bron (`averageDailyAvailableMinutes` nu een 14-daags voortschrijdend venster i.p.v. het statische 7-daagse weekpatroon). Typecheck/build slagen. Deployed naar de dev-stage (`npx sst deploy --stage dev`, expliciete toestemming Hillebrand na een classifier-blokkade op de eerste poging) en live geverifieerd via curl + een handmatig verzegelde sessiecookie (zelfde `sealed-cookie`-techniek als eerdere stories, ditmaal met het juiste cookie-`name` — bleek `nuxt-session`, niet h3's default `h3`, een kleine correctie t.o.v. eerdere Dev Notes-aannames): zonder gekoppelde agenda correcte 0-minuten-terugval (twee testtaken, plaatsing bevestigd via `/api/week`); tijdelijk gekoppeld aan een echte agenda ("Gezin", na overleg) → geen crash tegen echte Google-events. De mislukte-Calendar-call-deelscenario is bewust niet getest (het ontsleutelde access-token naar schijf halen werd terecht geblokkeerd door de veiligheidsclassifier) — expliciet als open gat vermeld in Task 7, geen blokkade voor "review". Alle testtaken verwijderd; `availabilityCalendarId` na overleg met Hillebrand direct in de database teruggezet naar `null` (er bestaat geen ontkoppel-endpoint, bewuste eerdere productbeslissing bij Story 2.1). Status → review. |
| 2026-09-03 | **Akkoord Hillebrand** op de code review-fixes hierboven (Opus 5-bevindingen + de 2 zelf-ontdekte gerelateerde routes) — geen verdere verificatieronde gevraagd. Status → done. |
| 2026-09-04 (verzoek Hillebrand) | **Sessies/beschikbare tijd mogen nooit in het verleden vallen.** Twee gaten gefixt: (1) `getAvailableMinutesForDate` (`calendar-blocks.ts`) telde voor vandaag de hele dag mee, ook een deel/heel blok dat al verstreken was — geclamped op `max(lokale middernacht, nu)` wanneer `date === todayInAmsterdam()`, toekomstige dagen ongewijzigd. (2) Drie plekken (`data/tasks.ts`'s `createTaskAndSession`/`placeSessionWithStackingOffset`, `session-placement.ts`'s `placeSessionOnDate`) stapelden een nieuwe/herplande sessie altijd vanaf het vaste `SESSION_ANCHOR_HOUR` (16:00), ook als het al later op de dag was — een nieuwe sessie op vandaag kon zo een startsAt in het verleden krijgen. Nieuwe gedeelde `resolveAnchorHourMinute` (`data/tasks.ts`, geëxporteerd): voor vandaag, als "nu" later is dan het anker, wordt "nu" zelf de stapelbasis; voor toekomstige dagen ongewijzigd. Nieuwe `nowAmsterdamHourMinute` toegevoegd aan `shared/utils/scheduling.ts`. `typecheck`/`build` schoon, opnieuw gedeployed, live geverifieerd op de dev-stage (22:23 lokale tijd): een nieuwe sessie op vandaag kreeg `startsAt` 22:23 (het huidige moment) i.p.v. het al-verstreken 16:00-anker, en `availableMinutes` voor vandaag viel terecht terug naar 0 zodra het laatste blok van die dag al verstreken was. Testtaken opgeruimd. |

| 2026-09-05 | **Heropend (Task 8 toegevoegd) — meerdere sessies vooruit plannen** (Correct Course, `sprint-change-proposal-2026-09-05.md`, na live-verificatie van Eveliens "wiskunde — hoofdstuk 4"-taak: 7 uur nodig, 25 min/sessie, deadline over ~1 week, maar slechts 1 sessie gepland — buiten een echt studietijd-blok). AC #3 toegevoegd (plan alle sessies tot het doelmoment, binnen echte blokken), nieuwe technische vereiste `getAvailableBlocksForDate` vastgelegd. Geen PRD/architectuurwijziging nodig — AD-1/AD-3 beschreven dit model al. Status teruggezet naar `in-progress`. |
| 2026-09-05 | **Task 8 geïmplementeerd.** Nieuw: `getAvailableBlocksForDate`/`getTimedBlocksAndWindow` (`calendar-blocks.ts`), `planSessionSlots`/`packSlotsInBlocks` (`doelmoment.ts`), `createTaskAndSessions`/`getSessionsForTask`/`deleteTaskAndSessions`/`insertSessionsForTask`/`deleteSessionsById` (`server/data/tasks.ts`). Tijdens implementatie bleek de "precies 1 sessie per taak"-aanname niet alleen in Story 3.5's herberekening te zitten maar in 7 aanroepplekken over 6 bestanden — na overleg met Hillebrand ("volledige rework nu, stap voor stap") zijn alle 7 meegenomen: `recalculate.ts` (3.5, regenereert nu de volledige sessiereeks, eerstvolgende sessie blijft in-place om een live-sessie nooit te laten verdwijnen), `replan.ts` (4.7, nieuwe `excludeSessionId`-optie), `delete-task.ts` (Epic 5), `apply-recommendation.ts`/`shortfall.ts` (Epic 6 — aanbeveling-id's dragen nu een expliciet sessie-id i.p.v. impliciet "de sessie van de taak"), `energy.ts` (Epic 6.4 — `EnergyProposalItem.sessionId`). Zie Task 8's eigen subtaken hierboven voor het volledige overzicht per bestand. `npm run typecheck`/`npx nuxt build` slagen. **Nog niet live gedeployed/geverifieerd** — wacht op akkoord Hillebrand. |
| 2026-09-05 | **Live gedeployed en geverifieerd op de dev-stage.** Eveliens scenario (420 min, 25 min/sessie, deadline over ~1 week) → 17 sessies over 5 dagen, allemaal binnen echte blokken, geen tekort — bevestigt de kern van dit Correct Course-traject. **Tijdens verificatie een écht resterend gat gevonden en gefixed:** een taak die haar deadline niet kan halen werd niet gedetecteerd (`detectAnyShortfall` toetste alleen dag-aggregaten, die met netjes verspreide sessies nooit overschreden raken). Toegevoegd: `detectDeadlineOverrun`/`ShortfallResult.overrunTaskId`/`generateDeadlineOverrunRecommendations`/`detectShortfallForDateOrOverrun` (`shortfall.ts`), en de accept/recheck/reject-routes aangepast om deze nieuwe her-detectie te gebruiken. Opnieuw gedeployed en herverifieerd: de fix werkt end-to-end (detectie → aanbevelingen → "laten vervallen" accepteren → tekort opgelost). Eén transiënte Calendar-API-fout tegengekomen (bekend, geaccepteerd risico sinds Story 3.1 Task 7, iets vaker zichtbaar door meer Calendar-aanroepen per taak-aanmaak) — geen structurele fix, genoteerd als Open Question. Alle testdata opgeruimd. |
| 2026-09-05 | **Story 4.7's sessie-afronden-flow apart live getest** via `POST /api/sessions/{id}/replan` — minder sessies nodig (4→1), meer sessies nodig (1→5), volledig afronden (1→0, `completedAt` gezet): alle drie correct. Testdata opgeruimd. Stories 3.4, 3.5 en 4.7 (meegenomen tijdens déze story's Task 8, zie hierboven) staan mede hierdoor ook op `review` in `sprint-status.yaml`. Status → review. |
| 2026-09-05 | **Code review-ronde 2** (Blind Hunter + Edge Case Hunter + Acceptance Auditor, Opus 5, parallel op de volledige `server/`-diff): 3 decision-needed, 12 patch, 4 defer, 1 dismissed. Kern van de decisions (alle drie gekozen voor de meest grondige oplossing, met Hillebrand): (1) herplannen/energie-pad (Epic 6) blok-bewust maken — nieuwe `findBlockAwareSlot` (`doelmoment.ts`), gebruikt door `session-placement.ts`; (2) sessies spreiden richting het doelmoment i.p.v. front-loaded plannen — `planSessionSlots` krijgt een zelfcorrigerend dagtarget; (3) de TOCTOU-concurrency-lock (`sessionPlacementLocks`) die per ongeluk was komen te vervallen, hersteld via een nieuwe `withSessionPlacementLocks`-helper. Van de 12 patches waren de belangrijkste: het terugvalpad zonder gekoppelde agenda kon sessies in het verleden/op de verkeerde kalenderdag plaatsen (nu gefixed); `recalculateTaskPlanning` verloor atomiciteit en verwijderde een net-afgeronde sessie vóór een potentieel falende herberekening (nu één atomaire transactie, ná een geslaagde planning); een taak zonder ingevulde "resterende tijd" (Story 7.1) trok bestede tijd niet af van het totaal; een taak met een al-verstreken deadline blokkeerde permanent alle andere tekort-detectie; en twee gelijktijdig overrunnende taken konden elkaars aanbevelingen laten "verdwijnen" (stille no-op bij accepteren). Alle 15 patches doorgevoerd, `npm run typecheck`/`npx nuxt build` slagen, opnieuw gedeployed naar `flowz.fyi` en live geverifieerd: Eveliens 420-min-scenario spreidt nu merkbaar meer (1/4/2/2/8 i.p.v. 1/7/2/2/5 sessies per dag); twee gelijktijdig overrunnende taken kregen elk hun eigen, correct te accepteren aanbeveling (geen stille no-op meer). Eén bekende restbeperking genoteerd als Open Question #9 (herplannen/energie-pad se kandidaat-zoeklus blijft aggregaat-gebaseerd, de uiteindelijke plaatsing zelf is wel blok-bewust en faalt hard i.p.v. stil een blok te schenden). Alle testdata opgeruimd. |
| 2026-09-06 | **Volledige-codebase-reviewcampagne gestart** (op verzoek van Hillebrand): dezelfde 3 agents (Blind Hunter, Edge Case Hunter, Architecture Auditor — deze laatste vervangt de Acceptance Auditor voor een codebase-brede audit, toetst tegen `ARCHITECTURE-SPINE.md`'s AD-1..AD-10 i.p.v. één story's AC's) worden per stuk (~2500 regels) op de hele `server/`+`app/`+`shared/`-codebase losgelaten. Stuk 1 (scheduling-core, 19 bestanden): stevige, elkaar bevestigende bevindingen — een UTC-vs-Amsterdam-lokale-datummismatch die door de hele engine liep, een concurrency-lock die niet meer sloot wat hij moest sluiten, `findBlockAwareSlot` die de hele taak uitsloot i.p.v. alleen de te verplaatsen sessie, gedupliceerde/verkeerd-getelde niveau-4-aanbevelingen, een fantoomtekort dat de hele dag doorgroeide, een onverwijderbare 0-minuten-taak, afgeronde/vervallen taken die alsnog herpland konden worden, en een gegarandeerde crash bij het accepteren van een weekoverzicht-suggestie. Op verzoek van Hillebrand is de resterende 5-stukken-campagne (data-laag, API-routes, 3x frontend) **niet** doorgezet — eerst is deze ernstige oogst uit stuk 1 zelf gerepareerd. |
| 2026-09-06 | **Review-ronde 3, deel 1 (fixes op stuk 1's bevindingen).** Root-fix: `PlannedSessionSlot.date` wordt nu altijd uit `startsAt` (UTC) afgeleid, nooit meer uit een apart bijgehouden Amsterdam-lokale dag — sluit de lock-sleutel-mismatch tussen `createTaskAndSessions`/`recalculateTaskPlanning` vanzelf mee. `findBlockAwareSlot` sluit nu alleen de specifieke sessie uit (niet de hele taak, `session-placement.ts`). `shortfall.ts`: niveau 1's `relocated`-set is nu sessie- i.p.v. taak-gesleuteld (voorkomt dat het verplaatsen van 1 sessie een taak se ándere sessie ten onrechte van niveau 3/4 uitsluit), niveau 4 dedupliceert per taak en telt de winst correct op, "tijd verruimen" trekt niet langer een nooit-verzilverde winst af van `remaining`. `sumPlannedMinutesForUserOnDate`: al-verstreken sessies van vandaag tellen niet meer mee (voorkomt een fantoomtekort dat de hele dag doorgroeit). `delete-task.ts`: een 0-sessie-taak (geldig bij `totalMinutesOverride: 0`) is niet langer onverwijderbaar. `update-task.ts`/`replan.ts`: een afgeronde/vervallen taak wordt niet meer herpland. `session-heartbeat-fallback.ts`: een nieuwe atomaire `claimStaleSessionForFinalization` sluit een dubbele-afronding-race. `startup-check.ts`: een falende blok-plaatsing tijdens de stille auto-herplan crasht niet meer de hele opstart-check. `week/[date]/suggestion/accept.post.ts`: crasht niet meer als de eerste aanbeveling toevallig "tijd verruimen" is. Ownership-checks + niet-positieve-minuten-guards toegevoegd in `apply-recommendation.ts`/`energy.ts`. Typecheck/build/deploy/live-verificatie: allemaal geslaagd. |
| 2026-09-06 | **Review-ronde 3, deel 2 (hertoetsing).** Dezelfde 3 agents zijn hervat (`SendMessage`, niet opnieuw vanaf nul) om exact dezelfde bestandenset te hertoetsen na de fixes hierboven. Bevestigd gefixed: alle bovenstaande punten. **Eén echte regressie in de eigen fix gevonden:** de nieuwe "verstreken sessies tellen niet mee"-filter zat alleen in `sumPlannedMinutesForUserOnDate`, niet in het zusje `getTasksWithSessionOnDate` — de escalatie-service (die beide leest) kon zo een al-voorbije sessie "oplossen" zonder dat het echte tekort daalde. Gefixed: dezelfde filter nu ook in `getTasksWithSessionOnDate`. Overige fixes deze ronde: een compenserende `releaseStaleSessionClaim` als de claim wél lukt maar de daaropvolgende herberekening faalt (anders bleef de sessie voorgoed "gestopt" zonder ooit gelogd te zijn); `placeSessionOnDate` kreeg zijn ownership-check terug (was per ongeluk verdwenen toen de `task`-parameter werd verwijderd in ronde 2); de dode `getOrCreateWeekPattern`/`getExceptionForDate` (AD-10-restanten zonder aanroepers) zijn verwijderd i.p.v. alleen het misleidende commentaar te corrigeren; diverse stale/onjuiste commentaren gefixed (een expliciet foutieve "veilig omdat..."-claim over de UTC-datumconventie, verwijzingen naar niet meer bestaande `findSessionDate`/`MAX_SEARCH_DAYS`/"precies 1 sessie"). Typecheck/build/deploy/live-verificatie (normale taak, 0-min-taak-verwijdering, tekort-detectie): allemaal geslaagd, alle testdata opgeruimd. **Bewust NIET verder gepatcht** (zie nieuwe Open Questions #10-14): interval-gebaseerde bezetting i.p.v. een scalair minutenaantal (`packSlotsInBlocks` kan nog steeds op een bestaande sessie overlappen na een `inkorten`-verkorting), volledige TOCTOU-sluiting (de planningsfase valt nog buiten de lock), AD-6 Notification-shape-verankering (nul aanroepers, bekend sinds eerdere rondes), de resterende UTC/Amsterdam-daggrens-spanning bij vroege-ochtend-blokken, en de live-Calendar-N+1-stapeling. Elk hiervan vergt een écht herontwerp, niet nog een losse patch — na drie ronden is het risico dat een snelle patch een nieuwe regressie introduceert (zoals hierboven gebeurde) groter dan de marginale winst. |
| 2026-09-06 | **Volledige-codebase-review, chunk 2** (data-laag `server/data/`, Calendar-integratie `server/domain/calendar-sync/`+`server/domain/auth/`, `shared/`): grotendeels pre-existente architecturale schuld (locking-ontwerp, OAuth-refresh-robuustheid, credential-redactie, dode AD-10-tabellen) — vastgelegd in `deferred-work.md`, niet in deze ronde gepatcht (eigen productbeslissingen nodig, te groot voor een reviewronde). **Twee echte regressies uit ronde 3/4 zelf gevonden en meteen gefixed (ronde 4):** (1) `releaseStaleSessionClaim` deed een onvoorwaardelijke reset i.p.v. compare-and-set — een legitieme Stop-knop-klik die tussen de claim en een mislukte herberekening viel, kon zo ongedaan gemaakt worden; nu scopet `claimStaleSessionForFinalization`/`releaseStaleSessionClaim` samen op het exacte geclaimde `stoppedAt`-tijdstip. (2) `syncHomeworkBlocksForDate` gebruikte de nieuwe "verstreken sessies tellen niet mee"-capaciteitsfunctie (`getTasksWithSessionOnDate`) om te bepalen welke Calendar-blokken moeten bestaan — zodra een sessie voorbij was, verdween 'm stilzwijgend uit die berekening en verwijderde de sync het bijbehorende, al-plaatsgevonden Calendar-event van Eveliens echte agenda. Gefixed: gebruikt nu `getTasksWithSessionOnDateIncludingCompleted` (geen elapsed-filter, toont ook afgeronde taken — beide correct voor "wat hoort er op de agenda te staan", in tegenstelling tot "hoeveel capaciteit is er nog"). Typecheck/build/deploy/live-verificatie geslaagd, testdata opgeruimd. |
| 2026-09-06 | **Volledige-codebase-review, chunk 3** (API-routes `server/api/**`, `server/middleware/session.ts`, `server/routes/auth/*` — 39 bestanden). Hoogste-risico bevindingen direct gefixed: `week/[date]/suggestion/accept.post.ts` paste voorheen blindelings `recommendations[0]` toe zonder te controleren of de client die suggestie daadwerkelijk zag — kon een taak laten vervallen op een klik bedoeld als "verplaats naar donderdag"; vereist nu een client-meegestuurd aanbeveling-id, gebruikt `detectShortfallForDateOrOverrun` (en `week-overview.ts`'s `buildWeekDay` inmiddels ook, anders was de overrun-tak onbereikbaar), en geeft een 404 i.p.v. een stille 200 bij een verlopen id. De Google-write-scope-upgrade (`auth/google.get.ts`) kon het publieke-computer-vinkje van een ander account overnemen bij een accountwissel in Google's kiezer — nu pas overgenomen wanneer de zojuist ingelogde gebruiker daadwerkelijk dezelfde is als de vorige sessie. `school-sessions.post.ts` kreeg een batchgrens (50), rowId/taskId-dedupe (taskId per-regel, rowId hele-batch — matcht het bestand se eigen per-regel-contract), en een compenserende `deleteTask()`-rollback op alle vier de faalpaden na een geslaagde `createTask()`, zodat een retry van een mislukte "nieuwe taak"-regel geen weeskind-taak meer achterlaat. Verder: vier GET-routes (`hidden-calendar-titles.get`, `homework-calendar-color.get`, `tasks/subjects.get`, `tasks/needs-suggestions.get`) kregen het ontbrekende try/catch → error-envelope-patroon van hun `availability-calendar.get.ts`-buurman; `home/plan.get.ts`'s stille `catch {}` logt nu; `day/shortfall.post.ts` geeft nu een 400 op een kapotte JSON-body i.p.v. die stil als leeg verzoek te behandelen; het verwijderen van een verborgen agenda-titel decodeert/trimt de routeparameter nu (met een eigen try/catch tegen een ongeldige percent-sequentie) zodat die weer matcht met hoe POST 'm opslaat, plus een lengte-/aantalgrens op de lijst zelf; de sessie-middleware se publieke-pad-matching is geankerd (geen `/inloggen-x`/`/__nuxtfoo` meer als publiek). Twee her-reviewrondes (dezelfde 3 agents, `SendMessage`-hervat) vonden en fixten nog 3 gaten in de eerste patchronde: de `verruimen`-tier-guard was per ongeluk verwijderd uit het week-accept-route, `week-overview.ts` was nog niet meeverhuisd naar `detectShortfallForDateOrOverrun` (maakte de overrun-fix onbereikbare code), en de `decodeURIComponent`-fix in het titel-verwijder-route stond zelf buiten zijn try/catch. Derde her-reviewronde bevestigt: alle drie gesloten. Typecheck/build/deploy (2x, na elke patchronde)/live-smoketest geslaagd. Resterende bevindingen (twee incompatibele `envelope()`-vormen, mutatie-ownership-afwijkingen in `subtasks/*`/`sessions/*`/`reopen.post.ts`, ontbrekende boven-/ondergrenzen op uren-/minuteninvoer, `energy-proposal/confirm.post.ts`'s idempotency, `AD-6`'s Notification-vorm die nul aanroepers heeft, e.a.) vastgelegd in `deferred-work.md` als bewuste productbeslissingen/bredere migraties — twee ervan (`reopen.post.ts`'s non-atomaire orkestratie, `startup-check.post.ts`'s ontbrekende concurrency-guard) door de reviewagents zelf als hoogste prioriteit voor een vervolgronde aangemerkt. |
| 2026-09-06 | **Volledige-codebase-review, chunk D** (frontend deel 1: `app/pages/schoolsessies.vue`, `index.vue`, `sessie/actief.vue`, `herstel/tekort-oplossen.vue`). Hoogste-risico bevinding: `tekort-oplossen.vue` kon Evelien op meerdere onafhankelijke manieren permanent vastzetten achter zijn eigen `onBeforeRouteLeave`-guard (AC #4) — een lege aanbevelingslijst, een lijst met uitsluitend "tijd verruimen"-kaarten, een hangende `$fetch` zonder timeout. Een `stuck`-computed (over de volledige `allRecommendations`, niet de op 3 afgekapte zichtbare set) met een expliciete "Terug naar Home"-uitweg toegevoegd, zonder de recheck-kaarten te verbergen (die blijven het enige bruikbare pad in het "alleen verruimen"-geval); een gedeelde `withTimeout`-helper op alle vier de server-aanroepen; `afwijzen` kreeg zijn ontbrekende `busyId`-guard én herlaadt nu bij een lege respons i.p.v. een verouderde `shortfallMinutes` te laten staan. `sessie/actief.vue`: een in-place route-wissel tussen twee `/sessie/actief`-URL's liet de pagina blanco vallen — de watcher navigeert nu zelf; dubbele-tap-guards op de subtaakknoppen; `stopSessie` wacht de navigatie af vóór het legen van state (voorkomt een blanco flits); een 401 op de heartbeat-/subtaak-aanroepen navigeert nu naar inloggen zonder de eigen leave-guard te laten vastlopen in een oneindige bevestig-modal-lus. `index.vue`: `today` is nu reactief (voorkomt een foute datum na middernacht bij een open tabblad); een 0-minuten/omgekeerd Calendar-event verdwijnt niet meer stilzwijgend. `schoolsessies.vue`: getypte invoer overleeft nu elke reload (heropenen, een gedeeltelijk mislukte batch); dezelfde taak kan niet meer dubbel gekozen worden via zoeken; het heropen-paneel valideert nu net zo streng als de server. Drie her-reviewrondes (dezelfde 3 agents, `SendMessage`-hervat) vonden en fixten in totaal 9 zelf-geïntroduceerde gaten over twee rondes: ronde 2 sloot o.a. de false-positive in `stuck` (las de afgekapte i.p.v. volledige lijst), het per ongeluk verbergen van de recheck-kaarten, en een bounce-back-lus tussen de "Terug naar Home"-link en `index.vue`'s opstart-check-redirect (opgelost met een gedeelde eenmalige skip-vlag). **Ronde 3 draaide één significante ronde-2-regressie terug:** het versturen van een stop-signaal bij het wegnavigeren van een gepauzeerde sessie bleek de bestaande server-side terugvalroute (`finalizeStaleSessionIfNeeded`) te saboteren — die herkent een verweesde sessie aan `stoppedAt === null`, en de ronde-2-fix zette dat veld zelf al, zonder bestede tijd te loggen. Bestede tijd van een gepauzeerde, verlaten sessie werd zo niet "eindelijk gestopt" maar permanent stilzwijgend weggegooid — teruggedraaid naar het oorspronkelijke gedrag (stilzwijgend toestaan). Typecheck/build/deploy (3x, na elke ronde)/live-smoketest geslaagd. Resterende bevindingen (AD-6/AD-10 Calendar-fout-als-technische-envelope, overlappende Calendar-blokken die elkaar occluderen, de vijfvoudig gedupliceerde `is401`, de whole-horizon-vs-single-date-tekortdetectie-mismatch, en — als hoogste-prioriteit resterend item — dezelfde `stoppedAt`-zonder-minuten-fout in de al langer bestaande `beforeunload`-beacon, die ook een gewone lopende sessie raakt) vastgelegd in `deferred-work.md` als productbeslissingen/bredere migraties. |
| 2026-09-06 | **Volledige-codebase-review, chunk E** (frontend deel 2, resterende 11 pagina's: `week/index.vue`, `sessie/overzicht.vue`, `taken/[id]/index.vue`, `herstel/reden-kiezen.vue`, `taken/index.vue`, `herstel/energie-voorstel.vue`, `sessie/starten.vue`, `inloggen.vue`, `instellingen/index.vue`, `taken/[id]/bewerken.vue`, `taak/nieuw.vue`). Hoogste-risico bevindingen direct gefixed: `sessie/starten.vue`'s Start-knop kon op het gouden pad stil een permanente no-op worden bij een mislukte achtergrond-fetch — nu een expliciete foutmelding, een retry die ook ná een eerdere fout werkt, en `sessie-start-taak` eenmalig geconsumeerd. `sessie/overzicht.vue` had geen leave-guard: het hamburgermenu liet de ingevulde resterende tijd stilzwijgend nooit versturen (AC #3) — nu een `onBeforeRouteLeave` die alsnog probeert te versturen, via een gedeeld idempotent `ensureReplanSubmitted()`-gat dat een dubbele-navigatie-race met de knop voorkomt. `week/index.vue`: een mislukte ververs-call ná een geslaagde mutatie werd getoond als "kon niet doorvoeren" (nodigde tot een gevaarlijke dubbele toepassing) — nu gescheiden foutafhandeling plus 400/404/timeout-herlaad i.p.v. een dode retry. `herstel/reden-kiezen.vue`: de ingetypte "hoeveel tijd heb je nog"-override werd weggegooid doordat `/herstel/tekort-oplossen` daarna blindelings de hele horizon scande — nu een gedeelde `shortfallSeed`. Overige fixes: `herstel/energie-voorstel.vue`'s redirect-timer-na-unmount en een misleidende lege-staat-met-levende-knop; `taken/[id]/bewerken.vue`'s stille bounce zonder `replace:true`/flashmelding (zelfde patroon als `taken/[id]/index.vue` al had); `inloggen.vue`'s bfcache-vastzittende inlogknop en ontbrekende `loggedIn`-guard; `taken/index.vue`'s ongeclampte voortgangsbalk; `TaakFormulier.vue`'s niet-opgeruimde `setTimeout`. Vier her-reviewrondes (dezelfde 3 agents, `SendMessage`-hervat) vonden en fixten meerdere zelf-geïntroduceerde gaten, waarvan één **release-blocker**: de globale `sessie-verval.client.ts`-plugin riep bij een 401 geen `clear()` aan, waardoor de client-side `loggedIn`-status stil bleef hangen — de nieuwe `inloggen.vue`-guard stuurde dan direct terug naar `/`, die opnieuw 401't: een `/` ↔ `/inloggen`-lus die het inlogscherm zelf onbereikbaar maakte, precies op het AD-9/UJ-10-sessieverval-pad. Gefixed door `session.value` synchroon te legen vóór de redirect. Een tweede ronde-2-regressie (dezelfde soort "erger gemaakt dan opgelost" als chunk D's stop-beacon-verhaal): de `shortfallSeed`-fix werd eerst versimpeld tot alleen de datum doorgeven (om een AD-1-inconsistentie met accept/reject/recheck te vermijden) — maar dat maakte het hoofdpad van de hele "te weinig tijd"-reis een no-op (een override-loze herberekening bij aankomst vindt per definitie geen tekort). Teruggedraaid naar de volledige-respons-doorgifte; de onderliggende inconsistentie (de override bereikt de mutatie-routes nooit) is een bestaand architecturaal gat, nu expliciet vastgelegd i.p.v. opgelost door het hoofdpad te breken. Typecheck/build/deploy (4x, na elke ronde)/live-smoketest geslaagd. Resterende bevindingen (AD-6/AD-10 Calendar-fout-als-technische-envelope, `is401`/`loggedIn` nu 14× gedupliceerd, `energy-proposal/confirm.post.ts`'s ontbrekende idempotency, de client-side AD-1-afwijkingsdrempel in `sessie/overzicht.vue`, vier verschillende laad/fout/leeg-idiomen, de settings-panelen se mid-save-race, en de override-propagatie naar de shortfall-mutatie-routes) vastgelegd in `deferred-work.md` als productbeslissingen/bredere migraties. |
| 2026-09-07 | **Volledige-codebase-review, chunk F — laatste chunk van de reviewcampagne** (`app/components/TaakFormulier.vue`, `InstellingenBeschikbareTijd.vue`, `InstellingenVerborgenAgendaItems.vue`, `HamburgerMenu.vue`, `InstellingenUiterlijk.vue`, `app/composables/useTheme.ts`, `app/plugins/sessie-verval.client.ts`). Hoogste-risico bevinding: de globale 401-plugin liet `skip-sessie-verval-redirect` zowel de redirect ALS de client-side sessie-`clear()` overslaan — `TaakFormulier.vue` zet die vlag tijdens het opslaan om onopgeslagen data zichtbaar te houden bij een verlopen sessie, maar dat liet `loggedIn` client-side ten onrechte `true` staan, wat een tweede ingang tot dezelfde soort `/` ↔ `/inloggen`-lus als chunk E's release-blocker opende. Gefixed door de sessie ALTIJD te legen bij een 401 en alleen de redirect optioneel te maken. Tweede significante bevinding: `HamburgerMenu.vue`'s "Uitloggen" is een rauwe `<a href>` die elke `onBeforeRouteLeave`-guard omzeilt — gefixed met een `beforeunload`/`sendBeacon`-fallback op `sessie/overzicht.vue` (zelfde precedent als `sessie/actief.vue`'s bestaande stop-beacon). Overige fixes: `TaakFormulier.vue` toont nu de echte serverfoutmelding i.p.v. een generieke tekst, kreeg ontbrekende client-side bovengrenzen (sessieduur, deeltaken-aantal, deeltaaknaam-lengte, matchend de server) en een `onBeforeRouteLeave`-guard voor een browser-terug-gebaar op een gewijzigd formulier; `InstellingenBeschikbareTijd.vue`'s huiswerk-kleur-select race (interactief vóór de eigen rehydratie-GET) en stille laadfout; `InstellingenVerborgenAgendaItems.vue`'s volledig stille verwijder-fout; `useTheme.ts`'s ongeving `localStorage`-toegang (kon de hele app op elke pagina laten stuklopen tijdens hydratie) en een `init()` die de gevalideerde staat nu altijd herbevestigt op de DOM; `HamburgerMenu.vue` kreeg een `touchstart`-listener naast `click` voor iOS. Vier her-reviewrondes (dezelfde 3 agents, `SendMessage`-hervat) vonden en fixten drie zelf-geïntroduceerde regressies: de nieuwe `onBeforeRouteLeave`-guard op `TaakFormulier.vue` liet de bevestigingsmodal in ronde 1 twee keer verschijnen bij Sluiten/Annuleren (een `bypassLeaveGuard`-vlag loste dat op), een globale `skipSessieVervalRedirect`-vlag bleef zonder `onUnmounted`-reset voor de rest van de SPA-sessie op `true` staan na een sessieverval-tijdens-opslaan, en een ronde-2-poging om de vak-wijziging-dialoog in de guard te negeren liet de navigatie per ongeluk ongehinderd door in plaats van 'm te blokkeren (precies omgekeerde polariteit) — in ronde 3 gecorrigeerd. Typecheck/build/deploy (4x, na elke ronde)/live-smoketest geslaagd. Resterende bevindingen (AD-10's `hasCalendarWriteScope`-re-consent-gat, de AD-10/AD-6-spine-vs-code-tegenstrijdigheid op het taak-aanmaken-pad, `is401`/`loggedIn` nu 17× gedupliceerd, de twee instellingen-panelen se top-level-`await`-tabwissel-vertraging, en een theoretisch hydratie-mismatch-risico in `useTheme.ts`) vastgelegd in `deferred-work.md`. **Campagne-brede afsluitende observatie (Architecture Auditor):** het merendeel van de gevonden bugs over alle 6 chunks heen was hetzelfde patroon in een ander jasje — gekopieerde idiomen (`is401`, de `envelope()`-helper, het `server:false`+`watch(error)`+skeleton-drietal) die 17× herhaald zijn i.p.v. gedeeld, en client-side state (een sessie-ref, een globale vlag, een timer) die zijn eigen component overleeft. Beide zijn genoteerd als de hoogste-waarde vervolgstappen ná deze campagne, samen met een voorstel om de architectuur-spine een "client conventions"-rij te geven — geen van beide is in deze bugfix-campagne zelf aangepakt. |
| 2026-09-07 | **`is401`/`is404`-consolidatie** (eerste opvolging op de campagne-brede afsluitende observatie hierboven, op verzoek van Hillebrand). `is401` stond letterlijk identiek in 14 bestanden (17 tellingen incl. `is404` in `sessie/starten.vue`) — vervangen door één `app/composables/httpError.ts` (`is401`/`is404`, Nuxt auto-import). De 14 lokale kopieën verwijderd, plus de overbodig geworden `import type { FetchError } from 'ofetch'` in de 8 bestanden waar dat type verder nergens meer gebruikt werd. Zuiver mechanisch, geen gedragswijziging. Typecheck/build/deploy/live-smoketest geslaagd. |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- **Buffer-in-dagen rondde bij een kleine sessieduur (25 min) voor beide difficulty/priority-combinaties naar dezelfde 1-dagsondergrens af** — geen bug, gewoon een te kleine `totalMinutes` om het verschil zichtbaar te maken tegen het echte weekpatroon (gemiddeld ~77 min/dag). Herbevestigd met `defaultSessionDuration: 600`: hoog/laag → 3 dagen buffer, laag/hoog → 1 dag buffer, zoals de formule voorspelt.
- **DST-conversie in twee losse, gerichte scripts getest (niet aangenomen)** — zowel de oktober- als de maart-omschakeling, telkens de dag ervoor/erop/erna. Alle zes conversies correct; het eenmalige-correctie-algoritme (naïeve UTC-gok, offset aflezen, corrigeren) blijkt robuust voor een 16:00-lokaal-anker omdat dat tijdstip nooit dicht bij het daadwerkelijke omschakelmoment (02:00/03:00 lokaal) ligt.
- **Kleur kon niet meer via de app gewist worden om het geen-kleur-scenario te testen** — Story 2.3's post-review-beslissing (kleur verplicht) blokkeert dat inmiddels bij de PATCH-route zelf. Opgelost door de kolom voor déze test rechtstreeks in de database op `NULL` te zetten (buiten de app om, puur voor verificatie) en nadien weer te herstellen naar Hillebrand's echte keuze.
- **Live e2e-verificatie tegen `https://flowz.fyi`, sealed-cookie-techniek (zelfde als elke eerdere story):** alle 5 validatieregels (deadline-verleden, sessieduur<5, lege titel, ongeldig type, ongeldige difficulty) → juiste 400-envelope; 4 taken aangemaakt met verschillende moeilijkheid/prioriteit-combinaties → doelmoment-verschuiving bevestigd; stapeling van twee sessies op dezelfde dag bevestigd (25 min uit elkaar, geen overlap); Calendar-sync bevestigd zowel mét kleur (4 echte events aangemaakt, titel/tijd rechtstreeks bij Google gecontroleerd, daarna verwijderd) als zonder kleur (geen event, bevestigd met een gerichte `GET`-zoekopdracht); `GET /api/tasks/subjects` bevestigd leeg na opruiming.
- **Browser-rendering van `/taak/nieuw` niet live getoetst** — de Chrome-extensieverbinding was op het moment van verifiëren niet beschikbaar; niet geforceerd/genegeerd, eerlijk gemeld in Task 6 en hier. Sectienesting wél bevestigd via een statische grep-check op de open/sluit-`<section>`-tags.

### Completion Notes List

- **AC #1 en #2 zijn end-to-end geverifieerd tegen de echte, gedeployde omgeving** — validatie, doelmoment-berekening (incl. de daadwerkelijke moeilijkheid/prioriteit-differentiatie), sessie-plaatsing/-stapeling, DST-correcte tijdstip-conversie, en de Calendar-sync-aanroep (zowel met als zonder kleur). Alleen de daadwerkelijke browser-rendering van het formulier is niet met echte clicks getoetst (extensie niet verbonden) — een bekend, expliciet gemeld gat, geen verzwegen aanname.
- **Scope strak gehouden, conform de story's eigen "buiten scope"-lijst:** geen deeltaken/benodigdheden/handmatige-totale-tijd-UI (Story 3.2/3.3), geen volgorde-algoritme bij concurrentie (Story 3.4, alleen simpele stapeling), geen tijdgebrek-escalatie (Epic 6), geen `googleEventId`-opslag (nog steeds Story 2.3's grens), geen bewerken/verwijderen (Epic 5).
- **Twee kwaliteitsronde vooraf toegepast, geen reviewbevindingen achteraf nodig geweest voor deze punten:** een onafhankelijke fresh-context validatiepas (tijdens create-story) vond één echte inconsistentie (ontbrekende server-side enum-validatie voor `difficulty`/`priority`, risico op stille `NaN`-propagatie) — meteen gefixed vóórdat dev-story begon, dus al in de eerste implementatie correct.
- **Drie kleine, expliciet gedocumenteerde Open Questions blijven open voor Hillebrand** (bufferformule-cijfers, `totalMinutes`-timing, Omschrijving-veld-toewijzing) — geen van drie blokkeerde de implementatie, allemaal met een beargumenteerd, makkelijk aan te passen default geïmplementeerd.
- **`.gitkeep`-bestanden verwijderd** uit `server/domain/scheduling/` en `server/domain/tasks/` zodra ze echte inhoud kregen — consistent met hoe alle eerdere Structural-Seed-mappen dat deden.

### File List

**Nieuw:**
- `server/data/migrations/0005_long_romulus.sql` (+ bijbehorende meta-bestanden)
- `server/data/tasks.ts` (incl. de post-review `createTaskAndSession`/`deleteTaskAndSession`; 2026-08-18: `createTaskAndSession` gewrapt met `acquireSessionPlacementLock`/`releaseSessionPlacementLock`)
- `server/domain/scheduling/doelmoment.ts`
- `server/domain/tasks/create-task.ts`
- `server/api/tasks.post.ts`
- `server/api/tasks/subjects.get.ts`
- `app/pages/taak/nieuw.vue`
- `shared/types/tasks.d.ts`
- `shared/utils/scheduling.ts` (post-review — `todayInAmsterdam`/`amsterdamLocalToUtcIso`, verplaatst uit `server/domain/scheduling/session-time.ts` zodat client en server dezelfde definitie van "vandaag" delen)

**Gewijzigd:**
- `server/data/schema.ts` (nieuwe `tasks`/`sessions`-tabellen, `TASK_TYPES`/`DIFFICULTY_LEVELS`/`PRIORITY_LEVELS`)
- `server/data/availability.ts` (nieuwe `getExceptionForDate`)

**Verwijderd (post-review):**
- `server/domain/scheduling/session-time.ts` — logica verplaatst naar `shared/utils/scheduling.ts` (client/server-hergebruik) resp. `server/data/tasks.ts` (de stapelings-som-lezing zit nu in de transactie)

**Hergebruikt, ongewijzigd:**
- `server/domain/calendar-sync/homework-events.ts` (Story 2.3, `createHomeworkEvent` rechtstreeks aangeroepen)
- `server/data/availability.ts`'s `getOrCreateWeekPattern` (Story 2.1)
- `shared/utils/availability.ts`'s `isValidCalendarDate` (Story 2.1/2.2)

**Verwijderd:**
- `server/domain/scheduling/.gitkeep`, `server/domain/tasks/.gitkeep` (mappen hebben nu echte inhoud)

**Live gedeployed:** dev-stage op `flowz.fyi`, migratie toegepast op de echte Turso-database.

---

**Task 7 (AD-10-rework, 2026-09-03) — nieuw:**
- `server/domain/availability/calendar-blocks.ts` (`getAvailableMinutesForDate`)

**Task 7 — gewijzigd:**
- `server/domain/scheduling/doelmoment.ts` (`availableMinutesForDate`/`findSessionDate`/`averageDailyAvailableMinutes` herbouwd op de live Calendar-bron; `pattern`-parameter en `getExceptionForDate`/`getOrCreateWeekPattern`-afhankelijkheid verwijderd)
- `server/domain/calendar-sync/day-events.ts` (`getEventsForCalendar` geëxporteerd, was module-privaat)

**Code review-fixes (2026-09-03) — gewijzigd:**
- `server/domain/scheduling/shortfall.ts` (`detectShortfallForDate` nieuwe optionele `availableMinutesOverride`-parameter; "verruimen"-aanbevelingstier niet meer gegenereerd, `VERRUIMEN_STEP_MINUTES` verwijderd)
- `server/api/day/shortfall.post.ts` (override gaat direct naar `detectShortfallForDate` i.p.v. via een `setExceptionForDate`-write-en-herlees)
- `server/domain/scheduling/apply-recommendation.ts` (`applyVerruimen` gooit nu een expliciete fout, `updateExceptionForDate`-import verwijderd)
- `server/domain/scheduling/week-overview.ts` (`buildWeekDay` hergebruikt de al-opgehaalde `availableMinutes` i.p.v. een tweede Calendar-call)
- `server/domain/tasks/create-task.ts` (scheduling-berekening in een try/catch met een duidelijk gelabelde log-regel bij een Calendar-fout)
- `server/domain/availability/calendar-blocks.ts` (`mergedBlockMinutes` clampt events nu op het dagvenster vóór het mergen — middernacht-overschrijdende blokken telden voorheen dubbel)
- `server/data/availability.ts` (`updateExceptionForDate`/`setExceptionForDate` buiten werking gesteld — expliciete fout i.p.v. een schrijf-naar-nergens; `acquireAvailabilityWriteLock`/`releaseAvailabilityWriteLock`/`DELTA_MINUTES` opgeruimd, enige aanroepers weg)

**Task 7 — live gedeployed:** dev-stage op `flowz.fyi` (`npx sst deploy --stage dev`, twee keer — vóór en ná de code review-fixes). Geen migratie nodig (geen schemawijziging).

---

**Task 8 (meerdere sessies vooruit plannen, 2026-09-05) — gewijzigd:**
- `server/domain/availability/calendar-blocks.ts` (nieuwe `getAvailableBlocksForDate`, module-interne `getTimedBlocksAndWindow`-extractie)
- `server/domain/scheduling/doelmoment.ts` (nieuwe `planSessionSlots`/`packSlotsInBlocks`/`PlannedSessionSlot`; `findSessionDate` ongewijzigd gelaten, geen aanroepers meer)
- `server/data/tasks.ts` (`createTaskAndSession` → `createTaskAndSessions`; `deleteTaskAndSession` → `deleteTaskAndSessions`; nieuwe `getSessionsForTask`/`insertSessionsForTask`/`deleteSessionsById`; `getSessionForTask` herzien naar "eerstvolgende sessie"-semantiek, geen `Error` meer bij >1 rij; `sumPlannedMinutesForUserOnDate`'s `excludeTaskId` accepteert nu ook een array)
- `server/domain/tasks/create-task.ts` (gebruikt `planSessionSlots`/`createTaskAndSessions`, synct Calendar-blokken per betrokken datum)
- `server/domain/scheduling/recalculate.ts` (`recalculateTaskPlanning` regenereert de volledige sessiereeks, nieuwe `excludeSessionId`-optie, return-waarde `sessions` i.p.v. `session`)
- `server/domain/scheduling/replan.ts` (geeft `excludeSessionId` door, return-waarde `sessions`, synct alle sessiedatums bij volledige afronding)
- `server/domain/tasks/delete-task.ts` (gebruikt `getSessionsForTask`/`deleteTaskAndSessions`, synct alle betrokken datums)
- `server/domain/scheduling/shortfall.ts` (`ShortfallRecommendation.id`-formaat voor `herplannen`/`inkorten` draagt nu ook het sessie-id)
- `server/domain/scheduling/apply-recommendation.ts` (nieuwe `parseTaskAndSessionId`-helper, `getSessionById` i.p.v. `getSessionForTask` voor `herplannen`/`inkorten`, `applyVervallen` synct alle sessiedatums)
- `server/domain/scheduling/energy.ts` (`EnergyProposalItem.sessionId` toegevoegd; "naar voren halen"-pad zoekt de juiste sessie ná de betrokken dag i.p.v. de eerstvolgende sessie ooit)
- `server/api/day/shortfall/recommendations/[id]/accept.post.ts`, `recheck.post.ts`, `reject.post.ts` (gebruiken nieuwe `detectShortfallForDateOrOverrun` i.p.v. `detectShortfallForDate`, zodat een deadline-overrun-aanbeveling ook echt geaccepteerd/herchecked/afgewezen kan worden)

**Task 8 (deadline-overrun-detectiefix, tijdens live-verificatie ontdekt) — gewijzigd:**
- `server/domain/scheduling/shortfall.ts` (nieuwe `detectDeadlineOverrun`, `detectShortfallForDateOrOverrun`, `generateDeadlineOverrunRecommendations`; `ShortfallResult.overrunTaskId`; `detectAnyShortfall`/`generateShortfallRecommendations` roepen deze nieuwe paden aan vóór hun bestaande dag-aggregaatlogica)

**Task 8 — live gedeployed:** dev-stage op `flowz.fyi` (`npx sst deploy --stage dev`, drie keer — initiële implementatie, ná de deadline-overrun-fix, geen verdere wijzigingen nodig na de laatste verificatieronde). Geen migratie nodig (geen schemawijziging). Alle testdata (2 taken, 18 sessies totaal) opgeruimd via `DELETE /api/tasks/{id}`.
