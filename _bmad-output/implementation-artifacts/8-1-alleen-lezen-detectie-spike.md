---
baseline_commit: 0302814
---

# Story 8.1: Alleen-lezen Detectie-spike

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As developer,
I want een watch-kanaal + syncToken-sync bouwen die uitsluitend logt wat er gedetecteerd wordt,
so that de echo-detectie-aanname (Google's `updated`/`etag`-gedrag) tegen de échte Calendar gevalideerd is vóór er iets gemuteerd wordt.

## ⚠️ Dit is een experiment, geen feature — lees dit voordat je AC's als een gewone checklist behandelt

**Doel van deze story is een JA/NEE-antwoord voor Hillebrand, niet een afgeronde tweewegs-sync.** Epic 8 is expliciet **gated** achter AD-11 (`ARCHITECTURE-SPINE.md`, status PROPOSED, niet ADOPTED) — Story 8.2 (mutatie: een handmatige verplaatsing overnemen) en 8.3 (debounce) mogen **niet** gebouwd worden vóórdat déze spike is uitgevoerd, de uitkomst met Hillebrand besproken is, en hij AD-11 expliciet naar ADOPTED zet. Deze story zelf is de bewuste uitzondering op die gate — het IS het experiment waarvan de uitkomst die beslissing informeert.

**Wat dit betekent voor de implementatie:**
- **Geen mutatie aan `sessions`/`tasks`.** Deze spike mag Google Calendar-wijzigingen detecteren en erover loggen — nooit een sessie verplaatsen, een taak aanpassen, of iets terugschrijven naar Calendar buiten de al-bestaande `syncHomeworkBlocksForDate`-flow.
- **CloudWatch-logs zijn de uitkomst, geen nieuwe UI.** Geen dialoog, geen dashboard — een handmatige verplaatsing van een huiswerk-event moet gewoon een duidelijk herkenbare logregel opleveren (zie Task 5/6 voor het exacte experiment en de rapportage-vorm).
- **Rapporteer, besluit niet zelf.** Aan het einde van deze story hoort een concrete uitkomst ("de echo-detectie werkte/werkte niet, met dit bewijs") in de Completion Notes, plus een aanbeveling — de daadwerkelijke AD-11-GO/NO-GO-beslissing is aan Hillebrand, niet aan de implementatie van deze story.

## Acceptance Criteria (experiment-deliverables, geen klassieke feature-AC's)

1. **Given** Evelien (of een testaccount) heeft een gekoppelde huiswerk-Calendar-kleur ingesteld (Story 2.3), **when** een watch-kanaal voor die Calendar geregistreerd wordt, **then** ontvangt Flowz bij een wijziging aan die Calendar een push-notificatie op een nieuw, publiek webhook-endpoint, geverifieerd via `X-Goog-Channel-Token`.
2. **Given** een binnenkomende notificatie, **when** de (nieuwe) Cron-tick 'm binnen het debounce-venster oppikt, **then** wordt `events.list` met de opgeslagen `syncToken` aangeroepen, en voor elk gewijzigd event vergeleken of het Google `updated`-veld overeenkomt met wat Flowz zelf laatst schreef (`homeworkCalendarBlocks.lastKnownUpdated`) — resultaat gaat als een duidelijk herkenbare regel naar CloudWatch, nooit naar `sessions`/`tasks`.
3. **Given** Flowz zelf een huiswerk-event schrijft (via de al-bestaande `syncHomeworkBlocksForDate`-flow, bv. door een taak aan te maken), **when** de volgende Cron-tick die wijziging oppikt, **then** wordt die herkend als "eigen schrijfactie" (geen valse positief) — dit is de kern van wat de spike moet bewijzen.
4. **Given** een testgebruiker verplaatst een huiswerk-event handmatig in Google Calendar, **when** de volgende Cron-tick draait, **then** wordt die wijziging herkend als "externe/handmatige wijziging", onderscheidbaar van punt 3's "eigen schrijfactie"-regel.
5. **Given** een watch-kanaal nadert zijn vervaltermijn (Calendar: max. 7 dagen), **when** de Cron-tick draait, **then** wordt het kanaal vóór het verloopt vervangen door een nieuw kanaal (zelfde `syncToken`-continuïteit).
6. **Given** de spike heeft 24-48 uur live gedraaid, **when** Hillebrand de uitkomst opvraagt, **then** is er een concreet, in de Completion Notes vastgelegd antwoord op: klopte de echo-detectie-aanname, met welk bewijs (CloudWatch-regels/tijdstippen), en wat is de aanbeveling voor AD-11.

[Source: _bmad-output/planning-artifacts/epics.md, regels 951-963 (Epic 8/Story 8.1); _bmad-output/planning-artifacts/research/technical-google-calendar-tweewegs-synchronisatie-voor-huiswerk-blokken-research-2026-09-12.md, secties "Google Calendar API — detectiemechanismen", "Echo/lus-preventie", "Voorgestelde verwerkingspijplijn", "Gefaseerde opbouw" (Fase 1)]

## Belangrijk: technisch ontwerp — lees dit vóór je begint

**1. Twee Google Calendar-mechanismen, niet één, en ze zijn geen alternatieven voor elkaar.** `events.watch` (push-notificatie) levert **nooit de wijziging zelf** — alleen headers (`X-Goog-Resource-State: exists`, `X-Goog-Channel-ID`, geen body). Elke notificatie is dus alleen een trigger om `events.list` met de opgeslagen `syncToken` aan te roepen voor de daadwerkelijke diff. Zie het onderzoek se "Google Calendar API — detectiemechanismen" voor de volledige onderbouwing (met Source-links naar Google's eigen documentatie).

**2. Debounce via de Cron-tick, geen apart AWS-primitief.** Elke binnenkomende notificatie zet alleen `calendarWatchChannels.lastChangeNotifiedAt` (nieuw veld, zie Task 1) op "nu" — de webhook-route zelf doet verder niets, antwoordt meteen 200. De Cron-tick (elke ~2 minuten, zie Task 4) verwerkt een user pas als `lastChangeNotifiedAt` langer dan het debounce-venster (voorstel: 2 minuten) geleden is — dat vangt een snelle reeks wijzigingen (bv. Evelien ruilt twee sessies in twee stappen) op als één verwerkingsronde i.p.v. voortijdig te reageren op de eerste helft.

**3. Echo-detectie is GEEN kant-en-klaar Google-patroon — eigen boekhouding nodig.** Elk Calendar-event heeft een `updated`-veld dat Google bij ELKE schrijfactie bijwerkt, ook Flowz' eigen `updateHomeworkEvent`-aanroepen. Sla dat veld op het moment van Flowz' eigen schrijfactie op (nieuwe kolom `homeworkCalendarBlocks.lastKnownUpdated`, Task 1) — bij de volgende sync-diff: komt het binnenkomende `updated` overeen met de opgeslagen waarde → eigen echo, negeren; wijkt het af → een externe wijziging. Dit vraagt een wijziging aan `server/domain/calendar-sync/homework-events.ts` (`createHomeworkEvent`/`updateHomeworkEvent` geven vandaag alleen `{googleEventId}` terug, niet `updated` — zie Task 2) én aan `server/domain/calendar-sync/homework-blocks.ts`/`server/data/homework-blocks.ts` (die het nieuwe veld moeten opslaan).

**4. De Cron-handler draait BUITEN de Nuxt/Nitro-runtime — `server/data/db.ts`'s `getDb()` werkt daar niet ongewijzigd.** `getDb()` roept `useRuntimeConfig()` aan, een Nuxt-auto-import die niet bestaat in een losse Lambda-handler (`sst.aws.Cron`'s `job`-functie is een eigen, apart gebundelde Lambda, geen Nitro-route). De Cron-handler heeft zijn eigen, kale Turso-clientverbinding nodig (rechtstreeks `TURSO_DATABASE_URL` uit zijn eigen environment lezen, `Resource.TursoAuthToken.value` werkt wel gewoon via SST-linking) — geen `server/data/db.ts` hergebruiken zonder aanpassing. Zelfde aandachtspunt voor `decryptToken`/`encryptToken` (`server/data/crypto.ts`, gebruikt `Resource.TokenEncryptionKey` — dat werkt wél ongewijzigd, want die module gebruikt geen `useRuntimeConfig`).

**5. Geen mooie UI voor kanaal-registratie — één simpele, geauthenticeerde route is genoeg voor een spike.** Geen instellingenpagina-knop bouwen; een nieuwe `POST /api/calendar/homework-watch/register`-route die Evelien/Hillebrand eenmalig zelf aanroept (bv. via een browser-`fetch` in de devtools-console, of gewoon een curl met een geldige sessie-cookie) is voldoende. Bewust minimaal — als AD-11 een GO krijgt, hoort een echte UX-flow bij Epic 8's latere stories, niet bij deze spike.

**6. Beveiliging van het webhook-endpoint.** `X-Goog-Channel-Token`: een zelfgekozen, geheim token dat je meegeeft bij het aanmaken van het watch-kanaal (`events.watch`'s `token`-veld); het endpoint verifieert dat elke binnenkomende notificatie exact dat token in de `X-Goog-Channel-Token`-header heeft, anders 404/403 zonder verdere verwerking. Sla het token op in `calendarWatchChannels` (niet als apart SST-secret — het is per-user/per-kanaal, geen app-breed geheim, AD-5 is hier niet van toepassing). De webhook-route zelf heeft geen gebruikerssessie (Google roept 'm aan, niet Eveliens browser) — authenticatie loopt volledig via dit token.

**7. Registratie gebruikt dezelfde Calendar als waar huiswerk-events al in staan.** `events.watch` registreren op `calendars/primary/events` (zelfde `CALENDAR_EVENTS_URL` als `homework-events.ts` al gebruikt voor schrijven) — niet op de beschikbare-tijd-agenda (die blijft onder AD-4's onvoorwaardelijke pull-only-regel, AD-11 geldt uitsluitend voor de huiswerk-Calendar, zie de architectuur se AD-11-tekst).

## Tasks / Subtasks

- [x] **Task 1: Datamodel** (AC #1, #2, #5)
  - [x] Nieuwe tabel `server/data/schema.ts`: `calendarWatchChannels` — `id` (uuid pk), `userId` (fk `users.id`, unique — één actief kanaal per user voor deze spike), `channelId` (text, het zelf-gekozen kanaal-id dat je aan Google meegeeft), `channelToken` (text, "Belangrijk" punt 6), `resourceId` (text, door Google teruggegeven bij `events.watch`), `syncToken` (text, nullable — leeg tot de eerste volledige sync), `expiresAt` (text, ISO-tijdstip), `lastChangeNotifiedAt` (text, nullable — "Belangrijk" punt 2), `createdAt`/`updatedAt`.
  - [x] `homeworkCalendarBlocks` (bestaande tabel, `server/data/schema.ts:334`): nieuwe kolom `lastKnownUpdated` (text, nullable) — "Belangrijk" punt 3.
  - [x] `npx sst shell -- npx drizzle-kit generate` (`0025_tricky_slyde.sql`) + `npx sst shell -- npx drizzle-kit migrate` — beide geslaagd tegen de dev-database.

- [x] **Task 2: `updated`-veld vastleggen bij Flowz' eigen schrijfacties** (AC #3, "Belangrijk" punt 3)
  - [x] `server/domain/calendar-sync/homework-events.ts`: `createHomeworkEvent`/`updateHomeworkEvent` lezen Google's respons-`updated`-veld uit en geven die mee terug (`CreateHomeworkEventResult.updated`, nieuwe `UpdateHomeworkEventResult` i.p.v. `void`).
  - [x] `server/data/homework-blocks.ts`/`server/domain/calendar-sync/homework-blocks.ts`: `insertHomeworkBlock`/`updateHomeworkBlockTimes` krijgen een optionele `lastKnownUpdated`-parameter, `syncHomeworkBlocksForDate` geeft `result.updated` door bij beide paden.

- [x] **Task 3: Kanaal-registratie + webhook-ontvangroute** (AC #1)
  - [x] Nieuwe `server/api/calendar/homework-watch/register.post.ts`: auth-gated, roept Google's `POST https://www.googleapis.com/calendar/v3/calendars/primary/events/watch` aan (`type: "web_hook"`, `address` = het publieke webhook-endpoint hieronder, zelf-gegenereerd `channelId`/`channelToken`), slaat het resultaat (`resourceId`, `expiration`) op in `calendarWatchChannels`. Bewust geen UI-knop ("Belangrijk" punt 5).
  - [x] Nieuwe `server/api/calendar/homework-watch/notifications.post.ts`: **geen sessie-auth** (Google roept dit aan) — valideert `X-Goog-Channel-Token` tegen de opgeslagen `channelToken` voor het `channelId` in `X-Goog-Channel-ID`; bij een match: `lastChangeNotifiedAt` op "nu" zetten voor die user, antwoord 200. Geen match / onbekend kanaal: 404, geen verdere actie ("Belangrijk" punt 6).

- [x] **Task 4: Cron-tick — kanaal-hernieuwing + syncToken-diff + detectie-logging** (AC #2, #3, #4, #5)
  - [x] `sst.config.ts`: nieuw `sst.aws.Cron("CalendarWatchTick", { schedule: "rate(2 minutes)", function: { handler: "server/cron/calendar-watch-tick.handler", link: [...], environment: {...} } })`. Afwijking t.o.v. de oorspronkelijke schets: `function` i.p.v. `job` (SST's huidige API — `job` is deprecated t.g.v. `function`, zelfde vorm). Ook alle drie de secrets gelinkt (niet alleen `tursoAuthToken`/`tokenEncryptionKey`, ook `googleOAuthClientSecret` — nodig voor `refreshCalendarAccessToken`), plus dezelfde `NUXT_TURSO_DATABASE_URL`/`NUXT_OAUTH_OIDC_CLIENT_ID`/`NUXT_PUBLIC_SITE_URL`-env vars als de Nuxt-component: de portability-fix (zie hieronder) laat de handler `server/data/db.ts`/`server/domain/auth/calendar-token.ts` ONGEWIJZIGD hergebruiken zolang die env vars ook hier staan — geen aparte, dubbele DB-/auth-laag nodig zoals "Belangrijk" punt 4 vreesde.
  - [x] Portability-fix (voorwaarde voor bovenstaande hergebruik): `server/data/db.ts`'s `getDb()` en `server/domain/auth/calendar-token.ts`'s `refreshCalendarAccessToken` lazen `useRuntimeConfig()` (Nuxt-only, crasht buiten Nitro) — nu rechtstreeks `process.env.NUXT_TURSO_DATABASE_URL`/`process.env.NUXT_OAUTH_OIDC_CLIENT_ID` (met een expliciete fout bij ontbreken), gedragsgelijk binnen de bestaande Nuxt-Lambda (Nuxt populeert die runtimeConfig-sleutels toch al uit exact deze env vars) en nu ook bruikbaar vanuit de Cron-Lambda. `npm run typecheck` (incl. de volledige bestaande Nuxt-app) blijft groen.
  - [x] Nieuw `server/data/calendar-watch-channels.ts` (datalaag, analoog aan `homework-blocks.ts`): `getWatchChannelForUser`, `getWatchChannelByChannelId`, `getAllWatchChannels`, `upsertWatchChannel` (met optionele `syncToken` — alleen gezet bij een verse initiële volledige sync, zie register-route), `markChannelChangeNotified`, `renewWatchChannel`, `completeSyncPass`.
  - [x] `server/data/homework-blocks.ts`: nieuwe `getHomeworkBlockByGoogleEventId` — voor de echo-vergelijking in de Cron-handler (een gewijzigd Calendar-event heeft alleen een `googleEventId`, niet Flowz' interne rij-`id`).
  - [x] `server/domain/calendar-sync/homework-events.ts`: `calendarRequestMetVerversing`/`CALENDAR_EVENTS_URL` geëxporteerd — de registratieroute en de Cron-handler hergebruiken hetzelfde token-refresh-patroon voor hun eigen `watch`/`list`-aanroepen, i.p.v. dat te dupliceren.
  - [x] Nieuw `server/cron/calendar-watch-tick.ts` (`handler`-export): (a) elk kanaal met `expiresAt` binnen 24u: hernieuwen (nieuwe `events.watch`-aanroep met een nieuw `channelId`/`channelToken` — Google staat geen hergebruik van een kanaal-id toe —, kanaal-rij bijwerken, `syncToken` behouden); (b) elke user met `lastChangeNotifiedAt` ouder dan het debounce-venster (2 min): `events.list` met de opgeslagen `syncToken` aanroepen (gepagineerd via `nextPageToken`), per gewijzigd event de `updated`-vergelijking uit "Belangrijk" punt 3 doen (`ECHO`/`EXTERNAL-CHANGE`/`DELETED` grep-bare regels), nieuwe `syncToken` opslaan, `lastChangeNotifiedAt` terug op `null`. Een `410`/foutrespons op de sync-call (verlopen `syncToken`) reset de token naar `null` i.p.v. te crashen — de eerstvolgende tick bouwt 'm dan opnieuw op (geaccepteerd spike-compromis: eenmalige valse `EXTERNAL-CHANGE`-golf mogelijk, expliciet benoemd in Completion Notes).
  - [x] Registratieroute (Task 3) haalt bij registratie ook meteen een initiële `syncToken` op via een volledige `events.list`-doorloop (nieuwe `fetchInitialSyncToken`) — nodig omdat `events.watch` zelf geen `syncToken` teruggeeft, en zonder deze stap zou de eerste Cron-tick na registratie alle al-bestaande huiswerk-events onterecht als externe wijziging loggen.
  - [x] Nooit een sessie/taak aanpassen vanuit deze handler — puur logging (herhaling van de scope-grens hierboven, dit is de belangrijkste regel van de hele story). Geverifieerd: de handler importeert uitsluitend `data/users`, `data/homework-blocks` (alleen-lezen `getHomeworkBlockByGoogleEventId`), `data/calendar-watch-channels` en `domain/calendar-sync/homework-events` — geen enkele `sessions`/`tasks`-schrijfoperatie.

- [x] **Task 5: Live-experiment**
  - [x] Deploy naar de dev-stage. Registreer een kanaal voor een testaccount (via Task 3's route). Bug gevonden en gefixt tijdens deze task: de globale sessie-middleware (`server/middleware/session.ts`) blokkeerde de webhook-route met een 401 vóórdat Google's notificatie de route kon bereiken — `X-Goog-Channel-Token`-authenticatie loopt buiten de sessie om, dus die ene route moest expliciet aan `PUBLIC_PREFIXES` toegevoegd worden. Zonder deze fix zou de hele spike stil hebben gefaald (geen enkele notificatie zou ooit aankomen). Na de fix opnieuw gedeployed en geverifieerd met een test-curl (401 → 404).
  - [x] Trigger een "eigen schrijfactie" (testtaak aangemaakt met een huiswerk-kleur ingesteld) — CloudWatch (Cron-tick 09:40:35 UTC) logde `ECHO user=... googleEventId=il68qavk16tu7frsrbf1hcfahg updated=2026-09-13T09:24:10.886Z` — geen valse `EXTERNAL-CHANGE`.
  - [x] Het bijbehorende huiswerk-event handmatig verplaatst in Google Calendar — dezelfde Cron-tick (09:40:35 UTC) logde `EXTERNAL-CHANGE user=... googleEventId=a4gclploln188cru2i4atq3pj0 updated=2026-09-13T09:38:01.351Z lastKnownUpdated=2026-09-13T09:24:11.370Z` — het `updated`-tijdstip springt exact op het moment van de handmatige verplaatsing, `lastKnownUpdated` blijft de aanmaak-tijd. Correct onderscheiden van de ECHO hierboven.
  - [x] Kanaal-hernieuwing (Task 4a) geverifieerd zonder de volle 24-48u af te wachten (in overleg met Hillebrand): `expires_at` van het testkanaal kunstmatig binnen het 24u-hernieuwingsvenster gezet (~12u vooruit i.p.v. 7 dagen). Eerstvolgende tick (09:44:35 UTC) logde `RENEWED user=... channelId=4b1b7676-f72f-4dbc-9ccd-2e016a3d2ec9`. Database-verificatie erna: nieuw `channelId`, `resourceId` ongewijzigd (`P7N3la84yDTBI4S-a5uNUXT7f0c`), `expires_at` weer 7 dagen vooruit (2026-09-20T09:44:35Z), **`sync_token` exact ongewijzigd** — de continuïteits-eis van AC #5 is aangetoond. Bijvangst: Google stuurt bij een nieuw kanaal automatisch een initiële "sync"-notificatie (`last_change_notified_at` kreeg meteen weer een waarde) — geen bug, inherent aan het protocol, opgevangen door de bestaande debounce/diff-logica.

- [x] **Task 6: Rapportage** (AC #6)
  - [x] Uitkomst + concreet bewijs (CloudWatch-tijdstempels/regels) + aanbeveling voor AD-11 vastgelegd in de Completion Notes hieronder — geen eigen GO/NO-GO-besluit, dat is aan Hillebrand.

### Review Findings

Code review 2026-09-13 (Blind Hunter + Edge Case Hunter + Verification Gap Reviewer + Acceptance Auditor, allemaal op Opus 5, alle vier geslaagd). Diff-scope: `0302814..HEAD` (incl. nog niet gecommitte wijzigingen), gescoped op de 13 bestanden uit de story se File List.

**Decision — opgelost door Hillebrand op 2026-09-13.**

- [x] [Review][Decision] **Besluit: optie a — bouw een kleine teardown-route.** Nieuwe `DELETE /api/calendar/homework-watch/register`-route die `channels.stop` aanroept (Google) en de rij verwijdert, in plaats van dit later handmatig te doen. Opgenomen in de Patch-lijst hieronder.

**Patch** — allemaal toegepast (`npm run typecheck` + `npx nuxt build` groen na alle wijzigingen):

- [x] [Review][Patch] **Oud watch-kanaal wordt nooit gestopt bij hernieuwing of herregistratie** — `renewChannelIfNeeded` en `register.post.ts` maken een nieuw Google-kanaal zonder het oude via `channels.stop` te beëindigen; het oude kanaal blijft tot zijn eigen vervaltermijn (max. 7 dagen) notificaties sturen die de webhook dan als `NOTIFICATION-REJECTED` (404) afwijst. **Al live bevestigd**: onze eigen Task 5-hernieuwingstest liet precies dit orphan-kanaal achter (oude channelId `50bc31c8-...`, actief tot 2026-09-20) [server/cron/calendar-watch-tick.ts:49-81, server/api/calendar/homework-watch/register.post.ts:65-91] — **fix:** nieuwe `stopWatchChannel`-helper (Google `POST /calendar/v3/channels/stop`), aanroepen vóór het overschrijven van een bestaand kanaal in beide functies; maakt de tot nu toe ongebruikte `getWatchChannelForUser` (dode code) alsnog nuttig in `register.post.ts`.
- [x] [Review][Patch] **`!response.ok` behandelt élke foutrespons als een verlopen syncToken, niet alleen 410 Gone** — `processDebouncedChannel` gooit bij een gewone transiënte 429/500/503 óók de nog geldige `syncToken` weg (en daarmee `lastChangeNotifiedAt`), terwijl de eigen code-comment expliciet alleen 410 als reden noemt. Dit ondermijnt AC #5's continuïteits-eis bij elke gewone Google-hik, niet alleen bij een echt verlopen token, en kan een valse EXTERNAL-CHANGE-golf veroorzaken die precies de AC #3/#4-meting vertroebelt [server/cron/calendar-watch-tick.ts:126-134] — **fix:** reset alleen bij `response.status === 410`; bij andere foutcodes loggen en de rij ongemoeid laten (volgende tick probeert opnieuw met dezelfde token).
- [x] [Review][Patch] **`completeSyncPass` wist `lastChangeNotifiedAt` onvoorwaardelijk** — een notificatie die binnenkomt terwijl de (mogelijk meerdere pagina's tellende) sync-pass nog loopt wordt zo stilzwijgend genegeerd; die externe wijziging wordt pas gediff't bij een latere, ongerelateerde notificatie [server/data/calendar-watch-channels.ts:89-94, server/cron/calendar-watch-tick.ts:144] — **fix:** `lastChangeNotifiedAt` vastleggen bij de start van de pass en alleen wissen als de waarde sindsdien niet is gewijzigd (compare-and-swap in de UPDATE-clausule).
- [x] [Review][Patch] **Registratie kan een niet-opgeslagen, onstopbare Google-kanaal achterlaten** — `events.watch` slaagt vóór `fetchInitialSyncToken` wordt aangeroepen; gooit die laatste een fout (elke non-OK respons), dan stopt de functie zonder ooit `upsertWatchChannel` aan te roepen — het kanaal bestaat dan bij Google maar nergens in de database (dus ook niet meer te stoppen via de fix hierboven, want `resourceId`/`channelId` staan nergens opgeslagen) [server/api/calendar/homework-watch/register.post.ts:65-91] — **fix:** `upsertWatchChannel` (met tijdelijk lege `syncToken`) direct na de succesvolle `/watch`-aanroep, `fetchInitialSyncToken` daarna los bijwerken; of wrap in try/catch met een `stopWatchChannel`-aanroep bij een fout.
- [x] [Review][Patch] **`DELETED` wordt gelogd voor élk geannuleerd Calendar-event, ook niet-huiswerk-afspraken** — de `status === 'cancelled'`-check gebeurt vóór de `getHomeworkBlockByGoogleEventId`-lookup, dus een verwijderde tandartsafspraak logt evengoed `DELETED` — dat vertroebelt precies het bewijsmateriaal dat AC #6 nodig heeft. Omgekeerd geldt: Flowz' eigen verwijderingen (`syncHomeworkBlocksForDate` verwijdert de DB-rij al vóór de volgende tick) zijn na deze fix ook meteen correct onderscheidbaar — geen blok gevonden betekent dan "niet van ons", een wél-gevonden blok bij een cancelled event is een echte externe verwijdering [server/cron/calendar-watch-tick.ts:86-90] — **fix:** lookup vóór de cancelled-check, alleen loggen als er een blok gevonden is.
- [x] [Review][Patch] **`updateHomeworkEvent`/`createHomeworkEvent` parsen nu de response-body zonder vangnet** — dit raakt het BESTAANDE productie-write-sync-pad (niet alleen de spike): als Google ooit een 2xx met een leeg/onparseerbaar lichaam teruggeeft, gooit `.json()` ná de succesvolle Calendar-mutatie, waardoor `updateHomeworkBlockTimes` nooit bereikt wordt — Calendar en `homeworkCalendarBlocks` raken dan stil uiteen. Inconsistent met dit bestand se eigen conventie (`foutBericht`'s `.catch()` voor exact deze foutklasse, maar dan alleen op het foutpad) [server/domain/calendar-sync/homework-events.ts:126,148 e.v.] — **fix:** `.json().catch(() => null)`, `null`/ontbrekend `updated`-veld defensief afhandelen.
- [x] [Review][Patch] **`getHomeworkBlockByGoogleEventId` filtert niet op `userId`** — onschadelijk met de huidige ene actieve gebruiker, maar bakt een single-tenant-aanname in terwijl de PRD expliciet vereist dat de architectuur later geen redesign mag nodig hebben voor multi-user [server/data/homework-blocks.ts:41-44] — **fix:** `userId`-parameter + filter toevoegen, zelfde patroon als `getHomeworkBlocksForDate` in hetzelfde bestand.
- [x] [Review][Patch] **Paginering verliest `syncToken` op pagina 2+** — `processDebouncedChannel`'s `if (pageToken) ... else if (channel.syncToken) ...` stuurt bij een vervolgpagina alleen `pageToken`, terwijl Google's incrementele-sync-contract vereist dat overige parameters gelijk blijven — nu onwaarschijnlijk (kleine agenda), maar een latente correctheidsbug [server/cron/calendar-watch-tick.ts:119-123] — **fix:** `syncToken` altijd meesturen zodra hij bekend is, `pageToken` er los aan toevoegen.
- [x] [Review][Patch] **De initiële `sync`-handshake-notificatie wordt als een echte wijziging behandeld** — elke registratie/hernieuwing triggert Google's automatische `X-Goog-Resource-State: sync`-notificatie (live bevestigd tijdens onze eigen Task 5-hernieuwingstest), die nu een volledige, nutteloze diff-pass in gang zet [server/api/calendar/homework-watch/notifications.post.ts:9-15] — **fix:** bij `resourceState === 'sync'` direct 200 teruggeven zonder `markChannelChangeNotified` aan te roepen.
- [x] [Review][Patch] **Geen `Number.isFinite`-check op Google's `expiration`-veld** — bij een ontbrekende/niet-numerieke waarde gooit `new Date(Number(expiration)).toISOString()` een `RangeError`, ná het moment dat het Google-kanaal al is aangemaakt (en dus, tot de teardown-fix hierboven, wéér een orphan) [server/api/calendar/homework-watch/register.post.ts:89, server/cron/calendar-watch-tick.ts:79] — **fix:** `Number.isFinite`-check met een duidelijke foutmelding.
- [x] [Review][Patch] **`fetchInitialSyncToken` kan stilzwijgend `undefined` teruggeven** — bij een verse registratie wordt dat dan `NULL` in de database, wat precies de valse "alles is externe wijziging"-golf veroorzaakt die deze functie moest voorkomen — zonder enige foutmelding die daar op wijst [server/api/calendar/homework-watch/register.post.ts:28-44] — **fix:** gooi een duidelijke fout als `syncToken` na de volledige paginering nog `undefined` is.
- [x] [Review][Patch] **Webhook-hardening**: de ruwe `X-Goog-Channel-ID`/`-Token`-headerwaarden worden ongesaneerd gelogd vóórdat enige validatie plaatsvindt (onauthenticeerd endpoint — log-injectierisico), en de token-vergelijking (`!==`) is niet constant-time [server/api/calendar/homework-watch/notifications.post.ts:10-22] — **fix:** headerwaarde afkappen/saneren vóór loggen, `crypto.timingSafeEqual` voor de tokenvergelijking.
- [x] [Review][Patch] **Ongebruikte `CALENDAR_EVENTS_URL`-import** in `server/cron/calendar-watch-tick.ts` — het bestand roept uitsluitend `calendarRequestMetVerversing` aan, die de URL zelf opbouwt; de export in `homework-events.ts` (met commentaar dat beide nieuwe bestanden 'm zouden gebruiken) is dus deels onjuist [server/cron/calendar-watch-tick.ts:9] — **fix:** ongebruikte import verwijderen.
- [x] [Review][Patch] **Teardown-route voor het beëindigen van de spike** (uit de Decision hierboven, optie a) — nieuwe `DELETE /api/calendar/homework-watch/register`-route: haalt het kanaal van de ingelogde user op (`getWatchChannelForUser`), roept `stopWatchChannel` aan (dezelfde helper als de renewal-fix hierboven), verwijdert de rij. Auth-gated, zelfde patroon als de bestaande registratieroute.

**Defer** — reëel, maar niet nu actionable:

- [x] [Review][Defer] Het nieuwe `sst.aws.Cron`-component is (net als het bestaande `FlowzWeb`-component) niet stage-gated en hardcodet `NUXT_PUBLIC_SITE_URL` [sst.config.ts:71-82] — deferred: bestaand, project-breed patroon (single-stage-only-workflow), niet nieuw geïntroduceerd door deze diff; wordt alleen een echt risico als er ooit een tweede deploy-stage met dezelfde productie-secrets gebruikt wordt.
- [x] [Review][Defer] AC #3's bewijs (`ECHO googleEventId=il68qavk16tu7frsrbf1hcfahg`) en AC #4's bewijs (`EXTERNAL-CHANGE googleEventId=a4gclploln188cru2i4atq3pj0`) betreffen twee verschillende Calendar-events i.p.v. één event dat beide takken doorloopt — het mechanisme is aangetoond, maar een A/B-test op precies hetzelfde event (eerst met rust laten → ECHO, dan verplaatsen → EXTERNAL-CHANGE) zou het bewijs sluitend maken — deferred: geen codewijziging, vereist nog een korte live testronde; niet blokkerend voor de huidige aanbeveling.

**Rejected:**

- `false` — "Cron-handler schrijft buiten `calendarWatchChannels` (via token-refresh naar de `users`-tabel), in tegenspraak met de mutatie-ownership-claim": geverifieerd — de story se eigen scope-grens noemt expliciet alleen `sessions`/`tasks`, niet "geen enkele andere tabel"; `updateCalendarAccessToken` is een blinde overschrijving (geen read-modify-write), dus geen dataverlies-risico bij een gelijktijdige refresh vanuit de Nuxt-Lambda.
- `false` — "AC #6's '24-48 uur live gedraaid' is niet gehaald": de versnelde test was een expliciete, in dit gesprek vastgelegde keuze van Hillebrand ("Versnellen (aanbevolen)"), geen verborgen afwijking — al zo gedocumenteerd in de Change Log/Completion Notes.
- `false` — "portability-fix (`process.env.NUXT_TURSO_DATABASE_URL`/`NUXT_OAUTH_OIDC_CLIENT_ID`) is fragiel voor toekomstige config-wijzigingen": het huidige, gedeployde gedrag is correct en al fail-loud (via `sst.config.ts`'s `requireEnv` op de onderliggende env vars) — het genoemde risico is speculatief over een hypothetische toekomstige refactor, geen bestaand defect.
- `false` — "de gereviewde diff is incompleet voor de migratie (journal/snapshot ontbreken)": artefact van hoe de diff voor deze review is gescoped (exact de story se File List), geen codedefect — de migratie-bestanden staan wel degelijk correct in de working tree/repo.
- `low`, niet de moeite waard — "verlopen access-token wordt per pagina/aanroep opnieuw ververst i.p.v. teruggegeven aan de aanroeper" (dubbele Google-calls na tokenverval binnen één tick): op deze schaal (1-2 gebruikers, korte ticks) negeerbare kostenimpact, en de fix vereist een grotere refactor van `calendarRequestMetVerversing`'s aanroeppatroon dan het probleem rechtvaardigt.
- `low`, niet de moeite waard — "overlappende Cron-runs bij een pass die langer dan 2 minuten duurt" (geen lease/lock): elke geobserveerde tick duurde 130-400ms, ruim onder de 2 minuten-schema — een lease/lock-mechanisme is een disproportionele toevoeging voor een risico dat zich bij de huidige schaal niet voordoet.
- `low`, niet de moeite waard — "geen geautomatiseerde tests / geen operator-runbook": dit project heeft nergens een testframework (vaste conventie, geen omissie van deze story); een runbook zou spec-inhoud zijn (valt onder "fix is het bewerken van de spec onder review").

## Dev Notes

### Architectuurcompliance

- **AD-11 (PROPOSED)** — deze story ÍS de spike die AD-11's Rule beschrijft: uitsluitend voor de huiswerk-Calendar, één Cron-component, geen uitbreiding naar de beschikbare-tijd-agenda. Status blijft PROPOSED totdat Hillebrand na Task 6 een expliciet GO geeft.
- **AD-4/AD-7 (pull-only/synchroon, "geen achtergrondtaken")** — deze story bouwt de EERSTE, bewust afgebakende uitzondering (via AD-11). Alle overige Calendar-toegang in dit project (de beschikbare-tijd-agenda, alle bestaande schrijfacties) blijft ONVERANDERD onder AD-4/AD-7's onvoorwaardelijke regel — deze story raakt die code niet.
- **AD-5 (secrets nooit in de repo)** — `channelToken` is geen app-secret (per-user/per-kanaal, opgeslagen in de DB, niet via `sst.Secret`) — dat is bewust, geen AD-5-schending.
- **Mutatie-ownership** — de webhook-route/Cron-handler muteren alleen hun eigen nieuwe tabellen (`calendarWatchChannels`), nooit `sessions`/`tasks` — dat is de kern-scope-grens van deze story, geen architectuurregel die al bestond, maar wel een die hier voor het eerst expliciet is.

### Bestaande code die deze story raakt (lezen vóór implementatie)

- `server/domain/calendar-sync/homework-events.ts` — `createHomeworkEvent`/`updateHomeworkEvent`/`calendarRequestMetVerversing` (token-refresh-patroon, hergebruiken voor de Cron-handler se eigen Calendar-calls). `CALENDAR_EVENTS_URL` is de basis-URL voor zowel schrijven (bestaand) als `watch`/`list` (nieuw).
- `server/domain/calendar-sync/homework-blocks.ts` + `server/data/homework-blocks.ts` — `syncHomeworkBlocksForDate`, `insertHomeworkBlock`, `updateHomeworkBlockTimes` — Task 2 raakt deze, verandert hun bestaande gedrag niet, voegt alleen het nieuwe veld toe.
- `server/domain/auth/calendar-token.ts` — `refreshCalendarAccessToken`, nodig in de Cron-handler (die geen sessie heeft, dus niet via `calendarRequestMetVerversing`'s bestaande user-sessie-pad, maar rechtstreeks met een opgehaalde+ontsleutelde `User`-rij).
- `server/data/crypto.ts` — `decryptToken` voor de Cron-handler se eigen Calendar-token-ontsleuteling (werkt ongewijzigd buiten Nitro, zie "Belangrijk" punt 4).
- `server/data/db.ts` — **niet rechtstreeks hergebruiken vanuit de Cron-handler** (zie "Belangrijk" punt 4) — wel als referentie voor hoe de Turso-clientverbinding eruitziet.
- `sst.config.ts` — huidige stack heeft alleen `sst.aws.Nuxt`; dit is de eerste toevoeging van een tweede component.
- `server/data/schema.ts:334` — `homeworkCalendarBlocks`, de tabel die Task 2 uitbreidt.

### Project Structure Notes

- Nieuwe map `server/cron/` voor de Cron-handler — bestaat nog niet, geen bestaande conventie om van af te wijken.
- `server/api/calendar/homework-watch/` — nieuwe submap, consistent met de bestaande `server/api/calendar/`-structuur (zie `homework-events`'s aanroepers).

### References

- [Source: _bmad-output/planning-artifacts/epics.md] — regels 951-963 (Epic 8, Story 8.1)
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-09-12.md] — volledige rationale, Epic 8/AD-11
- [Source: _bmad-output/planning-artifacts/architecture/architecture-Flowz-2026-07-14/ARCHITECTURE-SPINE.md] — AD-4, AD-7, AD-11 (PROPOSED)
- [Source: _bmad-output/planning-artifacts/research/technical-google-calendar-tweewegs-synchronisatie-voor-huiswerk-blokken-research-2026-09-12.md] — volledige technische onderbouwing; sectie "Google Calendar API — detectiemechanismen" (met Google-eigen-documentatie-Source-links), "Echo/lus-preventie", "Voorgestelde verwerkingspijplijn", "Debounce/coalescing", "Gefaseerde opbouw" (Fase 1 = deze story)
- [Source: server/domain/calendar-sync/homework-events.ts, homework-blocks.ts] — bestaande Calendar-write-patronen die Task 2/3/4 hergebruiken/uitbreiden

## Open Questions

- Exact debounce-venster (2 minuten, "Belangrijk" punt 2) is een beargumenteerd voorstel uit het onderzoek, geen vastgesteld cijfer — door de implementatie tijdens Task 5's live-experiment eventueel bij te stellen.
- Hoe de Cron-handler precies aan zijn eigen `TURSO_DATABASE_URL` komt (apart environment-var op het Cron-component in `sst.config.ts`, of een andere route) is niet vooraf uitgewerkt — "Belangrijk" punt 4 signaleert het probleem, de exacte oplossing is aan de implementatie.
- Geen beslissing over wat er met de gelogde detectie-resultaten gebeurt ná deze spike (bewaren voor Story 8.2's beslisregels, of weggooien zodra Hillebrand de uitkomst gezien heeft) — puur CloudWatch-logs volstaan voor déze story se doel (AC #6).

## Change Log

| Datum | Wijziging |
| --- | --- |
| 2026-09-12 | Story aangemaakt via create-story, voortbouwend op sprint-change-proposal-2026-09-12.md/het technisch onderzoek. Epic 8 gemarkeerd `in-progress` (eerste story). Status meteen `ready-for-dev` — expliciet een experiment-story, geen normale feature (zie de eigen waarschuwing boven de AC's). |
| 2026-09-12 | Tasks 1-4 geïmplementeerd (datamodel, `updated`-veld-vastlegging, registratie-/webhook-routes, Cron-tick). Portability-fix aan `server/data/db.ts`/`server/domain/auth/calendar-token.ts` toegevoegd (niet in de oorspronkelijke tasklijst, wel al aangekondigd in "Belangrijk" punt 4) zodat de Cron-handler de bestaande datalaag ongewijzigd hergebruikt. `npm run typecheck` groen na elke task. HALT vóór Task 5: vereist een `sst deploy` (infrastructuur-wijziging: eerste `sst.aws.Cron`-component) en live Google-account-toegang die de dev-agent niet heeft — beide zijn acties die expliciete goedkeuring/uitvoering door Hillebrand vereisen. Status blijft `in-progress`. |
| 2026-09-13 | Deploy naar `dev` uitgevoerd (Hillebrand se goedkeuring). Bug gevonden en gefixt tijdens Task 5: `server/middleware/session.ts` blokkeerde de webhook met een 401 (de sessie-middleware gold onterecht ook voor een route die per ontwerp geen sessie heeft) — opgelost door de route expliciet publiek te maken, opnieuw gedeployed. Live-experiment (Task 5) volledig uitgevoerd en geslaagd: `ECHO`/`EXTERNAL-CHANGE` correct onderscheiden op echte Calendar-data, kanaal-hernieuwing geverifieerd (met een versnelde test — `expires_at` kunstmatig vervroegd i.p.v. 24-48u natuurlijk te wachten, in overleg met Hillebrand) inclusief behouden `sync_token`-continuïteit. Task 6 (rapportage) afgerond in de Completion Notes. Status → `review`. |
| 2026-09-13 | Code review (Opus 5, 4 lagen): 1 decision (opgelost — optie a, teardown-route) + 14 patches, allemaal geïmplementeerd; 2 defers, 7 rejected. Belangrijkste fixes: `stopWatchChannel`-helper + nieuwe `DELETE`-teardown-route, 410-only syncToken-reset (was `!response.ok`), compare-and-swap op `lastChangeNotifiedAt`, `DELETED`-scope-fix, defensieve response-parsing in het bestaande productie-write-sync-pad. `npm run typecheck` + `npx nuxt build` groen. Opnieuw gedeployed naar `dev`; Hillebrand heeft daarna zelf de nieuwe teardown-route aangeroepen — het orphan-testkanaal (channelId `50bc31c8-...`) is bevestigd gestopt bij Google en de rij is verwijderd (database-check: `calendar_watch_channels` leeg), geen foutregels in CloudWatch. Status → `done`. |
| 2026-09-13 | **AD-11 GO.** Hillebrand heeft op basis van deze story se uitkomst + code review expliciet GO gegeven op AD-11 — status in `ARCHITECTURE-SPINE.md` gewijzigd van `PROPOSED` naar `ADOPTED`. Epic 8 se gate is opgeheven in `epics.md`/`sprint-status.yaml`; Story 8.2/8.3 mogen nu opgepakt worden (nog niet uitgeschreven als story-bestand). |

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5

### Debug Log References

### Completion Notes List

**Uitkomst: de echo-detectie-aanname klopt. Alle kern-AC's (#1-#5) zijn aangetoond met bewijs tegen de échte Google Calendar API, niet alleen tegen typecheck/code-inspectie.**

**Bewijs (CloudWatch, alle tijden UTC, `/aws/lambda/flowz-dev-CalendarWatchTickHandlerFunction-*` resp. `/aws/lambda/flowz-dev-FlowzWebServerEuwest1Function-*`):**

- **AC #1 (kanaal-registratie + notificatie-ontvangst)**: kanaal geregistreerd 09:20:41; na de sessie-middleware-fix (zie hieronder) ontving de webhook om 09:38:03 een geldige notificatie: `NOTIFICATION-RECEIVED channelId=50bc31c8-... resourceState=exists` → `NOTIFICATION-ACCEPTED user=d31b6e17-...`.
- **AC #2/#3 (eigen schrijfactie = ECHO)**: testtaak aangemaakt (huiswerk-kleur ingesteld) → nieuw blok geschreven met `lastKnownUpdated` vastgelegd (09:24:11.370Z). Cron-tick 09:40:35 logde `ECHO user=... googleEventId=il68qavk16tu7frsrbf1hcfahg updated=2026-09-13T09:24:10.886Z` voor een ander, gelijktijdig herschreven blok — geen valse `EXTERNAL-CHANGE`, exact het verwachte gedrag.
- **AC #4 (handmatige wijziging = EXTERNAL-CHANGE, onderscheidbaar van #3)**: hetzelfde testtaak-event handmatig verplaatst in Google Calendar. Dezelfde tick (09:40:35) logde `EXTERNAL-CHANGE user=... googleEventId=a4gclploln188cru2i4atq3pj0 updated=2026-09-13T09:38:01.351Z lastKnownUpdated=2026-09-13T09:24:11.370Z` — Google's `updated`-veld sprong exact naar het moment van de handmatige actie, en week af van de opgeslagen `lastKnownUpdated`. Duidelijk te onderscheiden van de ECHO hierboven (ander event, ander patroon).
- **AC #5 (kanaal-hernieuwing zonder gat in dekking)**: i.p.v. de volle 24-48u af te wachten is dit versneld getest (expliciete keuze van Hillebrand, zie Change Log) door `expires_at` kunstmatig binnen het 24u-hernieuwingsvenster te zetten. Cron-tick 09:44:35 logde `RENEWED user=... channelId=4b1b7676-f72f-4dbc-9ccd-2e016a3d2ec9`. Database-verificatie erna: nieuw `channelId`, `resourceId` ongewijzigd, `expires_at` weer 7 dagen vooruit, **`sync_token` exact ongewijzigd behouden** — dit laatste is de kernvereiste van AC #5 (geen dubbele volledige resync nodig na een hernieuwing). Bijvangst, geen bug: Google stuurt bij een nieuw kanaal automatisch een initiële sync-notificatie, opgevangen door de bestaande debounce-logica zonder aanpassing.
- **AC #6 (rapportage)**: dit blok, plus de aanbeveling hieronder.

**Onderweg gevonden en gefixt bug (niet in de oorspronkelijke tasklijst)**: de globale sessie-middleware (`server/middleware/session.ts`) blokkeerde de webhook-route met een 401 vóórdat Google's notificatie ooit de routelogica kon bereiken — de route authenticeert bewust via `X-Goog-Channel-Token`, niet via een Flowz-sessiecookie (die bestaat sowieso niet voor een server-naar-server-aanroep van Google). Zonder deze fix zou de hele spike stil hebben gefaald: geen enkele notificatie zou ooit zijn aangekomen, en er was geen foutmelding die daar direct naar wees (de Cron-tick zelf bleef foutloos draaien, want die wist niet dat er notificaties werden geweigerd). Gefixt door de route expliciet aan `PUBLIC_PREFIXES` toe te voegen; geverifieerd met een test-curl (401 → 404) vóór het echte experiment.

**Aanbeveling voor AD-11 (geen eigen besluit — dat is aan Hillebrand)**: de technische aanname achter AD-11 (echo-detectie via `updated`-veld-vergelijking is betrouwbaar te onderscheiden van externe wijzigingen) is bevestigd met échte Calendar-data, inclusief de kanaal-hernieuwing zonder sync-continuïteitsverlies. Er is geen technische blocker gevonden voor het bouwen van Story 8.2 (mutatie: een handmatige verplaatsing daadwerkelijk overnemen in `sessions`). Twee aandachtspunten voor die vervolgstory, geen van beide een showstopper: (1) de kunstmatige-versnelling-test dekt de hernieuwing zelf, maar niet een dagenlange ononderbroken run — een eventuele edge case die alleen bij langdurig draaien optreedt (bv. Google-quota, tijdelijke netwerkstoringen die de renewal-tick net missen) is met deze spike niet uitgesloten; (2) de "eigen sync-notificatie na kanaal-hernieuwing"-bijvangst betekent dat elke hernieuwing een extra, grotendeels overbodige diff-ronde triggert — functioneel onschadelijk (alles logt als ECHO), maar de moeite waard om in 8.2 evt. te dempen als dat relevant blijkt voor de rate-limits.

### File List

- `server/data/schema.ts` (Task 1, eerdere sessie: `calendarWatchChannels`-tabel, `homeworkCalendarBlocks.lastKnownUpdated`)
- `server/data/migrations/0025_tricky_slyde.sql` (Task 1, eerdere sessie)
- `server/domain/calendar-sync/homework-events.ts` (Task 2 eerdere sessie: `updated`-veld; Task 4: `calendarRequestMetVerversing`/`CALENDAR_EVENTS_URL` geëxporteerd; code review: nieuwe `stopWatchChannel`-helper, defensieve `.json()`-afhandeling in `createHomeworkEvent`/`updateHomeworkEvent`)
- `server/data/homework-blocks.ts` (Task 2 eerdere sessie: `lastKnownUpdated`-parameter; Task 4: `getHomeworkBlockByGoogleEventId`; code review: `userId`-parameter toegevoegd)
- `server/domain/calendar-sync/homework-blocks.ts` (Task 2, eerdere sessie)
- `server/data/db.ts` (portability-fix, deze sessie)
- `server/domain/auth/calendar-token.ts` (portability-fix, deze sessie)
- `server/data/calendar-watch-channels.ts` (nieuw, deze sessie; code review: `deleteWatchChannel` toegevoegd, `completeSyncPass` met compare-and-swap-parameter)
- `server/api/calendar/homework-watch/register.post.ts` (nieuw, deze sessie; code review: oud-kanaal-stop vóór herregistratie, NaN-guard, orphan-preventie rond `fetchInitialSyncToken`)
- `server/api/calendar/homework-watch/register.delete.ts` (nieuw, code review — teardown-route, decision "optie a")
- `server/api/calendar/homework-watch/notifications.post.ts` (nieuw, deze sessie; live-experiment-fix: diagnostische logregels; code review: header-sanering, constant-time tokenvergelijking, `sync`-notificatie niet als wijziging behandelen)
- `server/cron/calendar-watch-tick.ts` (nieuw, deze sessie; code review: oud-kanaal-stop na hernieuwing, 410-only syncToken-reset, `DELETED`-scope-fix, paginering-fix, NaN-guard, `userId`-scoping)
- `server/middleware/session.ts` (live-experiment-bugfix: webhook-route toegevoegd aan `PUBLIC_PREFIXES`)
- `sst.config.ts` (deze sessie: nieuw `sst.aws.Cron`-component)
