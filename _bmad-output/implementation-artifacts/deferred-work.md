# Deferred Work

## Deferred from: code review of 2-1-beschikbare-tijd-agenda-koppelen (2026-09-03, verificatieronde 2)

- **Geen `accessRole`-validatie bij het koppelen van een beschikbare-tijd-agenda** — `getVisibleCalendars` filtert alleen op `hidden`/`selected` en leest `accessRole` niet uit, dus de membership-check in `server/domain/availability/calendar-source.ts:64` accepteert ook een geabonneerde agenda met `freeBusyReader`, waar `events.list` titelloze/onbruikbare items uit geeft. Precies het "stil falen in de planner" waar de validatie tegen zegt te beschermen. Pas echt zichtbaar zodra de Epic 3-stories de scheduling-engine op deze agenda aansluiten — daar oppakken.
- **`cache-control: private, no-store` ontbreekt op de settings-GET-routes** — stond expliciet op de in deze story verwijderde `week.get.ts`/`exceptions.get.ts`, is niet meegenomen naar `server/api/settings/availability-calendar.get.ts`, die de volledige lijst agendanamen en -ids van de gebruiker teruggeeft. Geen regressie binnen `server/api/settings/`: `homework-calendar-color.get.ts` zet 'm ook niet. Bescherming komt nu uitsluitend uit SST's `defaultTtl: 0` — een eigenschap van de infrastructuur, niet van de routes. In één keer voor alle settings-routes oplossen.
- **Nieuwe UI-copy belooft engine-gedrag dat pas na Epic 3 bestaat** — "Flowz plant sessies alleen binnen die blokken" en "anders kan Flowz nog geen planning maken" (`app/components/InstellingenBeschikbareTijd.vue:215-242`) zijn vandaag onwaar: `availabilityCalendarId` wordt buiten `calendar-source.ts` nergens gelezen en de engine draait onverkort op `getOrCreateWeekPattern`. De copy spreekt bovendien scenario 8.2 tegen, dat nog een +/- minuten-UI naar de exceptions-tabel aanbiedt. Onderdeel van de bewust aanvaarde tussentoestand; moet met de Epic 3-stories (3.1/3.4/3.5) meeverhuizen en dan kloppend gemaakt worden.

## Deferred from: code review of 2-1-beschikbare-tijd-agenda-koppelen (2026-09-02)

- **De scheduling-engine draait op bevroren `available_time_patterns` terwijl de enige schrijf-UI ervoor is verwijderd** — Story 2.1 haalt het weekpatroon-/afwijkingen-scherm weg, maar `availableMinutesForDate` (`server/domain/scheduling/doelmoment.ts`) en zijn afnemers (`shortfall.ts`, `week-overview.ts`, `actual-availability.ts`) lezen die tabellen nog steeds; `users.availabilityCalendarId` wordt buiten `calendar-source.ts` door niets gelezen. **Reden voor doorschuiven:** dit is de expliciet gefaseerde scope-grens uit Task 1.3 / Dev Notes en epics.md (Stories 3.1/3.4/3.5, "nog te herbouwen, volgt na 2-1") — bewust aanvaard op een hobby-project met één gebruiker. **Aandachtspunt voor die vervolgstories:** een nieuwe gebruiker krijgt via `getOrCreateWeekPattern` een rij met alle dagen op 0 en kan die nergens meer wijzigen, en `shortfall.ts:272` suggereert nog steeds "verhoog je beschikbare tijd voor die dag" — een actie die tot die ombouw geen UI meer heeft. Zie [[2-1-beschikbare-tijd-agenda-koppelen]].
- **4.1-pagespec-drift breder dan de story meldt** — naast het door de story genoemde `avail-page-heading` beschrijft `design-artifacts/C-UX-Scenarios/04-.../4.1-beschikbare-tijd-instellen.md` nog `Route: /instellingen/beschikbare-tijd` en een `avail-back-section`/`avail-back-link` (beide verdwenen met de tab-shell in `894acbf`), en stelt hij dat `AvailableTimePattern`/`AvailableTimeException` "niet meer bestaan als datamodel" — wat na deze story (bewust) niet klopt. **Reden voor doorschuiven:** documentatie-onderhoud, geen codewijziging; logisch om samen met de Epic 3-ombouw in één keer bij te werken. Zie [[2-1-beschikbare-tijd-agenda-koppelen]].

## Deferred from: code review of 2-4/2-5/7-1/7-2 (gecombineerd, 2026-08-26)

- **Migratie `0018_silent_beyonder.sql` liet `sessions.google_event_id` vallen zonder opruimstap** — oude, per-sessie Calendar-events uit de Story-2.3-periode (titel "Huiswerk: {vak} — {titel}") kunnen verweesd op de echte Google-agenda blijven staan, niet meer vindbaar door de app. **Besluit (Hillebrand):** zelf de agenda handmatig nalopen en oude events verwijderen; geen geautomatiseerde opruiming mogelijk (de DB heeft de ids niet meer). Zie [[2-5-samenvattende-huiswerk-blokken-per-dag]].
- **`getTodayEvents`'s `Promise.all` over alle geabonneerde agenda's veroorzaakt N onafhankelijke, gelijktijdige token-verversingen bij een verlopen access-token** (één per agenda) i.p.v. één gedeelde verversing. Geen correctheidsfout, puur vermijdbare inefficiëntie (Google's refresh-token-grant staat gelijktijdig hergebruik toe). **Reden voor doorschuiven:** het onderliggende token-ververs-ontwerp bestond al vóór Story 2.4; deze story vergrootte alleen het bereik. Geen incident waargenomen. Zie [[2-4-alle-geabonneerde-agendas-meenemen-bij-calendar-lezen]].
- **`POST /api/tasks/{id}/reopen` heeft geen concurrency-lock op de `completedAt`-read-check-write.** Twee bijna-gelijktijdige heropen-verzoeken voor dezelfde taak kunnen beide de guard passeren. **Reden voor doorschuiven:** zelfde risico-postuur als het al geaccepteerde patroon bij Story 6.2/6.4/6.5's confirm-routes — single-user hobby-app, geen concurrent-devices-gebruikspatroon.
- **Dubbele DOM-id's over `v-for`-rijen zijn verder gegroeid in schaal** (Story 7.1's amendement: `school-session-task-subject`/`-task-title`/`-time-input` herhalen nu alle drie tegelijk per rij, i.p.v. incidenteel). Bevestigd geen enkele `getElementById`/CSS-id-selector-afhankelijkheid. Blijft onderdeel van dezelfde, hieronder al beschreven projectbrede doorschuif-beslissing — nog geen aparte pas nodig.

## Deferred from: code review van 7-2-onverwachte-taak-toevoegen-vanuit-het-verzamelscherm (2026-08-24)

- **Dubbele DOM-id's over meerdere `v-for`-rijen** (`school-session-task-select`/`school-session-time-input`, Story 7.1; `school-session-new-task-title-input`/`-deadline-input`, Story 7.2; ook al eerder `week-day-suggestion-accept-button`, Story 6.5) — een vaste id herhaald per rij, ongeldige HTML, risicovol zodra ooit een `getElementById`/`<label for>`/id-gebaseerde e2e-selector wordt toegevoegd. **Reden voor doorschuiven:** een consistent, herhaaldelijk toegepast patroon in dit project (selector-conventie voor UX-specs, niet strikte DOM-uniciteit); een losse fix op één plek zou inconsistent zijn met de rest. Verdient een eigen, projectbrede pas over alle vergelijkbare elementen tegelijk (zelfde soort aanpak als een toekomstige a11y-pas, zie de `aria-live`-op-verdwijnend-element-entry hieronder), niet losse patches per story. Vandaag geen enkele echte aanroeper in de app zelf die hierdoor breekt.

## Audit (2026-08-18, op verzoek van Hillebrand): alle resterende `getDb().transaction()`-aanroepen in het project nagelopen — twee nog kwetsbare plekken gevonden én opgelost

Aanleiding: na Story 3.5's en `availability.ts`'s TOCTOU-fixes vroeg Hillebrand of dit patroon nog ergens anders in het project speelt. Alle 7 resterende `.transaction()`-gebruiken doorgelopen (`server/data/tasks.ts` en `server/data/availability.ts`): 2 bleken genuine, nog niet gefixte varianten van hetzelfde risico (lezen-dan-beslissen binnen een multi-statement-`tx`, die bij dit project se Turso-verbinding niet betrouwbaar serialiseert); de overige 5 (`deleteTaskAndSession`, `logSessionAndCompleteTask`, `logSessionAndUpdateRemaining`, `getOrCreateWeekPattern`, `updateWeekPatternDay`) zijn vaste-key-only schrijfacties zonder read-then-decide-risico, dus veilig zoals ze zijn.

- ~~**`createTaskAndSession` (`server/data/tasks.ts`, Story 3.1) leest de dag-stapelings-som en plaatst de nieuwe sessie binnen dezelfde `tx`.**~~ **Opgepakt 2026-08-18.** Zelfde resource/conflict als `placeSessionWithStackingOffset` ("hoeveel van vandaag is al bezet voor deze gebruiker") — daarom bewust de bestaande `sessionPlacementLocks`-tabel hergebruikt in plaats van een nieuwe. Live herverifieerd met gelijktijdige taak-aanmaak-verzoeken: geen overlap meer. Zie Story 3.1's Change Log.
- ~~**`updateTaskAndSubtasks`/`updateSubtaskStatus` (`server/data/tasks.ts`, Story 5.3/5.1) — de Story 5.3-review had de bestaande-deeltaken-lezing al ín de transactie gezet, maar dat lost het onderliggende Turso-serialisatieprobleem niet op (bewezen bij Story 3.5: multi-statement-`tx` serialiseert niet, ongeacht leesvolgorde binnen de transactie).**~~ **Opgepakt 2026-08-18.** Andere resource dan sessieplaatsing (deeltaak-reconciliatie op taak-niveau) — daarom een nieuwe, eigen `taskEditLocks`-tabel (migratie `0017_wild_black_cat.sql`), niet `sessionPlacementLocks` hergebruikt. Live herverifieerd met gelijktijdige `/done`/`/later`/bewerk-aanroepen op dezelfde taak (één testrun leek eerst een regressie te tonen, bleek het bestaande, opzettelijke "Heropenen"-gedrag te zijn dankzij een foutieve testpayload — na correctie 6x consistent correct). Zie Story 5.3's Change Log.

## `availability.ts`'s transactie-gebaseerde concurrency-claim — gesignaleerd én opgelost (2026-08-18)

- ~~**`server/data/availability.ts`'s `updateExceptionForDate`/`setExceptionForDate` gebruikten hetzelfde (bij Story 3.5 empirisch weerlegde) `getDb().transaction(...)`-patroon.**~~ **Opgepakt 2026-08-18, op verzoek van Hillebrand.** Story 2.2's oorspronkelijke verificatie (twee gelijktijdige `PATCH increase`-calls, 15→30) had te weinig statistisch gewicht om een niet-serialiserende transactie betrouwbaar te onderscheiden van een echt-serialiserende. Vervangen door hetzelfde lock-patroon als Story 3.5 (nieuwe `availabilityWriteLocks`-tabel, migratie `0016_dazzling_komodo.sql`, bewust een eigen tabel i.p.v. `sessionPlacementLocks` hergebruiken — andere resource). Live herverifieerd met een striktere test (6 gelijktijdige `increase`-calls): perfecte reeks 45/60/75/90/105/120, geen enkele stap verloren. Zie Story 2.2's Change Log voor het volledige verhaal.

## Audit (2026-08-17, op verzoek van Hillebrand): Dev Notes van alle 21 stories nagelopen op vergelijkbare niet-gelogde gaten

Aanleiding: de hierboven beschreven `/instellingen/beschikbare-tijd`-omissie stond nérgens in dit ledger, terwijl zowel Story 2.1 als Story 5.1 het gat in hun eigen Dev Notes al signaleerden. Een subagent heeft daarop alle 21 story-bestanden doorzocht op dezelfde soort "dit hoort ergens anders thuis, wordt later opgepakt"-aantekeningen die nooit als getrieerd deferred-item zijn gelogd. Twee genuine, nog open gaten kwamen naar boven (hieronder toegevoegd als eigen secties); de rest van de ~20-25 kandidaten bleek ofwel al elders gelogd, ofwel inmiddels aantoonbaar opgelost door een latere story, ofwel een bewuste, herbevestigde scope-keuze (geen stil vergeten gat).

## Deferred from: audit van Story 4.5's Dev Notes (2026-08-17) — opgepakt

- ~~**AC #3's heartbeat-fallback-logica is nooit gebouwd.**~~ **Opgepakt 2026-08-17.** Nieuw `server/domain/scheduling/session-heartbeat-fallback.ts` (`finalizeStaleSessionIfNeeded`), aangeroepen vanuit `GET /api/tasks/[id]` (het enige moment waarop de server weet dat Evelien een sessie voor déze taak gaat (her)starten). Ontwerpkeuze (Hillebrand): stil automatisch afronden, geen bevestigingsprompt. Zie Story 4.5's eigen Change Log voor de volledige implementatie/verificatie.
- **Bekende, aanvaarde beperking:** een client die haar `actualMinutes` verzint via de normale `/replan`-route kan dat nog steeds doen — deze fix voegt alleen een eerlijk fallback-eindpunt toe voor het crash/verbinding-verlies-scenario (wanneer er géén expliciete `/replan`-aanroep komt), geen volledige anti-fraude-laag. Blijft buiten scope tenzij Hillebrand anders aangeeft.
- **`STALE_THRESHOLD_MS = 5 minuten` is een beargumenteerde keuze, geen UX-spec-waarde** — ruim boven de 30s-heartbeat-interval om vals-positieven door achtergrondtab-throttling te vermijden (zie Story 4.4's eigen gedocumenteerde risico hierover), maar niet expliciet met Hillebrand afgestemd als exacte waarde. Aanpassen als dit in de praktijk te gevoelig/ongevoelig blijkt.

## Deferred from: audit van Story 3.5's Dev Notes (2026-08-17)

- ~~**TOCTOU-race in `recalculateTaskPlanning`**~~ — **Opgepakt 2026-08-17/18.** Bleek bij nader (live) onderzoek twee aparte bugs te zijn: (1) een échte gelijktijdigheids-race, gesloten met een nieuwe `sessionPlacementLocks`-tabel nadat twee andere technieken (multi-statement-transactie, single-statement optimistic-guard) empirisch bleken te falen tegen deze Turso-verbinding; (2) een structurele fout in de stapelings-plaatsingsformule die ook **zonder enige concurrency** optreedt zodra `resolve-conflict.post.ts` twee taken die dezelfde dag delen ná elkaar herberekent — pas ontdekt tíjdens het testen van fix 1. Zie `3-5-idempotente-herberekening-bij-wijzigingen.md`'s Dev Notes ("De TOCTOU-race, alsnog opgepakt") voor het volledige, leerzame verhaal — inclusief waarom de eerste twee pogingen faalden, wat dat zegt over dit project se Turso-aannames elders (mogelijk ook `availability.ts`'s `updateExceptionForDate`, nooit met dezelfde rigueur gelijktijdig getest), en hoe de tweede bug pas zichtbaar werd door een strikt-sequentiële test uit te voeren toen de eerste fix de race niet bleek te verklaren.

## Losse fix (2026-08-17, gesignaleerd door Hillebrand): `/instellingen/beschikbare-tijd` was onbereikbaar

**Niet uit een code review, direct als bug gemeld.** Story 2.1 (done, 2026-07-31) bouwde `app/pages/instellingen/beschikbare-tijd.vue` bewust zonder menu-link, met de aantekening "hoort bij Epic 4's hoofdscherm". Epic 4 bouwde geen navigatiemenu. Story 5.1 (Epic 5) bouwde uiteindelijk het echte `HamburgerMenu.vue`-component en signaleerde daarbij expliciet dat déze pagina nog steeds nergens aan gelinkt was ("óók `/beschikbare-tijd`... heeft geen enkele link ernaartoe") — maar loste dat gat niet zelf op, en geen latere story (5.2/5.3/6.x) deed dat alsnog. Resultaat: een volledig werkende pagina (weekpatroon, dag-specifieke afwijkingen, huiswerk-kleur-instelling) was sinds Story 2.1 alleen bereikbaar via het rechtstreeks intypen van de URL.

**Fix:** `app/components/HamburgerMenu.vue`'s `ITEMS`-array uitgebreid met `{ label: 'Beschikbare tijd', to: '/instellingen/beschikbare-tijd' }`. `typecheck` schoon, gedeployed, live geverifieerd: het menu-item verschijnt, navigeert correct naar de pagina (weekpatroon/afwijkingen/kleurinstelling allemaal zichtbaar), en de bestaande "← Terug"-knop (met zijn al aanwezige `history.state.back`-fallback uit Story 2.1's eigen code review) navigeert correct terug naar Home.

## Deferred from: code review of 6-7-agendaconflict-detectie-bij-opstarten (2026-08-17)

- **7 losse Calendar-API-aanroepen (één per dag in het 7-dagen-venster) i.p.v. één ranged query** — `detectAgendaConflicts` roept `getTodayEvents` per dag aan i.p.v. één `timeMin`/`timeMax`-aanroep over de hele week. **Reden voor doorschuiven:** identiek patroon als het al bestaande `week.get.ts`/`buildWeekDay` (Story 6.5) — geen nieuw geïntroduceerd risico, en nu de blocking-`await`-fix is toegepast (zie Review Findings) drukt deze latency niet meer op Home's eerste render.
- **Geen server-side check dat een gedismiste `(date, googleEventId)`-combinatie ooit een echt gedetecteerd conflict was** — `dismiss.post.ts` accepteert en persisteert elke combinatie zonder tegen `detectAgendaConflicts` te verifiëren. **Reden voor doorschuiven:** laag risico (rijen zijn user-gescopet, kunnen alleen "vervuilen" met betekenisloze data, geen manier om iemand anders' data te raken of misbruiken).
- **Gedupliceerde "sessie-eindtijd = start + plannedMinutes"-berekening** tussen `conflict-detection.ts` en `actual-availability.ts` (Story 6.6). **Reden voor doorschuiven:** matcht dit project se al meerdere keren geaccepteerde "dupliceer tot een derde consument"-precedent (zie `session-time-check.ts`'s eigen redenering) — dit is pas de tweede plek.
- **Conflicten op vandaag met een al-verstreken tijdstip worden nog steeds getoond** — het 7-dagen-venster filtert niet op "is dit tijdstip al voorbij". **Reden voor doorschuiven:** geen bug maar een niet-beantwoorde ontwerpvraag (niet in de AC/UX-spec behandeld); vereist een productbeslissing, geen technische fix.
- **`plannedMinutes === 0` zou een sessie nooit als conflicterend laten registreren** (`overlapInterval` vereist een strikt positief interval). **Reden voor doorschuiven:** onbereikbaar gegeven bestaande validatie elders in het project (sessieduur moet 5-480 minuten zijn, Story 3.1/6.7's eigen testervaring bevestigde dit al bij het aanmaken van een taak).
- **Geen geautomatiseerde tests voor `conflict-detection.ts`, de nieuwe routes, `dismissed-conflicts.ts`, of `ConflictModal.vue`** — projectbreed al herhaaldelijk gevonden en getrackt, niet uniek aan deze story.

## Deferred from: code review of 6-6-beschikbare-tijd-aanpassen-na-conflict-samenvatting (2026-08-17)

- **`envelope()` in `prefill-conflict.post.ts`/`resolve-conflict.post.ts` heeft een `statusCode`-parameter die nergens in de functie-body gebruikt wordt** (de daadwerkelijke HTTP-status wordt apart via `setResponseStatus` gezet). **Reden voor doorschuiven:** triviaal, geen functioneel risico — puur een opschoonpunt in een copy-paste-helper die al eerder (Story 2.2) bewust als kleine, expliciete route-bestanden i.p.v. gedeelde abstractie is gekozen.
- **Geen validatie tegen datums in het verleden** op `prefill-conflict`/`resolve-conflict` — een conflict-flow voor een al-verstreken dag kan in theorie worden aangeroepen. **Reden voor doorschuiven:** lage kans (de flow wordt altijd vanuit een Home/Week-link met een geldige toekomstige/huidige datum geopend, nooit los ingetypt), en dit project valideert elders ook geen datumgrenzen op vergelijkbare endpoints (bv. `week/[date]/suggestion/accept.post.ts`'s venster-check is een uitzondering, geen algemene norm).
- **Geen concurrency-lock bij dubbele gelijktijdige `resolve-conflict`-aanroepen** voor dezelfde datum (dubbele klik voorbij de client-side guard, of een netwerk-retry) — kan leiden tot dubbele herberekening van dezelfde sessies. **Reden voor doorschuiven:** identiek risico-postuur als het al geaccepteerde patroon bij Story 6.2/6.4's confirm-routes — geen nieuw geïntroduceerd risico voor een single-user hobby-app.
- **`isTimedEvent` kan `NaN` opleveren als een Calendar-event een inconsistent datum/tijd-veld heeft** (getimede `startsAt` met datum-only `endsAt` of omgekeerd) — `overlapInterval` zou dan stil 0 overlap teruggeven i.p.v. de echte overlap te detecteren. **Reden voor doorschuiven:** zeer lage kans (Google Calendar levert dit paar altijd consistent), en zelfde aanname als het al langer bestaande `session-time-check.ts` (Story 4.2) waar dit patroon vandaan komt.
- **Geen geautomatiseerde tests voor `actual-availability.ts`, de nieuwe routes, of `aanpassen.vue`** — projectbreed al herhaaldelijk gevonden en getrackt (zie eerdere entries), niet uniek aan deze story.
- **`recalculateTaskPlanning(task.id)` in `resolve-conflict.post.ts` herverifieert zelf niet expliciet dat de taak bij `session.user.id` hoort** — de aanroepende query (`getTasksWithSessionOnDate(session.user.id, date)`) is al user-gescopet, dus de ids die de loop ingaan zijn vertrouwd, maar dat is een aanname over `recalculateTaskPlanning`'s interne contract, niet iets wat dit bestand zelf afdwingt. **Reden voor doorschuiven:** geen nieuw risico t.o.v. hoe `recalculateTaskPlanning` al overal elders (Story 3.5, 6.4) wordt aangeroepen — zelfde vertrouwensgrens als daar.

## Deferred from: code review of 6-5-weekoverzicht-met-knelpunt-signalering (2026-08-17)

- **`WeekSuggestionAcceptResponse` (de volledige `WeekDayDto`-respons van `POST /api/week/{date}/suggestion/accept`) wordt door de client nooit gebruikt** — ná bevestigen haalt de pagina sowieso de hele week opnieuw op (nodig omdat een "herplannen"-suggestie twee dagen kan raken, zie Task 5's bugfix), dus de respons van de accept-route zelf is dode payload. **Reden voor doorschuiven:** cosmetische opruiming (bv. een minimale ack teruggeven i.p.v. een volledige `WeekDayDto`), geen correctheids- of performance-risico dat de moeite van een aparte patch waard is.
- **`neededMinutes` (`buildWeekDay`, som via `getTasksWithSessionOnDate`) en `detectShortfallForDate`'s eigen interne som (`sumPlannedMinutesForUserOnDate`) zijn twee onafhankelijke implementaties van "geplande minuten op deze dag"** — quasi-gegarandeerd gelijk (zelfde onderliggende tabel/datumfilter), maar niet door één gedeelde functie afgedwongen. **Reden voor doorschuiven:** matcht een al eerder gedocumenteerd, geaccepteerd patroon in dit project (zie de entry hieronder over het 3×-gedupliceerde datumfilterpatroon in `data/tasks.ts`, Story 3.4) — zelfde soort bescheiden duplicatie, geen nieuw risico.
- **Eén falende dag binnen `GET /api/week`'s lus (bv. een DB-fout) laat de hele week-aanroep 500'en**, i.p.v. gedegradeerd alleen die ene dag te tonen — anders dan de per-dag Calendar-foutafhandeling, die wél al bestaat. **Reden voor doorschuiven:** laag risico (vereist een echte DB-fout, niet de al afgeschermde Calendar-fout), een correcte per-dag-degradatie voor de scheduling-berekeningen zelf is een aparte, niet-triviale uitbreiding.
- **`week-day-bottleneck-badge`'s `aria-live="polite"` zit op het element dat zelf via `v-if` verdwijnt zodra het knelpunt is opgelost** — een verdwijnende live-region kondigt niet betrouwbaar iets aan bij alle screenreaders. **Reden voor doorschuiven:** matcht een al meerdere keren eerder uitgestelde a11y-categorie in dit project (leave-confirm-modal, segmented-controls, informatiedichte widgets) — geen bestaand live-region-patroon om op aan te sluiten, verdient een eigen toegankelijkheidspas over alle soortgelijke elementen tegelijk, niet een losse patch op één badge.

## Deferred from: code review of 6-4-vandaag-niet-als-gepland-te-weinig-energie-pad (2026-08-17)

- **Geen transactie/gelijktijdigheids-guard rond `applyEnergyProposal`/`confirm.post.ts`** — twee gelijktijdige confirm-verzoeken (dubbele klik voorbij de client-side guard, of een netwerk-retry) kunnen beide een voorstel herberekenen en toepassen, met kans op dubbele verplaatsing/inkorting. **Reden voor doorschuiven:** identiek patroon als het al geaccepteerde `apply-recommendation.ts`/`accept.post.ts` (Story 6.2) — zelfde project-brede risico-postuur voor een single-user hobby-app, geen nieuw geïntroduceerd risico.
- **Bij gedeeltelijke afwijzing (sommige `gemiddeld`-taken wél, andere niet ingekort) krijgt de gebruiker geen per-taak uitleg voor de afgewezene** — `notShortenedReason` is een dag-brede vlag die alleen vult als *niets* is ingekort. **Reden voor doorschuiven:** AC #3 beschrijft letterlijk alleen het dag-brede "niets ingekort"-geval ("de conclusie is dat er bewust niets is ingekort"); per-item uitleg voor een gedeeltelijk scenario is een legitieme toekomstige UX-uitbreiding, geen huidige AC-schending.
- **Stap 2's "naar voren halen" vult capaciteit greedy op deadline-volgorde, zonder terug te vallen op een andere combinatie als een kandidaat net niet past** — kan vrijgekomen capaciteit onderbenutten. **Reden voor doorschuiven:** FR23 noemt dit expliciet optioneel ("kan eventueel"); geen AC vereist een optimale bin-packing, en dit is bewust eenvoudig gehouden zoals de rest van dit project's scheduling-heuristieken (geen exacte-optimalisatie-eis elders ook).

## Deferred from: code review of 5-2-taakdetail-bekijken-verwijderen (2026-08-16)

- **Verwijder-bevestigingsmodal mist dialoog-semantiek/focus-management** (`role="dialog"`, focus-trap, Escape-toets) — puur muis-georiënteerd. **Reden voor doorschuiven:** matcht een al geaccepteerd, herhaaldelijk gedocumenteerd gat in dit project (`active-leave-confirm-modal`, Story 4.5/3.3) — verdient een eigen toegankelijkheidspas over alle soortgelijke modals tegelijk, geen losse patch op nummer zoveel.
- **Twee gelijktijdige `DELETE`-verzoeken voor dezelfde taak kunnen racen** (bv. een dubbele klik die tóch door de `:disabled`-guard heen komt, of twee open tabbladen) — de tweede aanroep wordt een stille no-op die alsnog `{ ok: true }` teruggeeft. **Reden voor doorschuiven:** zeer lage kans, en dit project heeft nergens anders DB-niveau-locking voor dit soort races — consistent met de rest van de codebase.
- **`DELETE`-404 op een al-verwijderde taak wordt niet apart onderscheiden van een echte serverfout** — de generieke foutmelding ("Kon de taak niet verwijderen") is technisch niet fout, maar een specifiekere melding ("Deze taak bestaat al niet meer") zou dit randgeval beter afvangen. **Reden voor doorschuiven:** lage kans (vereist een gelijktijdige verwijdering vanuit een andere tab/sessie), huidige gedrag is niet incorrect, alleen niet optimaal specifiek.
- **Generieke, niet-genamespacete `useState`-sleutelnamen (`taak-detail`, `flash-message`)** — een toekomstige tweede consument (bv. Story 5.3's bewerkformulier, dat mogelijk ook taakdata via `useState` wil doorgeven) zou per ongeluk kunnen botsen met déze sleutels. **Reden voor doorschuiven:** speculatief, er bestaat nu geen concrete tweede consument — pas oplossen zodra die zich daadwerkelijk aandient.

## Deferred from: code review of 5-1-takenoverzicht-lijst-per-week (2026-08-15)

- **Race condition: onbeveiligde, niet-`await`-ede `/done`/`/later`-aanroepen kunnen in theorie in de verkeerde volgorde aankomen** — `subtaakLater()` zet een deeltaak terug in de wachtrij; wordt-ie later alsnog met "Klaar" afgerond, vuurt dat een tweede, ongerelateerde fire-and-forget-aanroep af. Bij netwerk-reordering zou de laatste server-aankomst kunnen winnen i.p.v. de laatste gebruikersactie. **Reden voor doorschuiven:** laag risico (normale HTTP-verzoeken naar dezelfde origin komen in de praktijk vrijwel altijd in volgorde aan), en een echte oplossing (sequentieel wachten, of een versiestempel per aanroep) staat op gespannen voet met de bewust snelle, niet-blokkerende lokale sessie-UX.
- **`HamburgerMenu` heeft geen volledige focus-management** (openen verplaatst focus niet naar het paneel, sluiten niet terug naar de knop) — puur muis-/klikgeoriënteerd. **Reden voor doorschuiven:** matcht een al meerdere keren eerder geaccepteerd patroon in dit project (leave-confirm-modal, Story 3.3/4.5) — verdient een eigen toegankelijkheidspas over alle soortgelijke elementen tegelijk, geen losse patch op één nieuwe component.
- **Geen index op `subtasks.task_id`** voor de nieuwe join+aggregatie-query (`getOpenTasksWithProgress`) — scant momenteel de volledige `subtasks`-tabel per aanroep. **Reden voor doorschuiven:** premature optimalisatie op de huidige (hobby-)schaal, consistent met dit project se "geen vroegtijdige optimalisatie"-houding elders.
- **Geen geautomatiseerde tests voor de nieuwe routes/pagina** (`GET /api/tasks`, `POST /api/subtasks/{id}/done|later`, `app/pages/taken/index.vue`) — projectbreed al herhaaldelijk gevonden en getrackt, niet uniek aan deze story.

## Deferred from: code review of 4-7-sessie-afronden-fire-and-forget-herplanning-bij-verlaten (2026-08-15)

- **Geen rollback bij een falende `deleteHomeworkEvent`-aanroep in `replanAfterSession`'s "taak klaar"-tak** — als de Calendar-verwijdering faalt (netwerk, 5xx, verlopen refresh-token) nadat `actualMinutes` al is opgeslagen, blijft de taak "niet afgerond" met een verweesd Calendar-event. **Reden voor doorschuiven:** matcht `recalculateTaskPlanning`'s eigen, al-gereviewde en geaccepteerde "geen rollback"-precedent voor exact dit type gedeeltelijke storing (DB-write vóór een externe Calendar-aanroep) — geen nieuw risico, consistent met een bestaand, bewust geaccepteerd patroon.
- **Fire-and-forget dataverlies bij netwerkfout, geen retry/notificatie** — `overzicht.vue`'s `terugNaarHome()` wacht niet op de `/replan`-response; bij een falende aanroep is de enige sporing een `console.error`, en de aanpassing (afronding/resterende tijd) gaat stilzwijgend verloren. **Reden voor doorschuiven:** expliciet UX-spec-gedrag (fire-and-forget is een letterlijke eis, geen impliciete aanname), en al vastgelegd als open punt (Story 4.7's Open Question #2 / UX-spec's eigen Open Question #4) — vereist een retry-/notificatiemechanisme dat nog nergens in dit project ontworpen is.
- **`tasks.totalMinutes` krijgt een tweede, overlappende betekenis** (creatie-tijd-schatting via Story 3.1/3.2 vs. post-sessie-"resterende tijd" via déze story) — een toekomstige wijziging aan één betekenis (bv. Story 5.3's bewerkformulier, dat `totalMinutes` mogelijk herberekent uit deeltaken) zou de andere kunnen overschrijven zonder dat iets dat detecteert. **Reden voor doorschuiven:** inherent aan de bevestigde Open Question #1-beslissing (Hillebrand, 2026-08-15) om `totalMinutes` te hergebruiken i.p.v. een nieuw veld toe te voegen — een bewuste afweging, geen onbedoelde bug; relevant om mee te nemen zodra Story 5.3 gebouwd wordt.
- **Geen geautomatiseerde tests voor het completion-/Calendar-verwijderpad** (`replanAfterSession`, `POST /api/sessions/{sessionId}/replan`) — projectbreed al herhaaldelijk gevonden en getrackt (zie eerdere entries), niet uniek aan deze story.

## Deferred from: code review of 4-6-sessie-afronden-overzicht-resterende-tijd-aanpassen (2026-08-15)

- **`sessieOverzichtLog.value = null` vóór een niet-`await`-ed `navigateTo('/')` (mogelijke blanke-flash)** — het `main`-element unmount't (via `v-if="loggedIn && log"`) zodra de log-state genuld wordt, terwijl `navigateTo` nog niet is afgerond, wat theoretisch een korte blanke flits kan geven vóór de daadwerkelijke navigatie. **Reden voor doorschuiven:** exact hetzelfde patroon zit al live in `app/pages/sessie/actief.vue:144`'s `stopSessie()` (Story 4.4/4.5, al done) — geen nieuw geïntroduceerd risico, consistent met een al geaccepteerd projectpatroon.
- **Geen testdekking voor `overzicht.vue` of de afwijkingsheuristiek** — projectbreed al herhaaldelijk gevonden en getrackt (zie eerdere entries), niet uniek aan deze story.
- **Hardcoded hex-kleuren i.p.v. design tokens in `overzicht.vue`** — identiek patroon als al aanwezig in `actief.vue`. **Reden voor doorschuiven:** projectbreed patroon, geen design-token-systeem in dit project; verdient een eigen pas over alle schermen tegelijk.
- **Geen focus management bij de details-toggle (`wrap-subtask-list`)** — het uitklappen van de subtakenlijst verplaatst focus niet naar de onthulde inhoud. **Reden voor doorschuiven:** geen AC-eis, geen bestaand focus-trap-precedent in dit project (zelfde reden als eerdere uitgestelde a11y-punten).

## Deferred from: code review of 4-5-sessie-actief-wegnavigeer-bescherming (2026-08-02)

- **`active-leave-confirm-modal` heeft geen `role="dialog"`, geen focus-trap, geen Escape-handler** — puur visueel/muis-georiënteerd, geen toetsenbord-/screenreader-toegankelijkheid. **Reden voor doorschuiven:** geen enkele AC noemt toetsenbord-/screenreader-navigatie voor dit element, en dit project heeft nog geen bestaand modal-component (`taak-confirm-overlay` bestaat wel maar met een ander specifiek doel/tekst) om op aan te sluiten — verdient een eigen ontwerppas, net als de eerdere uitgestelde a11y-punten in dit project.

## Deferred from: code review of 4-4-sessie-actief-timer-pauzeren-subtaken-afronden-uitstellen (2026-08-02)

- **Timer-weergave kan tijdelijk bevriezen in een achtergrondtab** — browsers kunnen `setInterval` in een niet-actieve tab throttlen/pauzeren; de onderliggende wandklok-berekening (`elapsedMs`) blijft correct, maar de zichtbare `active-timer`-tekst en `overGeplandeTijd`-signaal updaten pas weer bij de eerstvolgende tick, niet direct bij het terugkeren naar het tabblad. **Reden voor doorschuiven:** vereist een `visibilitychange`-listener die de weergave forceert te verversen bij focus-terugkeer — een kleine, losstaande verbetering, geen correctheidsfout in de onderliggende berekening zelf.
- **Kleur-only signaal voor "over de geplande tijd"** (`active-timer--over`) — geen tekst/icoon/`aria-*`-alternatief voor kleurenblinde gebruikers of screenreaders. **Reden voor doorschuiven:** UX-spec eist expliciet "subtiel, geen alarm"; een toegankelijk alternatief verdient een eigen ontwerppas (net als eerdere uitgestelde a11y-punten in dit project), geen losse patch tijdens déze review.
- **Geen click-guard tegen een zeer snelle dubbele klik op "Klaar"/"Later"** — kan in theorie twee subtaken in één fysieke actie verwerken (de knoppen worden niet gedisabled tijdens verwerking). **Reden voor doorschuiven:** lage kans, lage impact (herstelbaar door simpelweg door te werken), en de synchrone state-update maakt dit al lastig te raken in de praktijk.

## Deferred from: code review of 4-3-sessie-tussenscherm-benodigdheden-bekijken (2026-08-02)

- **`TaskPrepResponse` en `HomePlanResponse['nextTask']` hebben identieke velden zonder gedeeld brontype** — puur een codecomment die de parallel bewaakt, geen typesysteem-afdwinging. Een toekomstige wijziging aan één van beide zou stil kunnen driften. **Reden voor doorschuiven:** bewuste keuze in de story zelf (twee endpoints met een eigen levenscyclus); een gedeeld basistype is een redelijke toekomstige refactor, geen huidige correctheidsfout.
- **Geen rate limiting/logging op herhaalde ownership-mismatches** (`GET /api/tasks/[id]` met een niet-eigen taak-id) — de anti-enumeratie-bescherming (404 i.p.v. 403) heeft geen detectie erachter als iemand actief taak-id's raadt. **Reden voor doorschuiven:** geen bestaand rate-limiting-patroon ergens in dit project om op aan te sluiten; verdient een projectbrede aanpak, geen losse patch op één route.

## Deferred from: code review of 4-2-hoofdscherm-waarschuwing-banner-later-vandaag-calendar-rij (2026-08-02)

- **Overlappende kalenderblokken in `home-calendar-dayview` krijgen geen lane/offset-lay-out** — twee of meer elkaar overlappende Calendar-afspraken worden gewoon over elkaar heen getekend (volle breedte, geen zij-aan-zij-verdeling), zichtbaar geworden tijdens live-verificatie met drie kort-na-elkaar geplande testtaken. **Reden voor doorschuiven:** de UX-spec eist geen specifieke overlap-lay-out; een echte lane-toewijzingsalgoritme is een aparte, niet-triviale uitbreiding die niet bij déze story's kernscope hoort.
- **Geen filtering van afgewezen (`attendee.responseStatus: 'declined'`) of "vrij"/transparante Calendar-afspraken vóór de tijds-check** — zo'n afspraak zou nog steeds een `tight`/`unavailable`-waarschuwing kunnen triggeren, ook al blokkeert 'ie in de praktijk niets. **Reden voor doorschuiven:** vereist het uitlezen van extra Google-events-velden (`attendees`, `transparency`) die nu niet worden opgehaald; een nuance bovenop de spec, niet expliciet geëist door AC #1.
- **`home-calendar-dayview` heeft geen bredere ARIA-semantiek dan de native `title`-tooltip per blok** — geen `aria-label` op de dagweergave zelf, geen lijst-/rij-rollen, niets dat afsprakenaantal/-tijden aankondigt voor een screenreader. **Reden voor doorschuiven:** vergelijkbaar met Story 3.1's al-uitgestelde segmented-control-toegankelijkheidspas — verdient een eigen, doordachte pas i.p.v. een losse patch op één widget, en dit project heeft nog geen bestaand patroon voor informatiedichte, alleen-lezende widgets om op aan te sluiten.
- **`amsterdamMinutesSinceMidnight` heeft geen dag-grens-bewustzijn** — een Calendar-afspraak die de vorige avond laat begint (bv. 23:00) en doorloopt tot na middernacht zou fout gepositioneerd worden in `home-calendar-dayview` (alleen het uur-minuut-deel wordt gelezen, geen "vóór/na vandaag"-onderscheid). **Reden voor doorschuiven:** zeer lage waarschijnlijkheid voor een schoolagenda/huiswerkcontext; een correcte fix vereist het meenemen van de volledige datum in de positioneringslogica, wat de huidige eenvoudige minuten-sinds-middernacht-aanpak significant zou compliceren voor een randgeval dat in de praktijk zelden voorkomt.

## Deferred from: code review of 4-1-hoofdscherm-dagplanning-eerstvolgende-taak (2026-08-02)

- **Geen responsive/mobile CSS voor het hoofdscherm** — `app/pages/index.vue` gebruikt vaste desktop-breedtes (`max-width: 32rem`, geen media queries), terwijl Flowz primair op een telefoon/tablet gebruikt wordt (UX-context: Evelien plant huiswerk, geen deskop-only-workflow). **Reden voor doorschuiven:** eerste échte hoofdscherm-story, geen bestaand responsive-patroon in dit project om op aan te sluiten; verdient een eigen, doordachte pas over alle Epic-4-schermen tegelijk i.p.v. een losse patch op één pagina.
- **Derde onafhankelijke kopie van de `envelope()`-helper** — nu in `plan.get.ts`, naast de twee al-bestaande kopieën elders (bv. `tasks.post.ts`). Elke route herdefinieert dezelfde 4-argument-vorm. **Reden voor doorschuiven:** bescheiden DRY-winst, geen functioneel risico (de vorm is overal identiek); verdient een gedeelde `server/utils/`-helper i.p.v. een ad-hoc-extractie tijdens déze story se review.

## Deferred from: code review of 3-5-idempotente-herberekening-bij-wijzigingen (2026-08-02)

- **Geen vroegtijdige no-op-detectie in `recalculateTaskPlanning`** — elke aanroep doet onvoorwaardelijk een DB-write + Calendar-aanroep, ook als de herberekende plaatsing identiek is aan de huidige. **Reden voor doorschuiven:** reële optimalisatie, geen huidige aanroeper (Epic 4/5/6 nog `backlog`), en de self-healing-tak zou sowieso altijd moeten blijven controleren op een ontbrekende `googleEventId`.
- **`updateHomeworkEvent` onderscheidt geen echte 404 (een blijvend verwijderd event) van Story 2.3's al-geteste tombstone/200-scenario** (handmatig verwijderd via de Google-UI) — bij een echte 404 zou `recalculateTaskPlanning`'s Calendar-sync gewoon falen i.p.v. self-healing te triggeren. **Reden voor doorschuiven:** zeer zeldzaam randgeval; Story 2.3's live-testen dekten alleen het realistische pad (handmatige verwijdering via de UI geeft altijd een tombstone/200, geen 404).
- **Geen gebruikerszichtbaar signaal als een sessie met een `googleEventId` stil "verweest" raakt** doordat write-scope/kleur na het aanmaken van dat event wordt ingetrokken — `updateHomeworkEvent` no-opt dan stil (bestaand Story 2.3-gedrag), en het event blijft stale tot write-scope ooit hersteld wordt én een herberekening toevallig loopt. **Reden voor doorschuiven:** bestaand, al-gereviewd gedrag van `updateHomeworkEvent` zelf; déze story maakt het pad alleen vaker bereikbaar (via herberekening) zonder het zelf te veroorzaken.

## Deferred from: code review of 3-4-volgorde-algoritme-bij-meerdere-concurrerende-taken (2026-08-02)

- **Het join+`substr(startsAt,1,10)`-datumfilterpatroon staat drie keer onafhankelijk in `server/data/tasks.ts`** (`createTaskAndSession`, `sumPlannedMinutesForUserOnDate`, `getTasksWithSessionOnDate`) — een gedeelde helper zou de "16:00 Amsterdam-anker kruist nooit UTC-middernacht"-aanname op één plek houden i.p.v. drie onafhankelijke plekken die elk apart vertrouwd moeten worden. **Reden voor doorschuiven:** zou de twee bestaande, al-gereviewde functies aanraken voor een bescheiden DRY-winst — buiten scope voor de story die de derde kopie toevoegde.

## Deferred from: code review of 3-3-benodigdheden-met-auto-suggestie-per-vak (2026-08-01)

- **Geen vroege bovengrens op de ruwe `needs`-array-lengte vóór de per-item-validatielus in `POST /api/tasks`** — elk element wordt getrimd/afgekapt/aan een `Set` toegevoegd vóórdat de array pas aan het eind op `MAX_NEEDS_COUNT` (30) wordt afgekapt, dus een zeer grote array (bv. duizenden elementen) kost onnodig werk vóórdat het effect zichtbaar wordt. **Reden voor doorschuiven:** het endpoint vereist al authenticatie (beperkt het misbruikbereik), en het verwerken van korte strings is per request goedkoop — geen acuut DoS-risico, wel een inconsistentie met `MAX_SUBTASKS`'s eigen vroege-`length`-check (Story 3.2).
- **Nieuwe `taak-needs-subject-change-dialog` hergebruikt het bestaande `taak-confirm-overlay`-patroon, dat geen focus-management heeft** (geen `aria-labelledby`, geen focus-verplaatsing bij openen, geen Escape-toets-afhandeling). **Reden voor doorschuiven:** dit is een bestaand gat, al aanwezig in de originele leave-confirm-dialog sinds Story 3.1 — verdient een eigen toegankelijkheidspas voor alle `taak-confirm-overlay`-instanties tegelijk, niet een losse patch op één van de twee.
- **`lastConfirmedSubject` wordt ook bijgewerkt als de vak-wijziging-dialoog verschijnt en Evelien "Nee, laat mijn lijst staan" kiest** — exact dezelfde vak-tekst daarna nogmaals bevestigen (zonder tussentijdse wijziging) wordt dan een stille no-op: geen nieuwe auto-vul, geen nieuwe dialoog. **Reden voor doorschuiven:** vereist het opnieuw kiezen van identieke tekst zonder wijziging ertussen om te raken — zeer lage waarschijnlijkheid, lage impact (de gebruiker kan het vak-veld altijd nog handmatig legen en opnieuw invullen om alsnog een nieuwe trigger te krijgen).
- **Een komma-gescheiden string in `taak-needs-input` plakken produceert één tag met letterlijke komma's erin, geen losse tags per item** — het comma-splitsen gebeurt alleen op het `keydown`-event van de komma-toets zelf, wat een plak-actie (die geen los `keydown` per teken genereert) niet raakt. **Reden voor doorschuiven:** UX-verfijning, geen AC-eis; de basisfunctionaliteit (typen + Enter/komma voegt toe) werkt zoals gespecificeerd.

## Deferred from: code review of 3-2-deeltaken-automatische-tijdsom (2026-08-01)

- **Zero geautomatiseerde tests voor deze story** — projectbreed al herhaaldelijk gevonden en getrackt, niet uniek aan deze story; zie de eerdere entries hieronder voor dezelfde, nog steeds openstaande reden (geen testframework in het project).
- **`number | string | null`-getypeerde refs (`totalTimeHours`/`totalTimeMinutes`/`row.minutes` in `app/pages/taak/nieuw.vue`) zijn een latente valkuil voor toekomstige code** — `Number('')` levert stil `0` op i.p.v. een fout; elke huidige call site is al bewust met de `isEmptyField()`-helper afgeschermd, maar een toekomstige uitbreiding die dat patroon niet volgt zou hier stil op stuk kunnen lopen. **Reden voor doorschuiven:** geen huidige bug, alleen een code-hygiëne-notitie voor een volgende wijziging aan dit bestand.

## Deferred from: code review of 3-1-taak-aanmaken-kerngegevens-met-doelmoment-berekening (2026-08-01)

- **Moeilijkheid/Prioriteit-segmented-controls missen het WAI-ARIA-toetsenbordmodel dat `role="radiogroup"`/`role="radio"` belooft** — elke knop blijft individueel Tab-focusbaar, geen pijltjestoetsnavigatie, geen enkele tabstop voor de hele groep. Eerste component van dit type in de codebase. **Reden voor doorschuiven:** geen bestaand roving-tabindex-patroon in dit project om op aan te sluiten; verdient een eigen, doordachte toegankelijkheidspas i.p.v. een haastige losse patch.
- **`GET /api/tasks/subjects` toont geen foutstatus bij een mislukte aanroep (anders dan 401)** — `taak-subject-select` blijft dan gewoon leeg-typbaar, ononderscheidbaar van een nieuwe gebruiker zonder eerdere taken. **Reden voor doorschuiven:** cosmetische degradatie, geen functionele breuk (het vak blijft vrij typbaar), lage prioriteit.

## Deferred from: code review of 2-3-huiswerk-kleur-kiezen-calendar-write-sync-service (2026-08-01)

- **`hasCalendarWriteScope` synct nooit terug als de gebruiker Calendar-toegang buiten Flowz om intrekt** — als Evelien de Calendar-toegang via haar eigen Google-accountinstellingen intrekt zonder in Flowz opnieuw in te loggen, blijft `has_calendar_write_scope: 1` in de database staan en dus `needsReconsent: false`. De eerstvolgende write-sync-poging krijgt een 401, probeert te verversen met een inmiddels ongeldig refresh-token, en faalt hard — zonder enig pad terug naar een nieuwe consent-prompt. **Reden voor doorschuiven:** geen enkele intrekkingsdetectie bestaat al ergens in de OAuth-flow (niet specifiek aan deze story); oplossen vereist een bredere aanpak (bv. detecteren-bij-401 en `hasCalendarWriteScope` dan alsnog terugzetten) die buiten deze story's scope valt.
- **Geen geautomatiseerde testdekking voor de write-sync-service** — `server/domain/calendar-sync/homework-events.ts` heeft echte vertakkingslogica (zelf-bewaking, retry-bij-401, cumulatieve-scope-detectie) en praat met een echte, betaalde externe API, maar heeft alleen eenmalige handmatige verificatie tegen productie. **Reden voor doorschuiven:** nog steeds geen testframework in het hele project (al herhaaldelijk genoteerd sinds Story 1.2).
- **Geen DB-CHECK-constraint op `homework_calendar_color_id`'s 1-11-bereik** — het bereik wordt alleen gevalideerd in `PATCH /api/settings/homework-calendar-color`; niets op schemaniveau voorkomt dat een toekomstig ander schrijfpad een waarde buiten bereik wegschrijft, die vervolgens ongewijzigd als `colorId` naar Google gaat. **Reden voor doorschuiven:** consistent met dit project's bestaande patroon — validatie zit overal alleen in de applicatielaag, nergens als DB-constraint (bv. `minutes` op `available_time_exceptions` kent ook geen CHECK ondanks de 0-1440-clamp).
- **Geen onderscheid tussen een permanent ongeldig refresh-token en een tijdelijke fout** — zowel `refreshCalendarAccessToken` als de retry-wrapper in `homework-events.ts` gooien een generieke `Error` zonder code/status, dus een toekomstige aanroeper (Epic 3+) kan "vraag opnieuw consent" niet onderscheiden van "log en probeer later opnieuw" zonder foutteksten te parsen. **Reden voor doorschuiven:** consistent met de rest van de domain-laag (die ook overal platte `Error`s gooit); en onbereikbaar zolang er geen Epic 3+-aanroeper bestaat die hier daadwerkelijk op zou reageren.

## Deferred from: code review of 1-1-project-scaffolding-inlogscherm-ui (2026-07-28)

- **`CLAUDE.md` is feitelijk onjuist** — het bestand claimt nog "no application source code", maar er staat inmiddels een echte Nuxt/SST-codebase (`app/`, `server/`, `sst.config.ts`). Al expliciet als niet-blokkerend genoteerd in Story 1.1's Dev Notes; bijwerken zodra het uitkomt.
- **Geen lint/import-boundary-handhaving** — de architectuur schrijft voor dat `app/` alleen `server/api/` mag aanroepen, nooit rechtstreeks `server/domain/`/`server/data/`, maar niets handhaaft dit mechanisch (geen ESLint-config/custom rule). Teststrategie/CI-tooling staat al expliciet als "nog niet besloten, hoort bij deze epic/story-fase" in `epics.md`'s Additional Requirements — dit hoort daarbij.

## Deferred from: code review of 2-2-dag-specifieke-afwijkingen-instellen (2026-07-31)

- **AC #2's auto-opruimregel wordt alleen vanuit de exceptie-kant afgedwongen, nooit wanneer het weekpatroon zelf wijzigt** — `updateWeekPatternDay` (Story 2.1, `server/data/availability.ts`) raakt nooit `available_time_exceptions`. Scenario: Monday-patroon = 60, exceptie op 3 augustus = 90; verhoog het Monday-patroon naar 90 — de exceptie is nu identiek aan het patroon maar blijft bestaan, inclusief de stipmarkering in de kalender, tot iemand die datum opnieuw aanklikt. **Reden voor doorschuiven (Hillebrand, 2026-07-31):** vereist twee losse acties op dezelfde weekdag om te raken — zeldzaam. Oplossen zou Story 2.1's `updateWeekPatternDay` moeten uitbreiden zodat die bij elke wijziging ook bestaande excepties voor die weekdag opruimt die nu gelijk zijn aan de nieuwe waarde — extra scope voor een randgeval.

## Deferred from: code review of 1-3-sessieverval-opnieuw-inloggen (2026-07-31)

- ~~**AC #1's API-helft is niet geïmplementeerd**~~ — **Opgepakt 2026-08-17**, na Epic 6's afronding, op verzoek van Hillebrand. Zie `1-3-sessieverval-opnieuw-inloggen.md`'s Change Log voor de implementatie (`app/plugins/sessie-verval.client.ts`, globale `$fetch`-`onResponseError`-interceptor).
- ~~**AC #1's "onopgeslagen data"-verplichting herleeft bij het eerste formulier**~~ — **Opgepakt 2026-08-17**, samen met bovenstaand punt. Zie `1-3-sessieverval-opnieuw-inloggen.md`'s Change Log (`TaakFormulier.vue`'s save-flow toont nu een handmatige inlog-link i.p.v. automatisch weg te navigeren bij een 401 tijdens opslaan).
- **De 401 uit `server/middleware/session.ts` gebruikt niet de voorgeschreven error-envelope** — de architectuur (Consistency Conventions) schrijft `{ error: { code, message } }` voor met een gedeelde vocabulaire in `server/domain/errors.ts`; de middleware gebruikt `createError({ statusCode: 401, message: 'Unauthorized' })`, wat h3's default-body oplevert. Ontstaan in Story 1.2, gemist door die review, hier alsnog gesignaleerd.
- **Cookie-churn op beschermde paden blijft bestaan** — elke anonieme request naar een beschermd pad krijgt een `Set-Cookie`, ook op een 302 of 401, waardoor die responses niet door CloudFront gecachet kunnen worden. Story 1.2's `PUBLIC_PREFIXES` loste dit alleen op voor de publieke paden. Signaleerde zich opnieuw tijdens 1.3; hangt samen met de vraag of de middleware überhaupt een sessie zou moeten minten voor anonieme bezoekers.
- **Het oorspronkelijk gevraagde pad gaat verloren bij opnieuw inloggen** — de middleware redirect naar een kale `/inloggen` en `onSuccess` landt altijd op `/`. Voldoet aan AC #1, maar wordt gebruikerszichtbaar zodra er deep-linkbare pagina's zijn (Epic 4+).

## Deferred from: code review of 1-2-google-oauth-login-met-calendar-consent (2026-07-30)

- **Migraties worden niet door de deploy toegepast, en niets detecteert drift** — `npx sst deploy` bouwt en verscheept alleen de Lambda; het toepassen van `server/data/migrations/*.sql` is een losse, handmatige twee-commando-stap in `README.md:71-78`. Niets laat de deploy falen of faalt fail-fast bij runtime als het schema achterloopt — de eerste geraakte query geeft simpelweg een 500 in productie. Het Debug Log van Story 1.2 noteert bovendien dat `drizzle-kit migrate`'s CLI de onderliggende foutmelding volledig inslikt, dus ook de handmatige stap faalt ondoorzichtig.
- **Geen automatische tests** — geen `test`-script, geen testframework in `devDependencies`, geen testbestanden. Verificatie van Story 1.2 was één handmatig doorlopen OAuth-flow tegen de dev-stage op het productiedomein. Zowel de Dev Notes van Story 1.2 ("Teststrategie is nog steeds niet vastgesteld… dit is de eerste story met voor het eerst echte te testen logica") als dit ledger markeerden dit al; hiermee is het een tweede keer doorgeschoven. Er is nu niets dat een regressie in de token-upsert of de callback-afhandeling zou vangen.
- **Sessiecookie heeft geen `maxAge`** — nergens `session.maxAge` gezet, dus h3 verzegelt met `ttl: 0` (nooit serverzijdig verlopen; een gelekte cookie blijft onbeperkt geldig en is alleen te herroepen door `SessionPassword` te roteren) én `expires` blijft `undefined` (browser-sessiecookie, dus Evelien is uitgelogd zodra ze haar browser afsluit). **Reden voor doorschuiven (Hillebrand, 2026-07-30):** Story 1.3 gaat expliciet over sessieverval; daar in één keer consistent maken met NFR5's 7-dagen-refresh-tokenverval.
- **`flowz.fyi` is hardcoded voor élke stage, inclusief de OAuth-`redirect_uri`** — `sst.config.ts:29` zet `NUXT_PUBLIC_SITE_URL` en `domain.name` zonder `$app.stage`-vertakking; `server/routes/auth/google.get.ts:18` bouwt daaruit de `redirectURL`. Een tweede stage stuurt gebruikers na consent naar de dev-site en vecht om hetzelfde CloudFront-alias. Uitbreiding van een bewuste 1.1-beslissing, nu met OAuth-impact. **Reden voor doorschuiven (Hillebrand, 2026-07-30):** er is momenteel maar één stage, dus het probleem is latent; oppakken zodra er een tweede stage bijkomt.
- **Geen lint-/import-boundary-handhaving** (herbevestigd) — `server/domain/auth/users.ts:12` is een pure pass-through die alleen bestaat om de mutatie-ownership-regel te respecteren; niets weerhoudt een volgende story ervan die laag over te slaan. Al genoteerd bij de 1.1-review, opnieuw geraakt in 1.2.

## Deferred from: code review of 3-1-taak-aanmaken-kerngegevens-met-doelmoment-berekening, Task 8 (2026-09-05)

- **Tot ~90 sequentiële live Google Calendar-aanroepen per taak-planning** — `planSessionSlots` roept `getAvailableBlocksForDate` per gescande dag aan, sequentieel. Al genoteerd als Open Question #7 in de story zelf, nu iets prominenter zichtbaar dan ingeschat. **Reden voor doorschuiven:** heroverwegen (batchen/parallelliseren) als dit in de praktijk vaker dan incidenteel misgaat — al één keer een transiënte fout van gezien tijdens live-verificatie, die bij een retry meteen slaagde.
- **`getSessionForTask` kan een verouderde, al-gepasseerde sessie teruggeven** als een geplande dag ooit stilzwijgend wordt overgeslagen zonder dat iets een herberekening triggert. **Reden voor doorschuiven:** pre-existent gedrag van het "geen sessie-afgerond-vlag, alleen datums"-model (bestond al vóór deze rework), hoort bij een bredere sessie-levenscyclus-heroverweging, niet iets wat deze diff introduceerde.
- **`detectDeadlineOverrun`/`getOpenTasksWithProgress` N+1-patroon** — wordt meermaals per request opnieuw aangeroepen zonder hergebruik tussen `detectAnyShortfall`/`detectShortfallForDateOrOverrun`/`generateDeadlineOverrunRecommendations`. **Reden voor doorschuiven:** performance, geen correctheidsprobleem; consistent met bestaande N+1-tolerantie elders in dit project (bv. `week-overview.ts` vóór zijn eigen optimalisatie-patch).
- **`ShortfallResult.availableMinutes`/`plannedMinutes` zijn gefingeerd (`0`/`overrunMinutes`) voor het deadline-overrun-geval** — representatie-smell voor toekomstige onderhouders. **Reden voor doorschuiven:** geverifieerd dat dit vandaag geen client-gericht veld of niet-gerelateerde consument (bv. `calculateStudiedrukScore`) daadwerkelijk raakt; overweeg een discriminated union bij een volgende aanraking van `shortfall.ts`.

## Deferred from: volledige-codebase-review, chunk 2 — data-laag/Calendar-integratie/shared (2026-09-06)

Chunk 2 (server/data/, server/domain/calendar-sync/, server/domain/auth/, shared/) van de volledige-codebase-review (3 agents op Opus 5, zelfde aanpak als chunk 1) leverde grotendeels **pre-existente** architecturale schuld op (niet veroorzaakt door Story 3.1 Task 8's rework). Twee concrete regressies uit die rework zelf zijn gevonden en meteen gefixed (zie 3-1's Change Log, ronde 4: `releaseStaleSessionClaim` deed een unconditionele reset i.p.v. compare-and-set; `syncHomeworkBlocksForDate` gebruikte de nieuwe "verstreken sessies tellen niet mee"-capaciteitsfunctie, waardoor het Calendar-blok voor een al-plaatsgevonden sessie werd verwijderd). De rest is hieronder vastgelegd als bekende bevindingen voor een toekomstige, gerichte opruimronde — te groot en te veel eigen productbeslissingen (welke gedragsverandering is gewenst) om in één reviewronde te patchen.

**Concurrency/locking:**
- De drie lock-tabellen (`sessionPlacementLocks`, `taskEditLocks`, `homeworkBlockSyncLocks`) stelen een verlopen lock door 'm te verwijderen en opnieuw in te voegen, zonder ownership-token — een langzame houder kan zo de nieuwe eigenaar se lock alsnog verwijderen bij het vrijgeven. Oplossing: een eigenaar-token opnemen en het vrijgeven/stelen daarop scopen (`WHERE ... AND owner = ?`), in alle drie de kopieën.
- `LOCK_STALE_MS` (30s) > `LOCK_MAX_WAIT_MS` (10s) betekent dat een wachtende aanvrager altijd opgeeft vóórdat een lock als "gestolen" mag gelden.
- `withSessionPlacementLocks` lekt de resterende locks (tot de stale-window verstrijkt) als één release in de vrijgave-lus faalt.
- `createTaskAndSessions`/`recalculateTaskPlanning` nemen de lock pas ná de planningsfase (die de capaciteit al heeft "vastgesteld") — de TOCTOU die de lock moet dichten, blijft dus openstaan. `insertSessionsForTask`/`applyRecalculatedSessions` nemen zelfs helemaal geen lock.

**OAuth/Calendar-integratie:**
- `refreshCalendarAccessToken` heeft geen concurrency-controle — meerdere gelijktijdige 401's kunnen elk een eigen refresh-grant vuren, met racende schrijfacties naar hetzelfde token-veld; een `refresh_token` in de respons wordt genegeerd (rotatie niet ondersteund); `invalid_grant` (herroepen consent) wordt niet onderscheiden van een tijdelijke fout, dus geen her-consent-pad.
- Geen paginering op `calendarList.list`/`events.list` — een tweede pagina agenda's of events wordt stilzwijgend genegeerd.
- Geen retry/backoff op 429/5xx van de Calendar-API — een tijdelijke Google-storing wordt hetzelfde behandeld als een permanente fout.
- `getEventsForCalendar` fabriceert een dagvullend event bij een ontbrekend start/eind-tijdstip i.p.v. de rij te droppen.
- `timeMax`/`timeMin` op vaste 23:59/0:00-uren i.p.v. de volgende dag exclusief — verliest de laatste minuut van elke dag, en `amsterdamLocalToUtcIso` se DST-aanname ("16:00 ligt nooit dicht bij de omschakeling") klopt niet meer voor de huidige aanroepers die uur 0/23 doorgeven.

**Databeveiliging:**
- `getUserById` geeft een `User` terug met ontsleutelde OAuth-tokens erin, zonder geredigeerd/domain-gericht type — een onoplettende `return user` ergens zou Eveliens Google-credentials naar de client kunnen lekken.
- `crypto.ts` heeft geen sleutelversie/rotatiepad — een `TokenEncryptionKey`-rotatie maakt alle opgeslagen tokens permanent onleesbaar.

**Dode AD-10-restanten (bevestigd zonder aanroepers):**
- `server/data/availability.ts` is na de ronde-3-opschoning zelf volledig dood (geen enkele aanroeper meer) — de eigen bestandscommentaar noemt nog steeds twee routes die niet meer bestaan.
- `availableTimePatterns`/`availableTimeExceptions`/`availabilityWriteLocks`/`dismissedConflicts`-tabellen in `schema.ts` + hun beschrijvende commentaren beweren nog steeds dat de scheduling-engine ze leest.
- `shared/types/availability.d.ts`'s `UpdateExceptionResponse` en `shared/types/shortfall.d.ts`'s persistentie-claim beschrijven allebei een model dat niet meer bestaat.
- `calculateActualAvailableMinutes`/`isTimedEvent`-cluster in `actual-availability.ts` (Story 6.6/8.2, vervallen) heeft nul aanroepers.

**Overig:**
- Meerdere `server/api/*`-routes lezen `server/data/` rechtstreeks (mutatie-ownership-conventie zegt dat dit via `server/domain/` moet) — grotendeels lees-only, maar wel inconsistent met hoe andere routes het doen.
- `deleteTaskAndSessions` ruimt `homeworkCalendarBlocks`/hun Google-events niet op — een verwijderde taak laat een weeskind-agendablok achter.
- `homeworkCalendarBlocks` heeft geen taak/sessie-referentie, alleen tijdstip-matching — een greedy dichtstbijzijnde-match kan in theorie taak A se event met taak B se titel overschrijven.
- Schema-commentaren op `sessions`/`sessionLogs`/`subtasks` beschrijven nog het oude 1-sessie-per-taak-model resp. een niet-bestaande kolom.

**Reden voor doorschuiven:** dit is een grote, samenhangende opruimactie (concurrency-herontwerp, OAuth-robuustheid, credential-redactie, een schema-migratie voor de dode AD-10-tabellen) die eigen productbeslissingen vergt (bv. hoe een herroepen Google-consent aan Evelien getoond moet worden) — niet iets voor een losse reviewronde. Aanbevolen als een aparte, toekomstige story/epic.

## Deferred from: volledige-codebase-review, chunk 3 — API-routes/middleware/OAuth-routes (2026-09-06)

Chunk 3 (server/api/**, server/middleware/session.ts, server/routes/auth/*) leverde de meeste bevindingen tot nu toe op (139 findings-regels over 3 agents), grotendeels **pre-existente** architecturale schuld. De hoogste-risico items zijn direct gefixed (zie 3-1's Change Log): het week-suggestie-accept-route paste blindelings `recommendations[0]` toe i.p.v. de door de client geziene aanbeveling (kon een taak laten vervallen zonder dat te bevestigen), de Google-write-scope-upgrade kon het publieke-computer-vinkje van een ander account overnemen bij een accountwissel in Google's kiezer, vier GET-routes gaven h3's rauwe foutvorm i.p.v. de error-envelope, `home/plan.get.ts` slikte fouten zonder enige logging, de sessie-middleware matchte publieke paden ongeankerd op prefix, `day/shortfall.post.ts` behandelde een kapotte JSON-body stilzwijgend als leeg verzoek, het verwijderen van een verborgen agenda-titel decodeerde/trimde de routeparameter niet (mismatch met hoe POST opslaat), en `school-sessions.post.ts` had geen bovengrens/dedupe op de batch en kon een net-aangemaakte taak wees achterlaten bij een mislukte regel (nu compenserend verwijderd). Rest hieronder vastgelegd — te groot/te veel eigen productbeslissingen voor deze ronde.

**Mutatie-ownership-conventie (routes die rechtstreeks `server/data/` muteren i.p.v. via `server/domain/`):**
- `subtasks/[id]/done.post.ts`/`later.post.ts` — muteren rechtstreeks, en triggeren geen `recalculateTaskPlanning`. Onduidelijk of dat laatste een bug is (moet "deeltaak afgerond" de sessieplanning beïnvloeden?) — productbeslissing, geen pure bugfix.
- `sessions/[sessionId]/stop.post.ts`/`heartbeat.post.ts` — muteren rechtstreeks vanuit de route.
- `tasks/[id]/reopen.post.ts` — twee-staps orkestratie (`reopenTaskWithRemaining` + `recalculateTaskPlanning`) rechtstreeks in de route i.p.v. in een `domain/tasks/reopen-task.ts`, niet atomair (een falende tweede stap laat de taak halverwege heropend), en ondersteunt geen `droppedAt`-taken.
- **Reden voor doorschuiven:** een consistente migratie naar domain-services voor al deze routes is mechanisch maar breed (5+ routes); de reopen-atomiciteit en de subtask-recalc-vraag vergen een expliciete keuze van Hillebrand.

**AD-1/AD-6 — scheduling-logica en notificatie-vorm:**
- `GET /api/tasks/[id]` muteert als bijeffect (`finalizeStaleSessionIfNeeded`) — een `GET` die muteert, tegen het project se eigen expliciete precedent in (`startup-check.post.ts`'s commentaar: mutaties horen bewust op `POST`, nooit op `GET`). De onderliggende claim is al atomair (compare-and-set, ronde 4), dus geen dubbel-log-risico meer — wel een architecturale inconsistentie.
- `home/plan.get.ts` berekent scheduling-afgeleide waarden (`remainingMinutesToday`, sessie-eindtijd) rechtstreeks in de route i.p.v. in `server/domain/scheduling/`.
- `buildShortfallNotification` (AD-6's voorgeschreven `Notification`-vorm) heeft nul aanroepers — elke UJ-6/8-route retourneert een eigen bespoke DTO in plaats daarvan. Een falende Calendar-read op deze routes geeft de technische `internal_error`-envelope, niet de "schuldvrije Notification" die AD-10 daarvoor voorschrijft.
- **Reden voor doorschuiven:** AD-6 daadwerkelijk laten naleven is een bredere response-vorm-migratie over meerdere routes tegelijk, geen geïsoleerde fix.

**Twee incompatibele `envelope()`-helpers:** ongeveer de helft van de ~38 routebestanden gebruikt `envelope(event, statusCode, code, message)` (zet de status zelf), de andere helft `envelope(statusCode, code, message)` (accepteert een ongebruikte `statusCode`, vereist een losse `setResponseStatus`-regel ernaast). Werkt vandaag overal correct, maar is een val voor de eerstvolgende nieuwe route. **Reden voor doorschuiven:** mechanisch maar breed (~15 bestanden); geen actieve bug, puur consistentie.

**Ontbrekende boven-/ondergrenzen op invoer:**
- `actualMinutes`/`remainingHours` in `sessions/[sessionId]/replan.post.ts`, `tasks/[id]/reopen.post.ts` en `school-sessions.post.ts` hebben geen bovengrens — een absurde waarde (bv. 999 uur) drijft `recalculateTaskPlanning` tot honderden sessies/Calendar-writes.
- `day/shortfall.post.ts` en de drie `recommendations/[id]/*.post.ts`-routes accepteren elke syntactisch geldige datum (verleden of ver in de toekomst), inconsistent met de week-routes die zich bewust tot het venster `vandaag..+6` beperken.
- **Reden voor doorschuiven:** een zinnige bovengrens is een productkeuze (welk aantal uren is nog realistisch?), niet een pure technische fix.

**Overig, lager risico:**
- `week/[date]/suggestion/accept.post.ts` gebruikte `detectShortfallForDate` i.p.v. `detectShortfallForDateOrOverrun` — **meegenomen in de chunk-3-fix hierboven**, samen met het id-vereiste.
- `day/energy-proposal/confirm.post.ts` heeft geen idempotency-token/proposal-hash — een dubbele POST (of een "probeer opnieuw" ná een gedeeltelijke mislukking) past een ander, vers herberekend voorstel toe op een al gedeeltelijk gemuteerde staat. Vergt een expliciet ontwerp (hash meesturen, 409 bij mismatch).
- `scheduling/startup-check.post.ts` en de accept-routes (`day/shortfall/recommendations/[id]/accept.post.ts`, `week/.../accept.post.ts`) hebben geen lock tegen twee gelijktijdige aanroepen van dezelfde gebruiker (dubbele klik/twee tabbladen) — dezelfde bredere concurrency-heroverweging als chunk 2's lock-ownership-token-punt.
- Titels met een `/` kunnen nooit via `DELETE /api/settings/hidden-calendar-titles/[title]` verwijderd worden (het pad-segment kan een losse `/` niet dragen) — vergt een andere routevorm (query/body i.p.v. path-segment), niet opgelost in deze ronde.
- Drie deels overlappende task-read-routes (`tasks/[id].get.ts`, `tasks/[id]/detail.get.ts`, `tasks/[id]/edit.get.ts`) — `detail.get.ts` is een kandidaat om te laten vervallen (subset van `edit.get.ts`), maar dat is een API-contractwijziging die de client moet volgen.
- `tasks.get.ts` documenteert een `status`-queryparameter die nooit gevalideerd/gelezen wordt (`?status=done` geeft alsnog open taken terug, stil).

**Reden voor doorschuiven (algemeen):** deze chunk levert vooral consistentie- en robuustheidsschuld op die ofwel een brede mechanische migratie vergt (envelope-unificatie, domain-service-verhuizing) ofwel een productbeslissing (welke boven­grenzen, welk idempotency-ontwerp, welke UX voor "instellingenwijziging" bij een dropped taak) — niet iets voor een losse reviewronde. Aanbevolen als vervolg op chunk 2's al aangekondigde toekomstige opruim-epic.

**Ronde 2 (chunk 3 her-review, 2026-09-06):** de 3 agents zijn opnieuw ingezet op exact de gewijzigde bestanden. Twee resterende gaten zijn alsnog gefixed: (a) de `verruimen`-tier-guard was per ongeluk verwijderd bij de eerste fix van `week/[date]/suggestion/accept.post.ts` — hersteld, en de route retourneert nu een 404 i.p.v. een stille 200 op een verlopen/ongeldig aanbeveling-id; (b) `week-overview.ts`'s `buildWeekDay` gebruikte nog `detectShortfallForDate` i.p.v. `detectShortfallForDateOrOverrun`, waardoor de eerdere overrun-fix in de accept-route onbereikbare code was — nu gesynchroniseerd; (c) `hidden-calendar-titles/[title].delete.ts`'s `decodeURIComponent` stond buiten de try/catch (nieuwe regressie uit de eerste fix) — nu gewrapt; (d) `school-sessions.post.ts`'s compenserende rollback dekte alleen de `catch`, niet de twee `continue`-paden ná een geslaagde `createTask()` — nu via een gedeelde `failRow`-helper op alle drie de paden toegepast; de dubbele-`taskId`-check is bovendien van een hele-batch-400 naar een per-regel-afwijzing veranderd (consistent met het bestand se eigen "per-regel resultaat"-contract; dubbele `rowId` blijft terecht een hele-batch-400, want dat is altijd een client-bug).

Twee gedeprioriteerde items kregen van de agents zelf een hogere urgentie dan de eerste inschatting: **`tasks/[id]/reopen.post.ts`'s non-atomaire twee-staps orkestratie** (kan een taak in een stille, corrupte tussentoestand achterlaten — `completedAt: null` met verouderde/geen sessies — op een pad dat Evelien daadwerkelijk gebruikt) en **`scheduling/startup-check.post.ts`'s ontbrekende concurrency-guard** (het enige uitgestelde concurrency-geval dat zonder enige dubbele gebruikersactie afgaat — twee tabbladen of een herlaad tijdens een trage Calendar-aanroep volstaat, en kan tot een ongewilde taak-`vervallen` leiden). Beide zijn relatief geïsoleerde fixes (een `domain/tasks/reopen-task.ts` naar `delete-task.ts`'s precedent, resp. een `lastStartupCheckAt`-guard) — aanbevolen als eerste twee items van een vervolgronde, vóór de bredere envelope-/domain-service-migratie.

## Deferred from: volledige-codebase-review, chunk D — frontend deel 1 (2026-09-06)

Chunk D (`app/pages/schoolsessies.vue`, `app/pages/index.vue`, `app/pages/sessie/actief.vue`, `app/pages/herstel/tekort-oplossen.vue`). Hoogste-risico bevindingen direct gefixed: `tekort-oplossen.vue` kon Evelien op drie onafhankelijke manieren permanent vastzetten achter zijn eigen `onBeforeRouteLeave`-guard (een lege aanbevelingslijst, een lijst met uitsluitend "tijd verruimen"-kaarten, een hangende `$fetch` zonder timeout) — een `stuck`-detectie met een expliciete "Terug naar Home"-uitweg toegevoegd, plus een 15s-laadtimeout. `afwijzen` miste een `busyId`-guard (kon met `accepteren`/`controlerenOpnieuw` racen en een verouderde lijst laten winnen) — nu hetzelfde single-flight-patroon als de andere twee acties. Een niet-opgeruimde `setTimeout` kon de gebruiker na een geslaagde navigatie alsnog terugslepen naar Home — nu bewaard en opgeruimd bij unmount. `sessie/actief.vue`: een in-place route-wissel tussen twee `/sessie/actief`-URL's liet de pagina blanco vallen (de `watch(taakId)` deed niets in exact het scenario waarvoor de eenmalige setup-guard bestaat) — nu navigeert de watcher zelf ook. Dubbele-tap-guards op de subtaak-knoppen, een spam-fix op "Later" bij een enkele resterende subtaak, `stopSessie` wacht nu de navigatie af vóórdat de lokale state geleegd wordt (voorkomt een blanco flits), en een gepauzeerde sessie stuurt nu alsnog een stop-signaal bij een in-app-navigatie (dekte voorheen alleen tabblad-sluiten af). `index.vue`: `today` was een eenmalige `const` (fout na middernacht bij een open tabblad) — nu een reactieve waarde die elke minuut hercheckt; een 0-minuten/omgekeerd Calendar-event verdween volledig i.p.v. in de "buiten venster"-tekstlijst te vallen — gefixed; de (bewust niet-afgewachte) opstart-check kon een gebruiker die net "Start sessie" had getikt alsnog terugsturen — nu met een route-check. `schoolsessies.vue`: elke reload van de takenlijst (heropenen, een gedeeltelijk mislukte batch) wiste stilzwijgend nog niet-opgeslagen invoer van ándere rijen — nu een snapshot/herstel-mechanisme; "Andere taak zoeken" bood dezelfde taak twee keer aan (als vaste vandaag-rij én als zoekresultaat) — nu uitgesloten; de "Andere taak"-reset liet stale minuten/foutmeldingen achter — nu volledig gereset; het heropen-paneel accepteerde invoer die de server toch zou afwijzen (minuten > 59, totaal 0) — nu dezelfde validatie als het gewone resterende-tijd-veld; een gedeeltelijk mislukte batch liet `totalMinutes` van de geslaagde rijen verouderd staan — nu een herlaad ná een gedeeltelijke mislukking (met behoud van de nog-mislukte rijen se invoer).

**Niet gefixed, bewust:**
- **AD-6/AD-10: een falende Calendar-read komt bij deze pagina's aan als de technische error-envelope, nooit als een "schuldvrije" Notification.** Zelfde structurele gat als eerder al vastgesteld op de backend (`buildShortfallNotification` heeft nul aanroepers) — een frontend-fix zonder de backend-kant zou alleen een andere technische foutmelding tonen, geen echte Notification. Hoort bij dezelfde toekomstige AD-6-migratie als chunk 3's bevinding.
- **Overlappende Calendar-blokken op de homepage-dagview occluderen elkaar volledig** (`index.vue`) — een tweede event op hetzelfde tijdvak wordt onzichtbaar. Een echte fix vergt een lane-toewijzingsalgoritme (interval-overlap-detectie + horizontale verdeling); een layoutverandering, geen kleine patch.
- **`is401` is letterlijk gedupliceerd in vier bestanden** (`index.vue`, `schoolsessies.vue`, `tekort-oplossen.vue`, `sessie/starten.vue`) — de derde-consument-drempel is allang gepasseerd. Mechanische refactor naar een gedeelde composable, geen gedragswijziging; laag risico maar buiten deze ronde se scope (bugfixes, geen opschoning).
- **`sessie/actief.vue`'s `stopSessie`/`stuurStopBeacon` hebben nog steeds geen 401-afhandeling** (heartbeat en de subtaak-acties zijn nu wel gefixed). `stopSessie` navigeert sowieso altijd door naar het overzicht, ongeacht of de server-call slaagde — een 401 daar zou de gebruiker alsnog op een oude sessie-log laten kijken zonder dat de server iets persisteerde. Bewust ongemoeid gelaten: de navigatie zelf omzeilen zou een grotere UX-herziening van de Stop-flow vergen dan deze ronde se scope.
- **Entry- vs. follow-up-detectie-horizon-mismatch in `tekort-oplossen.vue`** (Architecture Auditor): `loadShortfall()` scant de hele horizon (`detectAnyShortfall`), terwijl `accepteren`/`afwijzen`/`controlerenOpnieuw` zich tot één specifieke datum beperken. Een "Tekort opgelost!"-navigatie na het oplossen van dié ene dag kan zo een ander, nog openstaand tekort op een andere dag verbergen. De nieuwe `stuck`-uitweg vangt de ergste consequentie (permanent vastzitten) af, maar de onderliggende contractmismatch vergt een productbeslissing (welke dag toont dit scherm precies?) — buiten deze ronde se scope.

**Reden voor doorschuiven (algemeen):** de niet-gefixte items zijn ofwel een grotere architecturale migratie die al elders is aangekondigd (AD-6), een layout-/algoritme-herontwerp, een pure opschoning zonder gedragswijziging, of een productbeslissing over gewenst gedrag — niet losstaande bugs die een geïsoleerde patch verdienen.

**Ronde 3 (chunk D her-review, 2026-09-06):** de 3 agents zijn opnieuw ingezet op exact de gewijzigde bestanden. Eén significante zelf-geïntroduceerde regressie gevonden en teruggedraaid (Architecture Auditor): ronde 2's "stuur een stop-beacon bij het wegnavigeren van een gepauzeerde sessie"-fix bleek de bestaande server-side terugvalroute (`finalizeStaleSessionIfNeeded`, `session-heartbeat-fallback.ts`) juist te saboteren — die route herkent een verweesde sessie aan `stoppedAt === null` + een verouderd heartbeat-moment en logt dan alsnog de bestede tijd via `replanAfterSession`. Door zelf al `stoppedAt` te zetten (zonder minuten te loggen) sloot de ronde-2-fix die terugvalroute voorgoed af — bestede tijd van een gepauzeerde, verlaten sessie werd zo niet "eindelijk gestopt" maar **permanent en stilzwijgend weggegooid** (0 minuten gelogd, geen `replanAfterSession`), erger dan het `stoppedAt: null`-gat dat de fix dacht te dichten. Teruggedraaid naar het oorspronkelijke gedrag (stilzwijgend toestaan, geen stop-signaal) — de bestaande heartbeat-fallback dekt dit scenario al correct af zodra Evelien de taak ooit opnieuw opent. Overige kleinere fixes deze ronde: een tekstfout ("hierboven" → "hieronder") in de stuck-notice, dangling 15s-timers in `withTimeout` nu opgeruimd via `finally`, `skipStartupRedirectOnce` nu onvoorwaardelijk geconsumeerd bij Home-mount i.p.v. alleen binnen de (kan-falen) opstart-check-respons-watcher, en `isIntentionalLeave` naar de top van `actief.vue` verplaatst (TDZ-risico) plus een `try/finally` op de nieuwe `watch(taakId)`-redirect (voorkomt dat de guard permanent uitgeschakeld blijft als die navigatie ooit wordt afgebroken).

**Niet gefixed, bewust — pre-existent, niet door chunk D geïntroduceerd, hoogste-prioriteit resterend item:** hetzelfde `stoppedAt`-zonder-minuten-probleem geldt ook voor de al langer bestaande `beforeunload`-beacon (`stuurStopBeacon`, Story 4.5 AC #2, gekoppeld aan `window.addEventListener('beforeunload', ...)`) — en dit raakt niet alleen een gepauzeerde sessie, maar ook een gewone LOPENDE sessie waarvan het tabblad gesloten/herladen wordt. `stopSessie()`'s eigen `/stop`-aanroep is wél veilig, maar uitsluitend omdat die altijd gevolgd wordt door een navigatie naar `/sessie/overzicht`, dat de echte bestede tijd alsnog via `POST /api/sessions/{id}/replan` post — de `beforeunload`-beacon heeft geen zo'n vervolgstap, dus is een kale `stoppedAt`-write zonder meer. Twee samenhangende AC's (4.5 AC #2's beacon, AC #3's heartbeat-fallback) spreken elkaar hiermee tegen: de beacon "wint" (sluit de fallback af) maar levert zelf niets op. Twee coherente oplossingsrichtingen, geen van beide in deze ronde uitgevoerd: (1) `POST /api/sessions/{id}/stop` zelf laten finaliseren (bestede tijd loggen + `replanAfterSession` aanroepen, i.p.v. alleen `stoppedAt` te stempelen) — dan wordt de beacon overal de betere uitkomst; of (2) de `beforeunload`-beacon helemaal laten vervallen en elke tabblad-sluiting aan de heartbeat-fallback overlaten, net als de zojuist teruggedraaide gepauzeerde-navigatie-route nu al doet. Vergt een productbeslissing (welke AC weegt zwaarder), niet een geïsoleerde patch — apart vervolgstuk.

## Deferred from: volledige-codebase-review, chunk E — frontend deel 2 (2026-09-06)

Chunk E (`app/pages/week/index.vue`, `sessie/overzicht.vue`, `taken/[id]/index.vue`, `herstel/reden-kiezen.vue`, `taken/index.vue`, `herstel/energie-voorstel.vue`, `sessie/starten.vue`, `inloggen.vue`, `instellingen/index.vue`, `taken/[id]/bewerken.vue`, `taak/nieuw.vue` — de resterende 11 pagina's). Hoogste-risico bevindingen direct gefixed: `sessie/starten.vue`'s Start-knop kon op het gouden pad (via Home) permanent een stille no-op worden zodra de achtergrond-fetch faalde — `heeftBruikbareData` onderdrukte dan élke foutstaat; nu een eigen `startFout`-melding bij de knop, de retry probeert het nu ook ná een eerder mislukte fetch (was voorheen uitgesloten), en `sessie-start-taak` wordt eenmalig geconsumeerd (zelfde precedent als `taken/[id]/index.vue`) i.p.v. een verwijderde taak stil te blijven tonen. `sessie/overzicht.vue` had helemaal geen leave-guard — het hamburgermenu liet de ingevulde "resterende tijd" stilzwijgend verdwijnen zonder ooit `/replan` aan te roepen (AC #3's hele doel); nu een `onBeforeRouteLeave` die alsnog probeert te versturen, plus een busy-guard tegen een dubbele klik op "Terug naar hoofdscherm" en de juiste state-na-navigatie-volgorde (zelfde les als chunk D's `sessie/actief.vue`). `week/index.vue`: een mislukte ververs-call ná een geslaagde mutatie werd getoond als "kon niet doorvoeren" en nodigde uit tot een retry die een tweede, mogelijk destructieve aanbeveling zou toepassen — nu eigen try/catch per stap; de globale `busyDate`-lock disablede voorheen alleen de eigen rij-knop, niet de andere dagen se knoppen (nu `!!busyDate` overal); een 400 door een over-middernacht-verouderde datum herlaadt nu de week i.p.v. een blijvend-herhalende foutmelding te tonen; alle drie de `$fetch`-aanroepen kregen dezelfde `withTimeout`-bescherming als `tekort-oplossen.vue`. `herstel/reden-kiezen.vue`: de zojuist ingetypte "hoeveel tijd heb je nog"-override werd weggegooid door `/herstel/tekort-oplossen` daarna blindelings een lege-lichaam-hele-horizon-scan te laten doen — nu draagt een gedeelde `shortfall-seed`-useState de al berekende respons door, eenmalig geconsumeerd; een oplossende override (shortfallMinutes ≤ 0) navigeert nu direct naar Home i.p.v. via een overbodige tussenstop; beide reden-kaarten zijn nu uitgeschakeld tijdens het versturen; timeout-bescherming toegevoegd. `herstel/energie-voorstel.vue`: de 2500ms-redirect-timer kon na een unmount alsnog gearmd worden (de vorige fix ruimde alleen een al-lopende timer op, niet een die pas ná een `await` gezet wordt) — nu een `isMounted`-check vóór het zetten; het "niets aangepast maar wel een `notShortenedReason`"-geval toonde ten onrechte een levende Bevestigen-knop i.p.v. "Terug naar Home" — conditie gecorrigeerd; retry-link kreeg een re-entrancy-guard en timeout. `taken/[id]/bewerken.vue`: dezelfde stille-bounce-zonder-flashmelding-en-zonder-`replace:true` als `taken/[id]/index.vue` ooit had (en zelf al fixte) — nu hetzelfde patroon toegepast, plus een laadskeleton i.p.v. een volledig blanco scherm tijdens de fetch. `inloggen.vue`: een bfcache-restore (iOS Safari/Firefox) na het afbreken van de Google-flow liet de inlogknop permanent uitgeschakeld staan — een `pageshow`-listener herstelt 'm nu; ontbrekende `loggedIn`-redirect toegevoegd (enige pagina zonder). `taken/index.vue`: dezelfde voortgangsbalk-clamp toegepast die `taken/[id]/index.vue` al had (twee weergaves van dezelfde data, één ongeclampt). `instellingen/index.vue`: panelen zitten nu in een `KeepAlive` i.p.v. bij een tabwissel te unmounten — voorkwam dat een lopende auto-save (bv. de beschikbare-tijd-agenda, AD-10's enige bron) zijn resultaat naar een vernietigde component-instantie zou schrijven. `taak/nieuw.vue` kreeg de ontbrekende `loggedIn`-guard (enige pagina zonder). `TaakFormulier.vue`'s `setTimeout(goBack, 800)` wordt nu opgeruimd bij unmount. `shared/types/week.d.ts`'s stale/foutieve commentaar over de accept-route se body gecorrigeerd, plus een ontbrekend `WeekSuggestionAcceptInput`-type toegevoegd. Typecheck/build/deploy/live-smoketest geslaagd.

**Niet gefixed, bewust:**
- **AD-6/AD-10: dezelfde Calendar-fout-als-technische-envelope als eerdere chunks** (`week.get.ts`, `energy-proposal.post.ts`) — `buildShortfallNotification` blijft consumer-loos. Hoort bij de al aangekondigde toekomstige AD-6-migratie.
- **`is401`/`loggedIn`-boilerplate nu 13-14× gedupliceerd** (was 5× ná chunk D) — de `sessie-verval.client.ts`-plugin dekt de 401-afhandeling feitelijk al globaal af; deze per-pagina-kopieën zijn grotendeels overbodige verdubbeling, geen aparte bug. Mechanische refactor (één route-middleware + één gedeelde fetch-composable), buiten deze ronde se scope (bugfixes, geen opschoning).
- **`energy-proposal/confirm.post.ts`'s ontbrekende idempotency** — zelfde klasse als de al eerder gedeferrede server-side bevinding (chunk 3): een verloren respons ná een geslaagde server-side toepassing nodigt tot een retry die de her-berekende voorstel een tweede keer toepast. Vergt een proposal-token/hash-ontwerp, geen geïsoleerde patch.
- **`sessie/overzicht.vue`'s afwijkingsdrempel (AC #2) is client-side herimplementeerde scheduling-logica** (AD-1 vergt server-autoriteit) — een tweede client zou een andere verdict kunnen tonen op identieke data. Productbeslissing (waar hoort deze regel te leven), niet een bugfix.
- **Vier/vijf verschillende loading/error/empty-idiomen over de lijst-achtige pagina's** (`week/index.vue`, `taken/index.vue`+`taken/[id]/index.vue`, `herstel/energie-voorstel.vue`, `sessie/starten.vue`) — consistentie-opschoning, geen actieve bug.
- **`sessie/overzicht.vue` negeert `/replan`'s `{ ok, completed }`-signaal** — een net-afgeronde taak toont geen bevestiging; kleine UX-verbetering, geen bugfix.

**Reden voor doorschuiven (algemeen):** zoals bij chunk 3/D — brede mechanische migraties, productbeslissingen over gewenst gedrag, of pure consistentie-opschoning zonder gedragswijziging. Niet losstaande bugs die een geïsoleerde patch verdienen.

**Ronde 2 en 3 (chunk E her-review, 2026-09-06):** de 3 agents zijn twee keer opnieuw ingezet op exact de gewijzigde bestanden. Eén significant zelf-geïntroduceerd probleem gevonden en gefixed (release-blocker): de `sessie-verval.client.ts`-plugin riep `useUserSession().clear()` niet aan bij een 401, waardoor de client-side `loggedIn`-status stil bleef hangen — `inloggen.vue`'s nieuwe `loggedIn`-guard stuurde dan direct terug naar `/`, die opnieuw 401't: een `/` ↔ `/inloggen`-lus die het inlogscherm zelf onbereikbaar maakte, precies op het AD-9/UJ-10-pad (sessieverval op een openbare computer) dat dit moest bedienen. Gefixed door `session.value` synchroon te legen vóór de redirect (in plaats van te wachten op `clear()`'s eigen DELETE-round-trip). Twee eigen review-rondes bleken elkaar bovendien tegen te spreken over de `herstel/reden-kiezen.vue` → `tekort-oplossen.vue`-datadoorgifte: ronde 2 droeg bewust alleen de datum door (om de override niet inconsistent te laten zijn met wat accept/reject/recheck zouden herberekenen), maar dat maakte het hoofdpad van de hele "te weinig tijd"-reis kapot — een override-loze herberekening bij aankomst vindt per definitie geen tekort, dus de pagina toonde meteen "Tekort opgelost!" zonder ooit een aanbeveling te tonen. Teruggedraaid naar ronde 1's aanpak (de volledige, override-verwerkte respons doorgeven) — de inconsistentie die ronde 2 probeerde te voorkomen is een bestaand, niet door deze reviewronde veroorzaakt architecturaal gat (zie hieronder), en het erger maken om het beter te maken was de verkeerde ruil. Overige ronde-2/3-fixes: `sessie/overzicht.vue`'s leave-guard stuurde bij een ongeldige resterende-tijd-invoer de HELE payload (incl. de altijd-geldige bestede tijd) niet — nu wordt alleen het ongeldige veld op `null` gezet (server behandelt dat al als "ongewijzigd"); een dubbele-navigatie-race tussen de knop en de guard opgelost via een gedeeld idempotent `ensureReplanSubmitted()`-gat i.p.v. een vlag die de hele wachttijd "aan" bleef staan; dangling timers opgeruimd; `week/index.vue`'s `accepteren` kreeg ook 404- en timeout-afhandeling (herladen i.p.v. een dode "probeer opnieuw" op een niet-meer-geldige of onzeker-toegepaste aanbeveling); `herstel/energie-voorstel.vue`'s confirm-timeout toont nu een neutrale "we weten het niet zeker"-boodschap i.p.v. een levende Bevestigen-knop op een niet-idempotente route; de `KeepAlive`-fix voor de instellingen-tabbladen is teruggedraaid (maakte de beschikbare-tijd-agenda voor de rest van de sessie stil verouderd — een slechtere ruil dan de zeldzame mid-save-race die het moest oplossen). Typecheck/build/deploy (3x, na elke ronde)/live-smoketest geslaagd.

**Bevestigd, niet opgelost — grotendeels bestaand architecturaal gat, met één nuance (Architecture Auditor, ronde 4-verificatie):** `availableHoursOverride`/`availableMinutesOverride` (de "hoeveel tijd heb je vandaag nog"-invoer op `herstel/reden-kiezen.vue`) wordt nergens doorgegeven aan `/api/day/shortfall/recommendations/{id}/accept|reject|recheck` — die routes herberekenen het tekort altijd override-loos vanuit de live Calendar-beschikbaarheid (`shortfall.post.ts`'s eigen commentaar noemt dit een bewuste scope-grens). Vóór chunk D/E deed `tekort-oplossen.vue` zelf al een override-loze `detectAnyShortfall`-scan bij binnenkomst — de getoonde lijst en de mutatie-routes deelden dus altijd dezelfde (override-loze) basis, intern consistent, hooguit de verkeerde dag. De `shortfallSeed`-doorgifte (chunk E) maakt de EERSTE weergave nu wél override-bewust, wat de vervolg-mutatie-inconsistentie concreet zichtbaar maakt op twee specifieke paden: (1) accepteren van een geseede aanbeveling kan `accept.post.ts` een override-loze her-detectie laten teruggeven die geen tekort meer vindt (`{shortfallMinutes: 0, recommendations: []}`) — de pagina toont dan "Tekort opgelost!" zonder dat er iets is toegepast; (2) of de her-detectie vindt een ánder tekort, de geseede aanbeveling-id zit niet in de nieuwe lijst → 404 → `loadShortfall()` herlaadt stilzwijgend een andere dag se tekort. Praktisch: Evelien krijgt effectief precies één bruikbare actie per bezoek aan dit scherm. Een echte fix vergt het doorgeven van de override aan die drie routes (of het teruggeven van een `setExceptionForDate`-achtige persistentie voor déze ene dag), een productbeslissing die buiten deze bugfix-ronde valt.

## Deferred from: volledige-codebase-review, chunk F — components/composables/plugins (2026-09-07)

Chunk F (`app/components/TaakFormulier.vue`, `InstellingenBeschikbareTijd.vue`, `InstellingenVerborgenAgendaItems.vue`, `HamburgerMenu.vue`, `InstellingenUiterlijk.vue`, `app/composables/useTheme.ts`, `app/plugins/sessie-verval.client.ts`) — het laatste stuk van de volledige-codebase-reviewcampagne. Hoogste-risico bevinding: de globale 401-plugin liet `skip-sessie-verval-redirect` zowel de redirect ALS de client-side sessie-`clear()` overslaan — `TaakFormulier.vue` zet die vlag tijdens het opslaan (om onopgeslagen data zichtbaar te houden bij een verlopen sessie), waardoor `loggedIn` client-side ten onrechte `true` bleef staan. Gevolg: elke volgende achtergrond-aanroep vanuit dat formulier (bv. het vak-veld wijzigen) triggerde alsnog een navigatie weg van de nog-zichtbare, onopgeslagen data, én de eigen "Naar het inlogscherm"-link in het formulier botste op `inloggen.vue`'s `loggedIn`-guard en bounce'te naar Home — dezelfde soort lus als chunk E's release-blocker, nu via een tweede ingang. Gefixed door de twee concepten te scheiden: de sessie wordt nu ALTIJD geleegd bij een 401, alleen de REDIRECT is optioneel; `TaakFormulier.vue`'s eigen `fetchNeedsSuggestions` navigeert niet meer weg zolang `sessionExpired` al actief is; `skipSessieVervalRedirect` blijft nu `true` zolang `sessionExpired` dat is (was voorheen onvoorwaardelijk teruggezet op `false`). Tweede significante bevinding: `HamburgerMenu.vue`'s "Uitloggen"-item is bewust een rauwe `<a href="/auth/logout">` (nodig voor de server-side cookie-invalidatie) en omzeilt daarmee elke `onBeforeRouteLeave`-guard — inclusief `sessie/overzicht.vue`'s guard uit chunk E, die specifiek bestond om de ingevulde resterende tijd bij het weggaan alsnog te versturen. Gefixed met een `beforeunload`/`sendBeacon`-fallback op die pagina (zelfde precedent als `sessie/actief.vue`'s bestaande `stuurStopBeacon`), die zowel een tabblad-sluiting als deze specifieke rauwe-link-navigatie afvangt. Overige fixes: `TaakFormulier.vue` toont nu de daadwerkelijke serverfoutmelding i.p.v. een generieke tekst, kreeg ontbrekende client-side bovengrenzen (sessieduur max 480 min, max 50 deeltaken, deeltaaknaam max 100 tekens — matcht de server) zodat een volledig ingevuld formulier niet meer met een onbruikbare generieke fout kan stranden, en kreeg een `onBeforeRouteLeave`-guard voor een browser-terug-gebaar op een gewijzigd formulier (was alleen bereikbaar via de Sluiten-knop). `InstellingenBeschikbareTijd.vue`: de huiswerk-kleur-select was interactief vóór zijn eigen rehydratie-GET terugkwam, waardoor een gebruikerskeuze tijdens die race door de tragere GET kon worden overschreven — nu geblokkeerd tot de GET binnen is; een niet-401-laadfout op die GET was volledig onzichtbaar — nu een foutmelding; de kleur-opslaan-fout toont nu de serverboodschap i.p.v. een generieke tekst. `InstellingenVerborgenAgendaItems.vue`: een mislukte verwijdering was volledig stil (alleen console.error) — nu een zichtbare foutmelding, plus kruis-guards tussen toevoegen/verwijderen tegen een race met stale-response-overschrijving. `useTheme.ts`: `localStorage`-toegang (get én set) nu in try/catch — een worp kon voorheen de hele app op elke pagina laten stuklopen tijdens hydratie (Safari-privénavigatie, geblokkeerde siteopslag); `init()` past de gevalideerde staat nu altijd opnieuw toe op de DOM, i.p.v. blind te vertrouwen op het blocking head-script. `HamburgerMenu.vue` kreeg een `touchstart`-listener naast `click` voor betrouwbaardere buiten-tik-sluiting op iOS Safari. Typecheck/build/deploy/live-smoketest geslaagd.

**Niet gefixed, bewust:**
- **AD-10: `hasCalendarWriteScope` wordt opgehaald maar nooit gebruikt om een hernieuwd-toestemming-melding te tonen** (Architecture Auditor) — een gebruiker die al een huiswerkkleur gekozen heeft maar de write-scope introk/nooit gaf, heeft geen enkele affordance om die scope alsnog te verlenen behalve toevallig de kleur te wijzigen (`wijzigHomeworkColor`'s `needsReconsent`-pad is de enige trigger). Een echte fix is een nieuwe UI-affordance (een banner/knop), een productbeslissing over hoe dat eruitziet, niet een geïsoleerde bugfix.
- **AD-10/AD-6-spec-vs-code-tegenstrijdigheid op het taak-aanmaken-pad** (Architecture Auditor) — dezelfde soort gat als eerder al vastgesteld voor de shortfall-/energy-routes (AD-6's "nooit een stille terugval" bindt volgens de spine-tekst overal, maar `create-task.ts` documenteert bewust een uitzondering voor UJ-2). Geen calendar gekoppeld → de taak wordt stil met gedegradeerde planning opgeslagen zonder enige melding; een calendar-leesfout → een generieke "probeer het opnieuw" voor een fout die nooit zal verdwijnen. Vergt een spine-aanpassing (AD-10's laatste zin scopen tot UJ-6/7/8) of een echt signaal in `CreateTaskResponse` — een documentatie-/architectuurbeslissing, geen losse patch.
- **`is401`/`loggedIn` nu 17× gedupliceerd** (was 14 ná chunk E) — dezelfde groeiende mechanische-opschoning-kandidaat als eerdere chunks, nog steeds buiten scope voor een bugfix-ronde.
- **`useTheme.ts`'s `init()` synchroon aangeroepen in `app.vue`'s root-`setup()`** kan in theorie een Vue-hydratie-mismatch geven op een pagina die kleur-afhankelijke classes rendert (bv. `InstellingenUiterlijk.vue`'s actieve-swatch-markering) voor een gebruiker met een niet-standaard opgeslagen thema — de server rendert met de standaardwaarden, `init()` update de reactive state vóór die pagina hydrateert. Geen crash, hooguit een console-waarschuwing en een kort verkeerd gemarkeerde swatch; een echte fix (bv. `init()` pas ná mount laten draaien, of de swatch-markering in een `<ClientOnly>` zetten) is een grotere herstructurering dan deze bugfix-ronde rechtvaardigt voor een cosmetisch, niet-functioneel gat.
- **De twee instellingen-panelen se top-level `await useFetch`** blokkeert de hele componentopstart (Vue's Suspense-gedrag) — een tabwissel toont daardoor tot enkele seconden lang nog het vorige tabblad terwijl de nieuwe fetch loopt, met de eigen skeleton-laadstaat als dode code (nooit bereikbaar zolang de `await` nog loopt). Een fix (`{ lazy: true }` i.p.v. de top-level `await`) is precies het soort structurele wijziging aan deze twee componenten die de eerdere `KeepAlive`-poging (chunk E, teruggedraaid) ook al raakte — verdient een eigen gerichte vervolgronde, niet een losse patch in dit al brede chunk.
- **`wijzigHomeworkColor`'s `needsReconsent`-pad** (`window.location.href = ...`) laat de select nog even interactief tot de browser daadwerkelijk navigeert — een tweede kleurwijziging in dat venster kan mid-flight afgebroken worden. Laag risico (smal tijdvenster, geen dataverlies erger dan "de laatst zichtbare keuze komt niet overeen met wat opgeslagen is"), niet in deze ronde gefixed.

**Reden voor doorschuiven (algemeen):** zoals bij eerdere chunks — brede mechanische migraties, spine-niveau architectuurbeslissingen, of structurele herzieningen van componenten die net al een gerelateerde wijziging (en terugdraai) achter de rug hebben. Dit was de laatste chunk van de volledige-codebase-reviewcampagne (server + alle app/pages + app/components/composables/plugins).

**Ronde 2-4 (chunk F her-review, 2026-09-07):** de 3 agents zijn drie keer opnieuw ingezet. Twee zelf-geïntroduceerde regressies gevonden en gefixed: (1) de nieuwe `onBeforeRouteLeave`-guard op `TaakFormulier.vue` liet de bevestigingsmodal twee keer verschijnen bij Sluiten/Annuleren (de knop-route riep `goBack()` aan, wat de guard opnieuw triggerde) — gefixed met een eenmalige `bypassLeaveGuard`-vlag; (2) de globale `skipSessieVervalRedirect`-vlag kreeg geen `onUnmounted`-reset, dus bleef na een sessieverval-tijdens-opslaan gevolgd door het verlaten van het formulier (zonder op te slaan) voor de rest van de SPA-sessie op `true` staan, wat elke latere 401 stil zou laten inslikken. Een tussenronde-poging om de vak-wijziging-dialoog in diezelfde guard te negeren kreeg per ongeluk de omgekeerde polariteit (liet de navigatie ongehinderd door i.p.v. te blokkeren) — in de laatste ronde gecorrigeerd naar "dialoog sluiten, dan de gewone dirty-check laten gelden". Kleinere fixes: `homeworkColorLoading` van een data-aanwezigheid-check naar een `status`-gebaseerde check (voorkomt een theoretisch permanent-geblokkeerde select bij een lege 200-respons); `InstellingenVerborgenAgendaItems.vue`'s `toevoegen` gebruikt nu ook `foutmeldingUit()` (de enige van de drie panelen waar de server een écht bruikbare, niet-generieke foutmelding kent); twee foutieve/misleidende code-comments gecorrigeerd (een verzonnen server-validatieregel, de plugin se overclaim over "élke 401"); `dismissNeedsSubjectChange()` aangeroepen i.p.v. een rauwe toewijzing zodat de vak-suggesties-datalist niet blijft hangen als de leave-modal geannuleerd wordt. Typecheck/build/deploy (4x)/live-smoketest geslaagd.

**Campagne afgerond (2026-09-07):** dit was de laatste chunk van de volledige-codebase-reviewcampagne (chunks 1-3: server/, chunk D+E: alle app/pages/, chunk F: app/components/+composables/+plugins). Alle drie de agents bevestigen chunk F gesloten. Zie de story se Change Log voor de campagne-brede afsluitende observatie van de Architecture Auditor (gekopieerde idiomen en "state overleeft zijn component" als de twee dominante, herhaalde defectpatronen over alle chunks heen) — deze twee zijn de aanbevolen hoogste-waarde vervolgstappen, geen van beide in deze campagne zelf aangepakt.

## Opgelost: `is401`/`is404`-consolidatie (2026-09-07)

De hierboven herhaaldelijk genoteerde `is401`-duplicatie (17× letterlijk identieke functie over 14 bestanden, plus één `is404`-tweeling in `sessie/starten.vue`) is opgeschoond: beide functies staan nu eenmalig in `app/composables/httpError.ts` (Nuxt auto-import, geen expliciete imports nodig per bestand). Alle 14 lokale kopieën verwijderd, inclusief de nu overbodige `import type { FetchError } from 'ofetch'` in de bestanden waar dat type verder nergens gebruikt werd (`index.vue`, `schoolsessies.vue`, `taken/index.vue`, `taken/[id]/index.vue`, `taken/[id]/bewerken.vue`, `sessie/starten.vue`, `sessie/actief.vue`, `herstel/energie-voorstel.vue`); de import bleef staan waar `FetchError` nog voor andere doeleinden gebruikt wordt (`week/index.vue`, `herstel/tekort-oplossen.vue`, `herstel/reden-kiezen.vue`, `InstellingenBeschikbareTijd.vue`, `InstellingenVerborgenAgendaItems.vue`, `TaakFormulier.vue`). Zuiver mechanisch — geen gedragswijziging. Typecheck/build/deploy/live-smoketest geslaagd.

## Opgelost: server-side `envelope()`-consolidatie (2026-09-07)

De campagne-brede afsluitende observatie noemde ook "twee incompatibele `envelope()`-vormen over ~34 routebestanden" — bevestigd en opgelost. Vorm A (25 bestanden) nam `event` als eerste argument en riep `setResponseStatus` intern aan; vorm B (10 bestanden: `week.get.ts`, de twee `week/[date]/suggestion/*`-routes, `scheduling/startup-check.post.ts`, `day/shortfall.post.ts`, `day/energy-proposal.post.ts`, de drie `day/shortfall/recommendations/[id]/*`-routes, `day/energy-proposal/confirm.post.ts`) nam geen `event` en vereiste een losse `setResponseStatus`-aanroep vóór elke `return envelope(...)` — functioneel identiek (beide zetten de statuscode correct, geverifieerd per call site), puur twee stijlen. Geconsolideerd naar één `server/utils/envelope.ts` (Nitro auto-import). Alle 35 lokale definities verwijderd; bij vorm B ook de nu overbodige losse `setResponseStatus`-regels vóór elke aanroep weggehaald en `event` als eerste argument toegevoegd op alle call sites. Zuiver mechanisch — geen gedragswijziging. Typecheck/build/deploy geslaagd; live-smoketest bevestigt onveranderd 401/302-gedrag op `/`, `/api/week`, `/api/tasks`, `/api/day/shortfall`.

## Opgelost: dode `Notification`-vorm verwijderd, AD-6 herzien (2026-09-07)

De campagne-observatie "AD-6's Notification-vorm heeft nul aanroepers" is uitgezocht en opgelost — geen bugfix maar een documentatie-/opschoningscorrectie. **Wat bleek:** Story 6.1 bouwde `server/domain/notification.ts` (`Notification`/`NotificationAction`-shape) + `buildShortfallNotification` omdat AC #2's tekst letterlijk "gebruikt uitsluitend de Notification-shape" eiste. Story 6.2 koos vervolgens bewust (Open Question #1, akkoord Hillebrand destijds) om `POST /api/day/shortfall` zijn eigen, rijkere `ShortfallResponse` te laten teruggeven (per aanbeveling een `id`/`tier`/`gainMinutes`, nodig voor de losse accept/reject-kaarten in de UI) — een geldige, akkoord-bevonden invulling van dezelfde AD-6-regel ("gebruikersgericht bericht ≠ technische error-envelope"), alleen zonder de oorspronkelijk voorziene gedeelde vorm. De generieke shape werd daarmee dode code; nooit hersteld of alsnog gewired.

**Aan Hillebrand voorgelegd of de Notification-vorm alsnog gewired moest worden** (bijv. als een niet-blokkerende banner op Home) — bleek bij doorvragen niet zinvol: Home heeft al een verplichte, blokkerende full-page-flow (opstart-check → hard redirect naar `/herstel/tekort-oplossen` zodra er een tekort is), waardoor een banner ernaast vrijwel nooit zichtbaar zou zijn (op één smal escape-hatch-venster na). Besloten: **dode code opruimen**, niet een nieuwe, overlappende UI bouwen.

**Fix:** `buildShortfallNotification` en `server/domain/notification.ts` verwijderd. `RecommendationTier` (de enige nog-gebruikte export) verplaatst naar `server/domain/scheduling/shortfall.ts` zelf (zijn enige overgebleven consument). Twee verwijzende comments in `shared/types/tasks.d.ts`/`shortfall.d.ts` gecorrigeerd (verwezen naar het nu-verwijderde bestand). **`ARCHITECTURE-SPINE.md`'s AD-6 herzien:** de regel eist niet langer één gedeelde `Notification`-wrapper, alleen dat gebruikersgerichte meldingen nooit via de technische error-envelope lopen — elk endpoint mag zijn eigen domeinspecifieke succesrespons kiezen (zoals `ShortfallResponse` al deed). AD-10 en de Consistency Conventions-tabel se kruisverwijzingen naar "AD-6's Notification-shape" ook bijgewerkt. Typecheck/build/deploy/live-smoketest geslaagd.

## Opgelost: signaal bij taak aanmaken zonder gekoppelde beschikbare-tijd-agenda (2026-09-07)

De resterende helft van "AD-10/AD-6-spec-vs-code-tegenstrijdigheid op het taak-aanmaken-pad" — het stille-degradatie-gedrag zelf (niet de architectuurtekst, die is met de AD-6-herziening hierboven al recht getrokken). Vóór deze fix kreeg Evelien geen enkel signaal als ze een taak aanmaakte zonder gekoppelde beschikbare-tijd-agenda: `planSessionSlots` valt dan stil terug op een gestapelde planning vanaf een vast anker (16:00) i.p.v. echte Calendar-blokken — een bewust geaccepteerde terugval (AD-10), maar zonder enige melding op déze plek (in tegenstelling tot de instellingenpagina se `avail-no-calendar-notice`).

**Fix:** `createTask()` (`server/domain/tasks/create-task.ts`) doet nu één extra lichte DB-lezing (`getUserById`, geen Calendar-call) en bouwt een `warnings: { type, message }[]`-array (zelfde lichte vorm als `HomePlanResponse.calendarWarnings`, geen Notification-plumbing — AD-6 bindt aan UJ-6/7/8, niet aan UJ-2). Nieuw `CreateTaskResult`-type (`shared/types/tasks.d.ts`, `CreateTaskResponse` + `warnings`) — bewust een apart type, niet `CreateTaskResponse` zelf verbreed, omdat `PUT /api/tasks/{id}` (bewerken) dat type ook hergebruikt en buiten scope van deze fix valt. `TaakFormulier.vue` toont de waarschuwing(en) onder de "Taak opgeslagen!"-bevestiging en verlengt de automatische terugnavigatie-timer van 800ms naar 4000ms zodat er tijd is om 'm te lezen. `school-sessions.post.ts` (het batch-invoerscherm, andere resultaatvorm) roept `createTask()` ook aan maar draagt de waarschuwing bewust niet door — buiten scope van deze fix.

**Live geverifieerd** (op verzoek van Hillebrand, met expliciete toestemming voor een tijdelijke database-wijziging): het account achter de actieve browsersessie had een gekoppelde agenda ("studie tijd") — de instellingenpagina zelf bleek geen ontkoppel-optie te bieden (bevestigt het al bekende "geen ontkoppel-endpoint"-gat hieronder), dus is `availabilityCalendarId` tijdelijk via een direct DB-script op `null` gezet. Een testtaak aanmaken toonde de waarschuwing correct (eerste poging toonde 'm niet door een verouderde browser-JS-bundle — na een harde herlaad wél zichtbaar; de rauwe API-respons via de browser se eigen `fetch` bevestigde `warnings` al eerder correct gevuld was). Vier testtaken aangemaakt tijdens het testen, allemaal verwijderd; de agenda-koppeling teruggezet naar "studie tijd" via de instellingenpagina zelf (die richting wordt wél ondersteund) en met een verse paginalaad bevestigd. Typecheck/build/deploy geslaagd.
